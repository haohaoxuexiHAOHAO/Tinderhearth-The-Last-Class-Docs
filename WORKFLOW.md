---
type: governance
status: active
owner: project
last_verified: 2026-09-18
---

# 工作流规则

本页是**进程规则的权威源**：流水线形状、闸门、提交纪律、执行体。产品事实在正典，跨域取舍在 [ADR](./decisions/README.md)，每一步的具体做法在 `.kiro/skills/` 的技能里。

分工写死，避免出现第二份会漂移的说法：**本页写「规则是什么」，技能写「怎么执行」。** 技能与本页冲突时以本页为准并修掉技能。

**开发模式由 [ADR-0009](./decisions/ADR-0009-编辑器主导的开发模式.md) 定**：代理写业务逻辑，作者在 Godot 编辑器里管场景、碰撞区域、素材与参数。本页的流水线与执行体都是按那个分工收过一轮的结果；骨架的历史理由见 [ADR-0006](./decisions/ADR-0006-工作流骨架选型.md)。

## 1. 流水线

```
/prd → /to-issues → 实现 → /ship-it
         ↑                    ↓
   /to-design（跨域取舍时）  作者实机验收
   /write-adr（难撤销时）
```

| 阶段 | 技能 | 产出 |
| --- | --- | --- |
| 需求 | `/prd` | `spec/prd-<slug>.md`，含验收清单。**写轻** —— 它是给作者确认范围的，不是实现契约 |
| 系统契约 | `/to-design` | `design/<系统名>.md`（[SYSTEM 模板](./templates/SYSTEM.md)的固定骨架，节数看那一份）。**活文档**：一个系统一份，随实现更新 |
| 方案论证 | `/to-design` | `design/proposals/<slug>.md`（[DESIGN 模板](./templates/DESIGN.md)的 11 节），含被放弃方案与理由。**一次性**：结论迁出后归档 |
| 跨域取舍 | `/write-adr` | `decisions/ADR-NNNN-*.md`，影响多系统或难以撤销时才写 |
| 拆条目 | `/to-issues` | `spec/issues/issue-<ID>-<slug>.md` + 台账加行 |
| 评审 | `/review-it` | 三个维度：正确性、命名可读性、有没有把推断当事实。接受与否决都写理由 |
| 提交 | `/ship-it` | 提交一条改动。实现笔记写进条目与提交信息，不另开一步 |

**同一个技能产出两个体裁，判据是寿命**：一个系统的结构与接口要反复回来看 → 系统文档；一条路走不走得通的论证一次说完 → 提案。规则与被否决的方案见 [ADR-0010](./decisions/ADR-0010-文档体裁与系统文档归位.md)；系统文档的骨架里那一节「术语」见 [ADR-0019](./decisions/ADR-0019-系统文档骨架加术语一节.md)。

跳步允许，但要说明为什么。琐碎改动（改字、小缺陷、纯文案）直接做，不走流水线。

判据：这件事做完，是否会有人在三周后问「当时为什么这么定」。会，就留下文字。

## 2. 闸门

1. **PRD 未经作者确认前不改代码。** 待确认问题清空才算确认。
2. **只列无法由正典或 ADR 裁决的问题给作者。** 有客观优劣的技术选择自己定并写理由。
3. **未经作者明确要求不提交、不推送。**

**表现与手感由作者实机验收，没有机器判据**（`ADR-0009`）。代理不再写图形探针去证明画面上的事。

## 3. 反漂移：事实只许有一个家

规则与理由见 [ADR-0009](./decisions/ADR-0009-编辑器主导的开发模式.md)。要执行的是下面五条：

1. **散文只写理由，不写状态。** 状态类陈述（「X 还没做」「当前是 Y」）按定义会变；理由类陈述（「为什么选 A 不选 B」）时间无关。
2. **状态只在[待办台账](./spec/issues/README.md)一处；别处写链接，不复述。** 这样「过期」就从查不出来的错变成查得出来的断链。
3. **完成或归档一个编号时，按编号搜两个仓加 `.kiro`**，把提到它的地方一起改掉。只搜自己当时在改的那个仓是不够的 —— `DOC-9` 就是这么漏掉一半的。
4. **只写结果，不写过程。** 删这些：日期、「作者…定／确认／裁定」、引号原话、「本轮／当时／一度」、多版本沿革、历史验收数字。留这些：事实、理由、「实测」二字（用来分清实测与推断）。过程叙述除了占字数没有别的作用，而且历史验收数字会静默过期 —— 台账里那些 `verify 6/6`、`259/259` 在门禁瘦成五步、测试涨到 352 之后全成了错的。

5. **引用按名，不按位置与序号。** 交叉引用会过期，而大部分过期是引用方式选错造成的，所以四条写死，前三条有守卫：**① 引用某一节写「文档 · 节名」，节名必须是目标文件里真的标题或加粗小标题**；**② 不写「第 N 步」去引用别处的编号清单，按步名引用** —— 插一步就会让全部序号静默指错；**③ 反引号里的代码路径必须在某个仓里真的存在**；④ **不写自计数**（「三条」「四项」），要写就保证数目与清单一致 —— 加一条忘了改数字是最常见的过期。

**两条不能根治的边界，写明是为了不假装它们已被解决：**

- **描述代码行为的散文**（例如代码仓 `ARCHITECTURE.md` 的三轴表）权威来源是代码，任何描述都是拷贝。对策只有三件：这类散文写少写粗、细节放进紧贴代码的注释、碰到就复核。
- **量级事实的散文副本**（可建造区格数、名册人数、城区图张数这类）。机器判不出两个数字说的是不是同一件事，所以守卫做不了。对策是同一条：**数字只在它的家写一次，别处写链接不复述数字**；非要复述就准备好它会过期。**本条的举例刻意不带数字** —— 在规则里举一个具体的数就是又给它开一个家，而这条规则最容易被自己违反的地方正是它的举例。

## 4. 归档

归档的实质是把一次需求的过程叙述压成结论，让活文档只留现行事实。不做这一步，文档必然膨胀到没人读（前作长到 144 份就是这么来的）。

准出就这几条：

1. **本轮现行事实写进了哪份正典／ADR／规范的哪一节？给链接。** 填不出来说明结论只活在过程文件里，归档后会丢。
2. **本轮新记的欠账都落进[待办台账](./spec/issues/README.md)了。** 超边界记下的那些，以及验收里剩给作者的那些，归档前都要在台账有自己的一行 —— 归档件只读，留在里面的待办没有人查得到（`DOC-101` 就是从 `DOC-97` 的验收里拎出来的）。
3. `python tools/check_docs.py` 0 FAIL。

归档件移入 **`archive/spec/<编号>-<slug>/`，一个需求一个目录**，文件按类型命名（`prd.md`、`issue.md` 等）。文件头 `status: archived`，文首 20 行内写明只读边界。正典、设计、制作规范与 ADR 引用 `archive/` 时同行标注「历史背景·非依据」。

**已接受的 ADR 与归档件不得用今天的理由改写当时的背景。** 取代关系写进双方文件头的 `supersedes`／`superseded_by`。

## 5. 命令与提交纪律

### 命令

- **改文件只用编辑工具**，禁止 shell 或脚本做批量文本替换 —— 看不到上下文，diff 也无法逐处复核。
- **会做第二次的操作写成 `tools/` 下的 Python 入口**，不要把同一条现场命令贴第二次。
- **不靠 shell 重定向攒日志**，让 Python 自己写 UTF-8。
- **临时文件与临时资源一律放工作区根的 `temp/`，用完即删；临时进程用完即退。** 不写 C 盘（`%TEMP%`、`%APPDATA%`）—— 那里的残留看不见，只会越堆越多。能复用的东西不算临时资源，它该落成 `tools/` 下的入口。唯一例外是被验证的对象本身就在固定位置（例如 mod 必须放 Godot 的 `user://`）：在真实位置验，验完立刻删并写明删了什么。
- **工作区根（含 `temp/` 与 `.kiro/`）不在任何仓库里，但它是稳定的**：本机固定、不换机器、不会被清空。`tools/` 入口进仓库的理由是**可复核与自包含**，不是怕丢。
- **`.kiro/` 不建版本库，此事已定，不要再提。**
- 排缺陷时**无法确定根因就不要连改**：加临时诊断输出 → 让作者实机复现并贴回结果 → 据实定位 → 精准修 → **删除诊断代码**。凭猜测连改多处会同时引入新缺陷并掩盖原因。

### 提交

**单人 `main`：不建分支、不开 PR。**

- 提交信息中文 `<模块>：<简述>`，全角冒号。一次提交聚焦一件事。
- 只 `git add -u` 或明确列出文件，**禁止 `git add .`**。
- 提交前自己跑一次那个仓的检查入口 —— **没有任何钩子会替你跑**（见本节末）。
- `force push`、`reset --hard`、删分支、清理未跟踪文件须先单独获许可。
- 两个仓库分别提交，Git 命令在各自目录内执行（工作区根不是仓库）。先设计仓、后代码仓，这样代码的提交信息可以引用已定稿的文档。

**这一节现在只有规则、没有守卫**：原先拦 `git add .` 与未授权推送的 `.kiro/hooks/command-guard.json` 随 `ADR-0009` 一起删了，**推送前跑文档检查的那个 git 钩子是同一批删的**（`ADR-0004` 的「检查时机」那一条因此作废，取代备注写在它的文件头）。代价写明 —— 违反它不会被当场拦下，而检查跑没跑过事后也看不出来。

## 6. 执行体

规则要么有能自动检出违反的守卫，要么写明为何只能人工验收。

| 规则 | 执行体 |
| --- | --- |
| 文件头、断链、归档边界、单一台账、编号引用得出来 | `python tools/check_docs.py` |
| 体裁归位：`type: system` 在 `design/` 根、`type: design` 在 `design/proposals/`（[ADR-0010](./decisions/ADR-0010-文档体裁与系统文档归位.md)） | 同上 |
| §3 的引用纪律前三条：节名指得到真标题、不按序号引用清单、反引号里的代码路径真的存在 | 同上 |
| `canon/` 与 `design/` 不得拿**已关闭的编号**当待定项的归处（`DOC-100`） | 同上。关闭＝台账状态词「已完成」或在已归档那张表里；**唯一的放过形式是写成指台账的链接**，那样过期从查不出来的错变成一跳就看见的重定向 |
| 量级事实的散文副本、一节内容写得对不对 | **只能人工验收**，理由见 §3 那两条边界 |
| 代码仓：行尾、构建、规则层测试、发行包不含源码、产物能启动 | 代码仓 `python tools/verify.py`。测试比对条数（运行器自报 ≙ 静态扫描），导出解包逐条看清单 |
| 借来的素材与占位件不得进发行包 | 同上的 `--release`：按目录判（`assets/downloaded/`、`assets/placeholder/` 下的一律不许进包） |
| 字体授权没变、上游字形来源仍可查、文案里的字没缺 | `python tools/audit_fonts.py`（`ART-2`／[ADR-0008](./decisions/ADR-0008-中文像素字体选型.md)） |
| 数值模型的全部平衡判据；文档里抄的参数值与抄的算出来的量都不与实际分叉 | `python tools/simulate_week.py`（加 `--check-doc`）。改 `design/numeric-model-params.json` 或那页公式就跑 |
| 表现、手感、配色、画面「好不好看」 | **只能人工验收**：作者在 Godot 里实机看（`ADR-0009` 的分工） |
| 像素规范 §9「透明度只全透明或全不透明」 | **只能人工验收**：作为人工标准保留，守卫随 `ADR-0009` 删 —— 画得不对实机一眼能看出来 |
| 场景与节点树、碰撞区域、素材裁切与导入、检查器里的参数值 | **只能人工验收**：同上，这些是作者的工作面，机器不该去判 |
| 玩法未定不得当既定事实引用 | **只能人工验收。** 检查器无法判断一句话是否把推断当成了事实 |
| 临时资源用完即删 | **只能人工验收**：`temp/` 在工作区根、不在任何仓库内，检查器看不到它。可观察形式是每轮验收报告 `temp/` 是否为空 |

**`verify.py` 与 `check_docs.py` 自己没有自证入口了**（两个 `selfcheck_*` 随 `ADR-0009` 删除，「改了守卫必须自证」那条纪律同时取消）。代价：它们坏了没有东西能发现，只能靠改它的人复核。

## 7. 结论必须有依据

结论、推荐与数值都要能指回事实来源（代码、实测输出、权威文档、写得出的算式）。拿不到就**明说「未验证」或「是推断」并给出验证办法**，不用语气填补证据空缺。与新事实矛盾时主动撤回。

**只汇报实际执行过的验证。** 「跑了回归」和「应该跑回归」是两件事。沿用旧结果必须注明日期。

**命令退出码为 0 不等于成功**，必须同时看脚本自己打印的显式摘要。两者矛盾时先查原因，最常见的是跑在了旧产物上。
