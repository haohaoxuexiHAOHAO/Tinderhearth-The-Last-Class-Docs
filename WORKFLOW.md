---
type: governance
status: active
owner: project
last_verified: 2026-08-26
---

# 工作流规则

本页是**进程规则的权威源**：流水线形状、闸门、配额、归档准出、提交纪律。产品事实在正典，跨域取舍在 [ADR](./decisions/README.md)，每一步的具体做法在 `.kiro/skills/` 的技能里。

分工写死，避免出现第二份会漂移的说法：**本页写「规则是什么」，技能写「怎么执行」。** 技能里出现与本页冲突的规则时，以本页为准并修掉技能。

骨架选型的理由见 [ADR-0006](./decisions/ADR-0006-工作流骨架选型.md)。

## 1. 流水线

```
/prd → /prd-to-spec（可选）→ /to-design（跨域取舍）→ /to-issues
                                                        ↓
              /verify-round ← /ship-it ← /note-it ← /review-it ← 实现
                    ↓                                    ↑
              /ship-archive                          /loop-it 批量驱动
```

| 阶段 | 技能 | 产出 |
| --- | --- | --- |
| 需求 | `/prd` | `spec/prd-<slug>.md`，含 US-NNN 与 FR-N 编号、可验证验收清单 |
| 技术规格 | `/prd-to-spec` | `spec/spec-<slug>.md`，实现契约 |
| 设计论证 | `/to-design` | `design/<slug>.md`，含被放弃方案与理由 |
| 跨域取舍 | `/write-adr` | `decisions/ADR-NNNN-*.md` |
| 拆条目 | `/to-issues` | `spec/issues/issue-<ID>-<slug>.md` + 索引加行 |
| 批量驱动 | `/loop-it` | 按依赖顺序逐条实现，检查点落 `.loop-state.json` |
| 评审 | `/review-it` | 接受／否决逐条写理由，改完重跑到干净 |
| 实现笔记 | `/note-it` | 往 issue 文件追加设计决策／偏离／权衡／待确认 |
| 提交 | `/ship-it` | 提交单个 issue |
| 验收 | `/verify-round` | 只含实际跑过的命令与结果 |
| 收尾 | `/ship-archive` | 蒸馏、归档、变更日志加一行 |

跳步是允许的，但要说明为什么。琐碎改动（改字、小缺陷、纯文案）直接做，不走流水线。

判据：这件事做完，是否会有人在三周后问「当时为什么这么定」。会，就走流水线。

## 2. 闸门

以下四条是硬规则，没有例外。

1. **PRD 未经作者确认前不写 SPEC，SPEC 或 PRD 未确认前不改代码。** 待确认问题清空才算确认。
2. **只列无法由正典或 ADR 裁决的问题给作者。** 有客观优劣的技术选择自己定并写理由。
3. **测试没过不请作者实机确认。** 那是浪费他的时间。
4. **未经作者明确要求不提交、不推送。**

**玩法结构已定**，见 [canon/gameplay/](./canon/gameplay/玩法定位.md) 四份正典 —— 依赖玩法结构的需求现在可以开工。**但数值仍未设计（`GP-2`）**：属性公式、成长曲线与判定公式在它归档前不得当成既定事实引用。

## 3. 配额

检查器同时看行数与字符数：前者软上限只警告，后者硬上限判失败。

| 文档 | 行（软／硬） | 字符（软／硬） |
| --- | --- | --- |
| 普通文档 | 600／900 | 40,000／60,000 |
| `WORKFLOW.md` | 150／200 | 12,000／16,000 |
| `spec/issues/README.md` | 200／250 | 20,000／25,000 |
| `spec/` 下的 PRD／SPEC／issue | 250／350 | 20,000／28,000 |

PRD／SPEC／issue 的「待确认问题」一节不计入配额。确需超限时在文件头写 `quota_exempt: <具体理由>`，全库最多三份豁免。

**拆分沿主题或职责边界。** 只有变更日志这类只追加台账可以按时间拆分。

逐格限制：`spec/issues/README.md` 每格 ≤360 字符，`变更日志归档.md` 每格 ≤220 字符。超出的解释回到该行链接的正文。

## 4. 归档与蒸馏

归档的实质是把一次需求的全部过程叙述压缩成「一处摘要 + 两处链接」，让活文档只留现行结论。不做这一步，文档必然膨胀到没人读（前作长到 144 份就是这么来的）。

**归档三问，缺一项不得归档：**

1. 本轮现行事实写进了哪份正典／ADR／规范的哪一节？给链接。填不出来说明结论只存在于过程文件里，归档后会丢失。
2. 遗留待办进入 `spec/issues/` 哪几个编号？「以后再说」不算记账。
3. 过程叙述是否已删除，只留归档小结？

准出＝三问填齐 + 现行事实已进权威源 + 变更日志**加一行** + `python tools/check_docs.py` 0 FAIL。

归档件移入 `archive/`，文件头 `status: archived`，且**文首 20 行内写明只读边界**。正典、设计、制作规格与 ADR 引用 `archive/` 时必须同行标注「历史背景·非依据」。

蒸馏必须**逐段对照原文**。凭记忆重建会悄悄丢事实或改变结论，而且没人能发现。

## 5. 命令与提交纪律

### 命令

- **改文件只用编辑工具**，禁止 shell 或脚本做批量文本替换。批量替换看不到上下文，而且 diff 无法逐处复核。
- **会做第二次的操作写成 `tools/` 下的 Python 入口**，不要把同一条现场命令贴第二次。
- **不靠 shell 重定向攒日志**，让 Python 自己写 UTF-8。
- **多步逻辑写 Python 直接干活**，不要在 Python 里回头调 PowerShell。
- **临时文件与临时资源一律放工作区根的 `temp/`，用完即删；临时进程用完即退。** 不写 C 盘（`%TEMP%`、`%APPDATA%` 之类）—— 那里的残留看不见，只会越堆越多，验收时也没法一眼核对。能复用的东西不算临时资源，它该落成 `tools/` 下的入口并写明用途。**唯一例外**是被验证的对象本身就在某个固定位置（例如 mod 必须放 Godot 的 `user://`）：那就在真实位置验，但验完立刻删，并在验收里写明删了什么。
- **工作区根（含 `temp/` 与 `.kiro/`）不在任何仓库里，但它是稳定的**：本机固定、不换机器、不会被清空。所以「工作区根的东西会不会丢」这件事不必再论证。`tools/` 入口该进仓库的真实理由是**可复核与自包含** —— diff 看得见改了什么，且只 clone 一个仓的人也用得上；不是怕丢。
- 排缺陷时**无法确定根因就不要连改**：加临时诊断输出 → 让作者实机复现并贴回结果 → 据实定位 → 精准修 → **删除诊断代码**。凭猜测连改多处会同时引入新缺陷并掩盖原因。

### 提交

**单人 `main`：不建分支、不开 PR。** 这是与上游 goal-workflow 最大的差别，`/ship-it` 因此只保留提交这一步。

- 提交信息中文 `<模块>：<简述>`，全角冒号。一次提交聚焦一件事。
- 只 `git add -u` 或明确列出文件，**禁止 `git add .`**。
- 保留钩子，不用 `--no-verify`。
- `force push`、`reset --hard`、删分支、清理未跟踪文件须先单独获许可。
- 两个仓库分别提交，Git 命令在各自目录内执行（工作区根不是仓库）。先设计仓、后代码仓，这样代码的提交信息可以引用已定稿的文档。

## 6. 执行体

规则要么有能自动检出违反的守卫，要么写明为何只能人工验收。两者都没有的规则会漂移，而且没人会发现。

| 规则 | 执行体 |
| --- | --- |
| 文件头、配额、断链、归档边界、逐格限制、入口可达、单一台账 | `python tools/check_docs.py` |
| 代码仓：构建、测试、导出与产物校验串成一条，门禁只调它 | 代码仓 `python tools/verify.py`（`ENG-3`）。测试比对条数、导出解包看清单，退出码之外另有量具 |
| 上面那个验收入口本身没退化 | 代码仓 `python tools/selfcheck_verify.py`（注入真实缺陷形状、还原后复验、自报覆盖量） |
| 工作区行尾符合 `.gitattributes` | 同上（`--fix-eol` 按声明改回来）。`git diff --check` 当不了守卫，理由见[踩坑记录 28](./reference/踩坑记录.md) |
| 上面那个检查器本身没退化 | `python tools/selfcheck_docs_guard.py` |
| 一轮对话结束时复查文档 | `.kiro/hooks/docs-gate.json` |
| 推送前复查文档 | `.githooks/pre-push` |
| `git add .`／`--no-verify`／shell 批量替换 | `.kiro/hooks/command-guard.json`（直接拦下） |
| 未授权提交推送／`reset --hard`／force push／`git clean` | 同上，转成作者当场确认 |
| 上面那个命令守卫本身没退化 | `python .kiro/hooks/selfcheck_command_guard.py` |
| 每会话恢复基线 | `.kiro/hooks/session-baseline.json` |
| 玩法未定不得当既定事实引用 | **只能人工验收。** 检查器无法判断一句话是否把推断当成了事实 |
| 验收标准可观察、纯观感项实机确认 | **只能人工验收。** 自动检查判不了配色、手感和「好不好玩」 |
| 临时资源用完即删 | **只能人工验收**：`temp/` 在工作区根，不在任何仓库内，检查器看不到它，而让检查器去翻父目录会让它依赖工作区布局。可观察形式是**每轮验收报告 `temp/` 是否为空** |

改了守卫必须**自证**：故意造一个真实缺陷形状的违反 → 确认判定失败且报错指向正确位置 → 改回来确认恢复通过。守卫还必须自报覆盖量；登记了检查目标却没实际执行，就算失败。

文档检查器的自证已经做成入口：`python tools/selfcheck_docs_guard.py`。它枚举 `check_docs.py` 里所有 `check_*` 函数，**任何一条既没有自证用例、也没写豁免理由就判失败** —— 所以新增检查却不自证会被当场拦住，而不是等到某天发现守卫一直在空转。

## 7. 结论必须有依据

结论、推荐与数值都要能指回事实来源（代码、实测输出、权威文档、写得出的算式）。拿不到就**明说「未验证」或「是推断」并给出验证办法**，不用语气填补证据空缺。与新事实矛盾时主动撤回。

**只汇报实际执行过的验证。** 「跑了回归」和「应该跑回归」是两件事。沿用旧结果必须注明日期。

**命令退出码为 0 不等于成功**，必须同时看脚本自己打印的显式摘要。两者矛盾时先查原因，最常见的是跑在了旧产物上。
