---
type: design
status: draft
owner: project
last_verified: 2026-09-20
upstream: canon/gameplay/玩法定位.md
---

# 一份载体装下全部修饰：能并存的归这里，一次只能一个的归动作机

> **正典层级**：第 5 级（系统细化）。与[玩法正典](../canon/gameplay/玩法定位.md)冲突时以正典为准；**全部时长、量级与阈值归 `GP-2`／`GP-6`，本页一个都不定。**
>
> 返回 [总索引](../README.md)｜[专项设计索引](./README.md)｜相关：[角色动作状态机](./角色动作状态机.md) · [玩法定位](../canon/gameplay/玩法定位.md) · [战斗与关卡](../canon/gameplay/战斗与关卡.md)｜台账：[`GP-18`](../spec/issues/README.md)

## 摘要

角色身上能同时挂着的东西全部走**同一份载体**：硬直与无敌（按帧）、伤病与营养不良（按天）、失衡增益与战斗授课（永久）、宝物词条、常驻光环、异常状态，以及**什么都不改、只显示一行字的纯描述状态**。

**分类只有三类，判据是「它动不动动作机、改不改数值」**：控制类锁操作、参数类只改数值或判定、纯描述类零效果。**与[动作机](./角色动作状态机.md)的接缝只有一条**：动作机每帧问「有没有锁操作的效果在场」，有就切被控姿态 —— 时长永远由本页这一侧计。

还有一条不太常见但本作必须有的能力：**载体要能表达「显示成这样，实际是另一回事」**。主角的状态栏是伪造的，而那不是给他开的特例，是载体本来就该有两个字段。

## 背景与动机

正典已经裁定「全部临时性与持续性效果走同一套状态系统」，并点名了它要装的东西：宝物负面词条、纯负面特质、断粮与熬夜降低的体力上限、伤病、战斗内的硬直与无敌与失衡、常驻光环、异常状态（[玩法定位 · 跨系统约定](../canon/gameplay/玩法定位.md)）。裁定的理由也写在那儿：不统一，「屏障 + 光环 + 负面词条同时生效算谁的」就没人说得清。

**但「同一套」不等于「同一份文档」，而混着写已经出过一次问题。** 此前这些内容挂在[角色动作状态机](./角色动作状态机.md)的一个小节里，于是那份讲帧级流转的文档里出现了「考差的学生身上那条什么都不影响的状态」——读起来像「受击是个 buff」，方向就歪了。分开之后两边各答一个问题：那边答「这一帧角色能做什么」，本页答「这个角色身上现在有什么」。

**载体只有一份，文档分两份。** 这不是妥协：载体一份是正典要求，文档两份是因为它们的读者不同 —— 改战斗手感的人只看那边，加一个跨日状态的人只看这边。

**三处新需求是这一版才出现的**，它们决定了载体的形状：活跃失衡作为永久增益（[世界观](../canon/world/世界观.md)）、教学与考试产生的跨日状态（[人物](../canon/characters/人物.md)）、以及用状态栏让角色显得活着——这需要零效果的纯描述状态。

## 上游约束

- [玩法定位 · 跨系统约定](../canon/gameplay/玩法定位.md)：全部临时与持续效果走同一套状态系统，每条带「可否被解除」。**这是本页存在的理由，也是「载体只能有一份」的来源。**
- [战斗与关卡 · 异常状态](../canon/gameplay/战斗与关卡.md)：异常状态与硬直、无敌、失衡、光环、词条同套；分可解除（中毒/灼烧）与不可解除（硬直/失衡）。**约束分类必须按「锁不锁操作」而不是按「战斗内/外」切。**
- [世界观 · 活跃失衡的规模](../canon/world/世界观.md)：失衡以**永久增益**的形式改造一个生命，形状由掌握程度与失衡高低共同决定。**约束载体必须支持无时长的永久项，且量级可正可负。**
- [人物 · 面具、右臂与伪装](../canon/characters/人物.md)：主角的手环读数是他伪造的，真实增益只在展示全力时显出来。**约束载体分「显示什么」与「实际是什么」两个字段。**
- [人物 · 教学](../canon/characters/人物.md)：考差的学生得到一条什么都不影响的描述文本；连续被授课收益归零。**约束纯描述类与「按天」时长。**
- [ADR-0009 · 分工边界](../decisions/ADR-0009-编辑器主导的开发模式.md)：参数唯一来源是场景与资源，代码不留能用的默认值。**约束本页定出的每个量纲将来落成外置数据。**
- [角色动作状态机](./角色动作状态机.md)：接缝的另一端。**姿态归那边，时长归这边。**

## 当前事实与证据

代码里的载体是 A1 留下的最小形态（代码仓 `rules/Combat/StatusEffects.cs`）：**两个种类、定长两槽、一律不可解除、无量级、时长只有帧一种单位**。它够用是因为 A1 只有硬直与无敌，而这两样恰好都是帧级、不可解除、无量级的。

所以本页的实现路径是**扩容一个已经在跑的东西**，不是新建。这一点很重要：`MotorState.IsInvulnerable` 的唯一依据就是它，动作机已经在按「问载体」的方式工作，接缝不用新造。

**待验证项**：跨日状态在存档里怎么序列化、卸载 mod 后缺失来源怎么处理（接 `ENG-7`）—— 两条都要等存档设计落地才答得了，届时以那份设计为准。

## 设计

### 三类，判据是「它动不动动作机、改不改数值」

| 类 | 动作机 | 数值与判定 | 例 |
| --- | --- | --- | --- |
| 控制类 | **切被控姿态、封输入** | 通常不改 | 硬直、失衡、击倒、冻结、眩晕 |
| 参数类 | 不动 | **改** | 无敌、减速、持续伤害、攻击增益、常驻光环、宝物词条、伤病、营养不良、失衡增益、战斗授课 |
| 纯描述类 | 不动 | **不改** | 考差的评语、伪装用的「旧伤未愈」 |

**无敌是参数类，不是控制类。** 它从不单独存在，总伴随闪避窗、起身逃生窗或复活保护；它只让命中忽略，不接管动作机。这条两套行业口径都同意。

**纯描述类必须是载体的一等成员**，不能让界面自己编字 —— 否则「状态栏里显示什么」会有两个来源，而其中一个查不到出处。它也是本作把角色写活的便宜手段：一条不影响任何数值的状态，成本接近零。

### 时长有三种单位，各有各的推进点

| 单位 | 谁推进 | 例 |
| --- | --- | --- |
| 帧 | 战斗逻辑帧（顿帧时不推进） | 硬直、无敌、击倒 |
| 天 | 每日结算固定那一步 | 伤病、营养不良、考试评语、授课疲劳 |
| 永久 | 不推进，只能被显式移除 | 失衡增益、战斗授课、特质 |

**一条硬约束：同一条效果只能有一种单位。** 想要「战斗内按帧、平时按天」的东西必须拆成两条，否则推进它就要看当前在哪个场景——而那正是「两处各算一套」的开始。

### 载体的字段

- **种类**：可扩，不是定长枚举的固定索引。
- **可否解除**：正典要求的属性。可解除的有对应道具或技能，解除动作走读条、可被打断。
- **量级**：可正可负。硬直这类没有量级的填空，但**字段要在**，否则加第一个带量级的状态时要改结构。
- **叠加与刷新**：同种再次施加时是刷新时长、叠层数、还是取更强的那个 —— 每种效果自己声明，不做全局默认。
- **来源**：谁施加的（某次命中、某件宝物、某条特质、某个 mod 的内容）。**这条是 `ENG-7` 的前提**：卸载 mod 后要能指出哪些状态因缺少来源而不可用。
- **显示名与显示值**：与实际值分开的两个字段，见下。

### 显示与实际是两个字段

状态栏显示的是**显示字段**，结算读的是**实际字段**。两者默认相同，只有被显式伪装时才不同。

**主角是唯一用到这条的角色，但它不是为他开的特例。** 手环本身就是一台会误报、可被欺骗的量产设备（[世界观 · 多功能手环与治理](../canon/world/世界观.md)），而他能骗过它的常规展示。所以：

- 玩家看到的是**真的界面、假的数据** —— UI 没有例外分支，它如实显示它拿到的显示字段。
- 展示真实力量时，伪装解除、显示字段换成实际字段；**结束后恢复伪装**。
- **这条能力对敌人同样可用**（将来若要做「看起来很弱的强敌」），所以它是载体的通用能力，不是主角的硬编码。

### 与动作机的唯一接缝

> 动作机每帧问一次：**有没有锁操作的效果在场**。有就切到对应被控姿态、丢弃本帧主动输入。

- **时长永远由本侧计。** 动作机不记时，所以把一条控制类效果清零后，姿态必须在下一帧退出。
- **多条控制类同时在场时按优先级选姿态**：更重者优先，同重取更晚施加的。优先级值归 `GP-2`。
- **参数类与纯描述类一律不切姿态。**
- 接缝只有这一条，方向只有一个（动作机问、本侧答）。**本侧永不主动调用动作机**，否则两边会互相触发。

### 跨场景与存档

- **载体一份，跨战斗与经营延续。** 伤病、词条、增益不因为进出关卡而重置。
- **按帧的效果在离开战斗时清空**，因为它们的单位在经营侧没有意义；按天与永久的保留。
- **进存档的是按天与永久那两类**，外加它们的来源。按帧的不进 —— 正式存档只在睡眠时写，那时不可能有硬直。

## 理由与取舍

- **没选按「战斗内／战斗外」分成两套载体。** 那条线看起来自然，却会把硬直的时长与伤病的时长分开，同时把硬直的时长与无敌的时长也分到两边 —— 而它们本来就该在一起。按互斥性分才切得干净，按场景分是把「什么时候用」当成了「是什么」。
- **没选把控制类整条留在动作机里。** 那样动作机要自己管时长、自己判无敌豁免、自己写叠加与优先级，等于在里面重造半个本页。
- **没选让纯描述状态只活在界面层。** 界面自己编字省事，代价是「状态栏显示什么」有两个来源，其中一个查不到出处、也进不了存档 —— 而它恰恰是玩家会截图问「这是什么」的那一个。
- **没选用一个全局叠加规则。** 中毒该叠层、减速该取最强、硬直该刷新时长，这三件事没有共同的正确答案；让每种效果自己声明，代价是数据多一个字段，收益是不会出现「为什么这个 buff 叠起来了」这类查不清的行为。
- **没选给主角写一条伪装专用分支。** 把「显示与实际分离」做成载体的通用能力，成本几乎一样，但它同时给了将来「看起来很弱的强敌」这类设计一条现成的路；而专用分支会让 UI 里出现一个 `if 主角`。
- **没选无时长的永久项也参与叠加计算。** 永久项（失衡增益、战斗授课、特质）只能被显式移除，不参与刷新与过期 —— 因为它们一旦进了同一套过期逻辑，就得为「永不过期」造一个特殊值，而特殊值是漏洞的常见入口。

## 兼容性

**这是一次破坏性扩容，得正面承认。** 现有载体是「两槽定长数组、两个种类、一律不可解除、无量级、只有帧」，要长成「可扩种类 + 可否解除 + 量级 + 叠加规则 + 来源 + 显示/实际两字段 + 三种时长单位」。`Index(kind)` 那套固定索引与定长存储要换掉。

**唯一的硬约束是行为不变**：A1 的硬直与无敌必须表现得一模一样，由现有规则层测试盯住。

**渐进路径**：先给现有载体加字段而不改行为（可否解除、控制类标记、来源都填默认值），再按切片逐个加种类与单位，不一次重写。

**对动作机零影响** —— 接缝本来就是「问载体」，`MotorState.IsInvulnerable` 的写法不用动。

## 实现与过渡

1. **加字段不改行为**：可否解除、种类标记、量级、来源、显示/实际两字段，全部给默认值；回归必须全绿。
2. **换存储**：定长两槽换成可扩结构，种类从固定索引改成可扩标识。
3. **三种单位与各自的推进点**：帧（战斗帧，顿帧不推进）、天（每日结算固定一步）、永久（不推进）。
4. **叠加与刷新按种类声明**，外加多条控制类同时在场时的优先级选择。
5. **纯描述类与显示/实际分离**，接进状态栏。
6. **跨场景与存档**：离场清帧级项，按天与永久项随存档走，来源缺失时给出可读提示（接 `ENG-7`）。

前两步不依赖任何玩法，随时可做；第 3 步之后每一步都跟着对应的玩法切片走。

## 边界与非目标

- **不定任何数值**：时长、量级、阈值、优先级值、衰减速率全归 `GP-2`／`GP-6`，等对应系统存在才定得出来的那些归 `GP-7`。
- **不定姿态与动画**：被控姿态、受击链的流转、流转表全在[角色动作状态机](./角色动作状态机.md)。
- **不定具体效果清单**：有哪几种异常状态、每件宝物带什么词条，属内容，走[内容外置](../canon/gameplay/玩法定位.md)那条边界（`ENG-5`）。
- **不设计存档格式**，只定「哪两类进存档、要带来源」。
- **不做效果之间的互相触发**（例如「中毒时受到的伤害额外提高」这类联动）。不是否决，是留到有真需求时再设计 —— 现在加它等于凭空造一层规则引擎。

## 验收

**可自动验证项**（纯规则层，与引擎无关）：

- 三类各自的行为：控制类 → 动作机报被控且封输入；参数类 → 不切姿态但数值变了；纯描述类 → 既不切姿态也不改任何数值。
- **动作机不计时**：把一条控制类效果清零，姿态必须下一帧退出。
- 可解除类 `TryRemove` 成功，不可解除类拒绝；零时长不能当作解除通道。
- 三种单位各自只被自己的推进点推进：推进战斗帧不该动「按天」项，跑一次每日结算不该动「按帧」项。
- 叠加按种类声明生效：同一种效果的刷新、叠层、取强三种规则各有一条测试。
- 显示与实际分离：伪装状态下显示字段与实际字段不同，而**结算只读实际字段**。
- A1 回归：硬直与无敌的既有行为一条都不变。

**必须实机确认项**：状态栏里同时挂着多条状态时读不读得清、图标与文字挤不挤 —— 按 [ADR-0009](../decisions/ADR-0009-编辑器主导的开发模式.md) 的分工由作者实机看，本页不判。

**完成边界**：载体长出上面那六个字段与三种单位、三类行为各有测试、状态栏能显示显示字段。做到这些，后续每个玩法切片加状态就只是加数据。

## 下游同步

- **代码**：`rules/Combat/StatusEffects.cs` 扩容；状态栏读显示字段。
- **正典**：稳定后把「三类 + 显示与实际分离」上升进[玩法定位 · 跨系统约定](../canon/gameplay/玩法定位.md)那一节，本页留活跃部分。
- **数值**：时长、量级、优先级与衰减进 `GP-2` 的参数表。
- **台账**：与[角色动作状态机](./角色动作状态机.md)共用 [`GP-18`](../spec/issues/README.md)；载体扩容是其中的第一批，且**不依赖任何玩法**，可以先做。
