---
type: system
status: draft
owner: project
last_verified: 2026-09-22
upstream: canon/gameplay/战斗与关卡.md
---

# 一次命中只走一条结算线：倍率 → 防御 → 暴击 → 保底 1

> **正典层级**：第 5 级（系统细化）。与[玩法正典](../canon/gameplay/玩法定位.md)冲突时以正典为准；**全部倍率、系数、幅度与概率归 `GP-2`，本页一个都不定。**
>
> 返回 [总索引](../README.md)｜[系统文档与提案索引](./README.md)｜相关：[战斗与关卡](../canon/gameplay/战斗与关卡.md) · [角色与成长](../canon/gameplay/角色与成长.md) · [状态效果系统](./状态效果系统.md) · [角色动作状态系统](./角色动作状态系统.md) · [数值模型](./数值模型.md)｜台账：[`GP-32`](../spec/issues/README.md)

## 摘要

一次命中要算出两样东西：**一个伤害数字**，以及**要不要附带一条状态**。本页持有的就是这两条线怎么算，以及它们的顺序为什么不能换。

**伤害线的顺序写死**：招式倍率 → 防御减免（穿透在这一步生效）→ 暴击 → 保底 1。**状态线独立于伤害线**：技能声明一个施加概率，目标按那条状态的抗性减掉一部分，余下的才是实际概率。

**伤害类型只有物理与魔法**，各对一份自己的防御。**元素不是第三种伤害类型** —— 它只决定这一击尝试施加哪种状态，所以没有「火抗」这一层，被读的是灼烧那条状态的抗性。

**本页不持有**：命中判定（打没打着是横向与纵深两个条件，见[战斗与关卡 · 打击反馈](../canon/gameplay/战斗与关卡.md)）、状态本身的时长与叠加（[状态效果系统](./状态效果系统.md)）、受击的姿态与流转（[角色动作状态系统](./角色动作状态系统.md)）、打击反馈的表现、技能有哪些字段（`GP-31`）、属性怎么派生出防御（`GP-30`）。

最要紧的那条承诺：**不新建载体、不新建姿态、不新建时间单位。** 本页算完就把结果交出去 —— 数字交给 HP，状态交给载体，硬直交给动作机。

## 上游约束

只链接约束本页的正典与已接受的 ADR，并说明各约束本页的哪一部分。

- [战斗与关卡 · 打击反馈](../canon/gameplay/战斗与关卡.md)：命中要同时满足横向与纵深两个条件；伤害数字默认关闭、杂兵不给血条。**约束本页的入口（已经判定命中才进来）与出口（数字可以不显示，但必须算得出来）。**
- [战斗与关卡 · 异常状态](../canon/gameplay/战斗与关卡.md)：异常状态与硬直、无敌、失衡同属一套机制，每条带「可否被解除」。**约束本页只管「施不施加得上」，不实现效果本身。**
- [战斗与关卡 · 防御与失衡](../canon/gameplay/战斗与关卡.md)：防御两档、精准防御成功不消耗体力条并回少量 HP 与 MP、体力条耗尽进失衡。**约束减伤那一步要认得出「这一下被防住了、而且是精准防住」。**
- [角色与成长 · 四项属性的作用方向](../canon/gameplay/角色与成长.md)：攻分物理与魔法、各对一份防御；魔法攻击同时驱动伤害、屏障与治疗的量；韧性上限就是失衡值要累积到的阈值。**这是本页两条伤害类型与暴击、韧性接口的来源。**
- [人物 · 战斗中的位置](../canon/characters/人物.md)：主角伤害极低，输出来自学生。**约束本页不得给主角的招式开任何例外通道** —— 他的价值在打断、攒蓝与辅助，不在这条结算线上。
- [状态效果系统](./状态效果系统.md)：载体持有种类、可否解除、量级、叠加与来源。**本页只调用它的施加入口，时长与叠加一概不碰。**
- [角色动作状态系统](./角色动作状态系统.md)：硬直与失衡的姿态由「有锁操作的效果在场」驱动。**约束本页产出的硬直是一条效果，不是一个姿态。**
- [ADR-0009](../decisions/ADR-0009-编辑器主导的开发模式.md)：参数唯一来源是场景与资源，代码不留能用的默认值。**约束本页定出的每个量纲将来落成外置数据，缺配置当场报错。**

## 结构

### 两条线，各自独立

```
一次命中（横向与纵深都已判定通过）
  │
  ├─ 伤害线 ── 招式倍率 → 防御减免（先扣穿透）→ 暴击 → 保底 1 → 扣 HP
  │
  └─ 状态线 ── 技能声明的施加概率 − 目标对那条状态的抗性 → 钳在 0–100 → 掷一次 → 施加
```

**两条线互不影响。** 伤害被防到最低不代表状态就施加不上，状态被抗掉也不减伤害。合成一条会长出「打得越疼越容易中毒」这类没人要的联动，而那一层规则引擎现在没有需求（与[状态效果系统](./状态效果系统.md)那条「不做效果之间互相触发」同一条边界）。

### 伤害线的四步，顺序固定

| 步 | 做什么 | 为什么在这个位置 |
| --- | --- | --- |
| 倍率 | 攻击方的物理攻击或魔法攻击乘上这一招的倍率 | 起点必须是「谁打的、用哪招」，而不是一个裸数字 |
| 防御 | 按目标对应那一份防御算减免；**穿透先从防御里扣掉**，扣完不得小于 0 | 减免要作用在完整伤害上；排到暴击后面会让暴击的意义随对面防御浮动 |
| 暴击 | 命中暴击则把**已减伤的伤害**再乘暴击倍率 | 见下面「暴击为什么排在减伤之后」 |
| 保底 1 | 结果不足 1 则取 1 | 保证不存在「打不动」的对局 |

**防御的边际收益必须递减**，形式与系数在[数值模型](./数值模型.md)。递减是结构要求而不是调优：线性减免早晚会有人把减伤堆到 100%，那时保底 1 就成了唯一还在起作用的东西。

### 伤害类型只有物理与魔法，各对一份防御

| 伤害类型 | 由谁的哪个量打出 | 被哪份防御减免 | 被哪种穿透穿 |
| --- | --- | --- | --- |
| 物理 | 攻击方的物理攻击 | 目标的物理防御 | 物理穿透 |
| 魔法 | 攻击方的魔法攻击 | 目标的魔法防御 | 法术穿透 |

**一招只能是其中一类。** 不做「一半物理一半魔法」的混伤：混伤要同时读两份防御，于是「这件装备对它有没有用」说不清，而玩家判断敌人该换什么装备正是靠这个。

**两份防御都不是单一来源**：属性给基底（体质），大头来自宝物、宝石、天赋与状态（[角色与成长 · 四项属性的作用方向](../canon/gameplay/角色与成长.md)；结构在[`装备系统`](./装备系统.md)）。本页只读那个算出来的值，不关心它由几项加成。

### 穿透是攻方的字段，作用在防御那一步

- **穿透按类型分两种**，各只穿自己那一份：物理穿透只减物理防御，法术穿透只减魔法防御。
- **穿透后的有效防御不得小于 0。** 不做负防御放大 —— 那会让「穿透超了」变成额外增伤，一个本该是「抵消对面堆防」的机制就变成了纯增伤，而且叠上递减曲线之后完全算不清。
- **穿透是减防御的值，不是按比例忽略防御。** 防御那一侧已经有递减曲线管边际收益，再叠一层百分比就有两处递减，说不清「加 10 点穿透值多少」。
- 谁带多少穿透是技能字段，归 `GP-31`；量纲与取值归[数值模型](./数值模型.md)。

### 暴击为什么排在减伤之后

暴击乘的是**已经减伤过的伤害**。反过来（先暴击再减伤）在数学上对单次伤害是等价的，但它让**玩家看到的暴击倍数随对面防御变**：同一个「暴击 1.5 倍」，打软的看起来翻一倍半、打硬的看起来只多一点。放在减伤之后，暴击倍数就是它字面的意思。

- **暴击率与暴击倍率是攻击方的属性派生量**（敏捷给暴击率），本页只读。
- **暴击只作用于伤害。** 治疗与屏障的量不暴击 —— 回血量随机跳动会让玩家没法规划「这一口药够不够」，而屏障更甚：它是主角的主要保护手段，随机强度等于随机失效。

### 元素不是伤害类型

**元素是技能字段，它唯一的机制作用是决定这一击尝试施加哪种状态。** 火对灼烧、冰对冰冻、灵能对它自己那条，映射是固定的。

- **所以没有「火抗」这一层。** 说某个敌人「弱火」，机制上读的是它**灼烧抗性低**。这是内容描述用语与机制读数的对应关系，不是两套数据。
- **元素不参与减免计算** —— 一记火元素的物理招式，减免仍然只看物理防御。
- **一招可以有元素而不附带状态**（施加概率为 0），也可以没有元素。元素不是必填。
- **为什么不做元素抗性**：做了就有物理／魔法两份防御之外再加一排元素抗性，而玩家要同时盯两套减免；而元素的表达力（火→灼烧、冰→冰冻）光靠状态抗性就够了。少一层，装备词条与敌人面板都少一半。

### 状态线：抗性减的是施加概率

```
实际施加概率 = 钳到 0–100（技能声明的概率 − 目标对这条状态的抗性）
```

- **抗性按状态各记一份**，不按元素、不按伤害类型。中毒抗性与灼烧抗性是两个数。
- **两条硬约束，两头都要留一线**：
  - **技能声明的施加概率不得等于或超过 100** —— 必中的状态会让抗性这一维彻底没意义。
  - **抗性不得堆到 100** —— 完全免疫会让一整类技能对某个目标归零，而玩家看不出「为什么这招对它一点反应都没有」。
- 钳制是在**相减之后**做的，所以「概率 0 对抗性 0」与「概率 100 对抗性 100」这类极端输入都不会产出越界值。
- **施加成功之后本页就交出去了** —— 时长、叠加、可否解除、显示全在[状态效果系统](./状态效果系统.md)。

### 硬直与失衡：本页只产出量，不管姿态

- 一次命中按轻重产出**硬直**，它是一条锁操作的效果，交给载体；姿态由动作机自己问载体拿（[角色动作状态系统 · 与状态效果系统的唯一接缝](./角色动作状态系统.md)）。
- 一次命中同时累积**失衡值**。累积到目标的**韧性上限**就进失衡 —— 韧性上限是体质派生的量，本页只读。
- **失衡值的衰减与失衡本身的时长不在本页**，在载体那一侧。

### 阵营：不做友军伤害

**同阵营之间的命中不进结算。** 不是「伤害算 0」，是这一次命中根本不产出伤害、不产出状态、不产出硬直 —— 算 0 会让顿帧、闪白与击退照样触发，玩家会以为自己打到了队友。

理由与正典的立场一致：本作的队友是玩家在养的人，不是站位障碍。开了友伤，玩家会为了避免打到队友而不敢放大范围技能，而那是对「同屏协战」的直接惩罚。

## 接口

### 它读谁

| 读什么 | 从哪里来 |
| --- | --- |
| 物理攻击、魔法攻击、两份防御、暴击率与暴击倍率、韧性上限 | 属性派生，归[`成长系统`](./成长系统.md)的派生表；本页只读算完的值 |
| 招式倍率、伤害类型、元素、状态施加概率、两种穿透 | 技能字段，归将来的 `技能系统`（`GP-31`） |
| 各状态的抗性 | 目标身上的抗性集合，来源是宝物、宝石、天赋与状态 |
| 这一下是否被防住、是否被精准防住 | [角色动作状态系统](./角色动作状态系统.md)的防御姿态 |
| 全部系数与幅度 | [数值模型](./数值模型.md)与 `GP-2` |

### 它把结果交给谁

| 产出 | 交给哪个已有系统 |
| --- | --- |
| 伤害数字 | 目标的 HP；显示与否归界面（默认关闭） |
| 状态 | [状态效果系统](./状态效果系统.md)的施加入口，带上来源 |
| 硬直 | 同上，作为一条锁操作的效果 |
| 失衡值增量 | 同上；越过韧性上限由载体那一侧触发失衡 |
| 击退量 | 代码仓 `rules/Combat/HitResolution.cs` 现在产出的那份命中反应，本页不另造一条 |
| 命中当帧的表现（顿帧、闪白、火花、震屏、音效） | 已实现的打击反馈，本页不碰 |

**挂载点到此为止。** 下次加一样与「打中之后发生什么」有关的东西时，先问「它挂在上面哪一个」，而不是顺手插一个新钩子。

### 代码落点

`rules/Combat/HitResolution.cs` 现在只产命中反应（击退距离、硬直帧数、顿帧帧数），一点伤害计算都没有。所以本页落地时是**在它旁边新增一条结算线**，不是改写既有行为 —— A1 的命中反馈回归不受影响。

## 边界与非目标

- **不定任何数值**：倍率、防御系数与递减形状、穿透量纲、暴击倍率、各状态抗性幅度、硬直帧数、失衡值增量全归 `GP-2`／`GP-6`。
- **不做命中判定**：打没打着由横向与纵深两个条件决定（[战斗与关卡 · 打击反馈](../canon/gameplay/战斗与关卡.md)，实现在 `rules/Combat/DepthOverlap.cs`）。本页的入口是「已经判定命中」。
- **不做打击反馈**：顿帧、闪白、击退表现、震屏、音效都已实现，本页只提供它们要的「轻还是重」。
- **不实现状态**：时长、叠加、可否解除、显示全在[状态效果系统](./状态效果系统.md)。
- **不定姿态**：受击、失衡、击倒的样子与流转在[角色动作状态系统](./角色动作状态系统.md)。
- **不定技能字段**：一个技能要声明什么归 `GP-31`；本页只列出它必须声明的那几项**给谁用**。
- **不做混合伤害类型**：一招只能是物理或魔法。
- **不做元素抗性**：没有火抗冰抗这一层，元素只选状态。
- **不做元素反应与状态联动**：例如「中毒时受到的伤害额外提高」。不是否决，是留到有真需求时再设计。
- **不做友军伤害。**
- **不做伤害浮动**：同一招打同一个目标，不暴击时伤害是确定值。随机浮动会让玩家分不清「这次少了是因为运气还是因为对面换了装备」，而本作已经用暴击承载了随机性。
- **不做背击与部位伤害**：站位已经由纵深承载（[战斗与关卡 · 战斗关卡的空间模型：带纵深的横版](../canon/gameplay/战斗与关卡.md)），再加背击倍率等于要求玩家同时算朝向与纵深；部位则要每个敌人各画一套判定框。两者都是「看着小、成本很大」的那一类。

## 理由与取舍

- **没选把元素做成第三、第四种伤害类型。** 那样每个目标要带一排元素抗性，而玩家得同时盯两套减免（防御一套、元素一套）；装备词条与敌人面板也跟着翻倍。让元素只选状态之后，「弱火」这句话仍然成立 —— 它读的是灼烧抗性 —— 而数据少了一整层。
- **没选让抗性直接减伤害。** 抗性如果既减概率又减伤害，它就成了第二份防御，和物理／魔法那两份职责重叠。只减概率之后，抗性回答的是「这条状态挂不挂得上」，防御回答的是「这一下疼不疼」，两个问题各有一个答案。
- **没选把暴击排在减伤之前。** 单次伤害算出来一样，但玩家看到的暴击倍数会随对面防御浮动，而暴击是最需要「一眼看懂」的机制。
- **没选让穿透按比例忽略防御。** 防御已经有递减曲线，百分比穿透会叠出两处递减，玩家算不清一点穿透值多少；减值式穿透还天然带一个可读的上界 —— 扣到 0 就到顶。
- **没选允许负防御。** 穿透超出防御时不产生额外增伤：那会让穿透从「抵消对面堆防」变成「纯增伤」，于是最优解是无脑堆穿透。
- **没选让暴击作用于治疗与屏障。** 随机的回血量让玩家没法规划，而屏障是主角的主要保护手段 —— 随机强度等于随机失效。
- **没选做友军伤害。** 开了它，玩家会为了不打到队友而不敢放范围技能，那是对「同屏协战」的直接惩罚；而正典的立场是队友是玩家在养的人，不是站位障碍。
- **没选伤害带随机浮动。** 随机会让「这次少了」有两种解释（运气／对面换装），而玩家只能猜。随机性已经由暴击承载，那一份是可读的。
- **没选把结算写进技能各自的定义里。** 那等于每个技能都能自己决定顺序，于是「为什么这招无视防御」会有一百个答案。顺序固定在本页，技能只填字段。

## 验收

**可自动验证项**（纯规则层，与引擎无关）：

- 结算顺序：构造一次「高防御 + 暴击」的命中，结果必须等于「减伤后的伤害 × 暴击倍率」，而不是「暴击后的伤害再减伤」。
- 保底 1：把防御调到极高，伤害仍为 1，不为 0 也不为负。
- 类型隔离：物理招式对「魔法防御极高、物理防御为 0」的目标不被减免，反之亦然，各一条。
- 穿透隔离：物理穿透对魔法防御无效，反之亦然，各一条。
- 穿透下界：穿透大于防御时，有效防御取 0 而不是负数；且伤害不超过「零防御时」的伤害。
- 暴击不碰治疗与屏障：暴击命中时，治疗量与屏障量与不暴击时相同。
- 元素不减免：同一招带元素与不带元素，对同一目标的伤害相同。
- 状态概率钳制：`概率 100 对抗性 0` 得 100、`概率 0 对抗性 100` 得 0，都不越界；`概率 60 对抗性 20` 得 40。
- 两条硬约束：声明概率 ≥100 的技能定义**载入时被拒**；抗性来源叠加到 100 时**被钳在上限之下**。
- 友军：同阵营命中**不产出**伤害、状态与硬直，一条反证测试。
- 交出去而不是自己实现：施加状态走[状态效果系统](./状态效果系统.md)那份载体的入口（可用替身验证被调用一次），硬直同理；本页不直接改姿态。
- **A1 回归全绿**：本页是在 `rules/Combat/HitResolution.cs` 旁新增结算线，既有的击退、硬直帧、顿帧行为一条都不变。

**必须实机确认项**：伤害量级读起来顺不顺（HP 三位数、代表性伤害两位数是为了纸面能手验，但「打起来数字好不好看」得实机看）；状态挂上去时玩家看不看得出来。按 [ADR-0009](../decisions/ADR-0009-编辑器主导的开发模式.md) 的分工由作者实机看，本页不判。

**完成边界**：结算顺序落成一条线、两种伤害类型各对一份防御、穿透与暴击各就各位、状态线的钳制与两条硬约束都有测试。做到这些，后面加技能与加状态就只是填字段。

## 下游同步

- **代码**：结算线落 `rules/Combat/HitResolution.cs` 一侧的规则层；抗性集合挂在角色数据上，值走内容外置（`ENG-5`）。
- **数值**：倍率、防御递减形状、穿透量纲、暴击倍率、状态抗性幅度进[数值模型](./数值模型.md)与 `GP-2` 的参数表；派生公式那一侧的同步归 `GP-35`。
- **台账**：立项在 [`GP-32`](../spec/issues/README.md)；技能字段在 `GP-31`，属性派生在 `GP-30`，载体扩容在 `GP-18`，手感帧数在 `GP-6`。
- **该上升进正典的**：只有一条 —— **「元素不是伤害类型，它只决定附带哪种状态」**。等真有几个元素技能、证明这条不需要例外之后，上升进[战斗与关卡](../canon/gameplay/战斗与关卡.md)的异常状态那一节。结算顺序与字段按定义不进正典。
