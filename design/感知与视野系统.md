---
type: system
status: draft
owner: project
last_verified: 2026-09-24
upstream: canon/gameplay/战斗与关卡.md
---

# 一份可见性数据，两个阵营各取并集：AI 的目标选择与玩家看到的画面都从它读

> **正典层级**：第 5 级（系统细化）。与[战斗与关卡](../canon/gameplay/战斗与关卡.md)冲突时以正典为准；**视野角度、距离、近身半径与各项时长全归 `GP-7` 与 `GP-6`，本页一个都不定。**
>
> 返回 [总索引](../README.md)｜[系统文档与提案索引](./README.md)｜相关：[战斗与关卡 · 感知与视野](../canon/gameplay/战斗与关卡.md) · [战斗AI系统](./战斗AI系统.md) · [关卡系统](./关卡系统.md) · [ADR-0013](../decisions/ADR-0013-战斗片段就是一屏.md)｜台账：[`GP-51`](../spec/issues/README.md)

## 摘要

本系统每帧算出一份**可见性数据**：**每个阵营这一刻看得见哪些实体**。AI 的目标选择、玩家看到的画面、资源点的显形与残影的表现，全部从这一份读。

**它持有**：视野组件的声明字段、阵营并集怎么取、遮挡怎么判、目标离开视野之后的记忆项与残影、什么显形什么不显形。

**它不持有**：AI 的决策与行为（[`战斗AI系统`](./战斗AI系统.md)）；压暗与残影画成什么样（作者在 Godot 里配）；角度、距离与各项时长的值（`GP-7`／`GP-6`）；片段里遮挡区域摆在哪（作者搭片段，[`关卡系统`](./关卡系统.md)）；俯视场景（本系统在那里不生效）；可见性计算的优化手段。

最要紧的那条承诺，也是本系统存在的全部理由：**AI 感知与玩家视野表现由同一份数据驱动，一次计算、没有第二份。** 正典已经写出两边各算一遍的后果 —— 屏幕全黑却已被攻击，或者敌人站在亮处装作没发现你。**这条要有反证钉住**：把可见集清空，AI 必须找不到目标。

**它不是迷雾。** 要约束的对象是**实体**不是地形，所以地形始终可见，只把未被感知的区域压暗。

## 上游约束

- [战斗与关卡 · 感知与视野](../canon/gameplay/战斗与关卡.md)：没有角色全知、不做三态迷雾、地形始终可见并以统一灰调压暗、敌人与可交互物件只在阵营视野并集内显形、友方取并集而敌方独立取并集且两边共用同一套视野组件、目标离开视野后在最后已知位置留残影并淡出、**AI 感知与玩家视野表现必须由同一份可见性数据驱动**、本系统只在侧视关卡生效。**这是本页全部结构的来源。**
- [战斗与关卡 · 队友与敌人](../canon/gameplay/战斗与关卡.md)：敌人发现玩家后向附近同伴传播警戒，**这条是感知系统的回报**；同屏敌人上限，超出这个量级需要空间划分优化。**约束本页要给警戒那一侧交出什么，也约束「开销由那条上限兜着」这句话的出处。**
- [战斗与关卡 · 战斗关卡的空间模型：带纵深的横版](../canon/gameplay/战斗与关卡.md)：纵深连续、角色不因远近缩放、绘制按纵深排序。**约束视野与遮挡都要在纵深上成立，不能只按屏幕上的二维距离算。**
- [玩法定位 · 像素基准](../canon/gameplay/玩法定位.md)：侧视有效视野的两个数。**「一屏」指的就是它，本页不复述那两个数。**
- [ADR-0013](../decisions/ADR-0013-战斗片段就是一屏.md)：一个战斗片段就是恰好一屏，这一屏清干净才能往前。**这条决定本页兑现的是什么** —— 不是绕开整个房间，而是一屏之内逐个偷袭还是被一群一起围。
- [`关卡系统` · 资源点受视野约束](./关卡系统.md)：没被感知到的资源点不显形，且**不许给「这关还剩几个资源点」的计数器**。**这是本页的第三类读者，也是那条「没有计数器」的出处。**
- [`关卡系统` · 片段之间是转场，不是无缝滚动](./关卡系统.md)：加载时只需要当前那一个片段。**约束可见性数据的范围是当前片段，不跨片段。**
- [`关卡系统` · 一个种子，加一份关卡进度](./关卡系统.md)：关卡进度进临时续玩点。**约束记忆项跟着它走，不进正式存档。**
- [`战斗AI系统` · 决策循环：一条顺序固定的链](./战斗AI系统.md)：循环的第一步是取可见集与记忆集，而 AI 不许直接读世界里的实体表。**这是本页那条反证的另一半 —— 两边要各写一次同一件事。**
- [`角色动作状态系统`](./角色动作状态系统.md)：姿态与流转归它。**约束本页不产出任何姿态 —— 「发现了」不是一个姿态，警戒状态在 AI 那一侧。**
- [`数值模型` · 尚未给值的量，各自写明校准办法](./数值模型.md)：值的唯一来源是参数表，本页只写量纲与约束形式。**约束本页新增的量一律进那张表。**
- [ADR-0009](../decisions/ADR-0009-编辑器主导的开发模式.md)：参数唯一来源是场景与资源、代码不留能用的默认值；场景与摆位由作者在 Godot 里管。**约束视野组件缺字段当场报错，也约束遮挡区域是作者摆的、压暗长什么样不在本页。**

## 结构

### 一份数据，一次计算

每帧算出的东西只有两样，**按阵营各一份**：

| 产出 | 内容 | 谁读它 |
| --- | --- | --- |
| **可见集** | 这个阵营这一刻看得见的实体 | AI 的目标选择、显形、资源点 |
| **记忆集** | 离开可见集之后还记着的条目，各带一个最后已知位置 | AI 的目标选择、残影 |

**两侧都只读，不各算一遍。** 这条是本系统的承重项，形式是**读口只有一个**：谁想知道「看不看得见」都问这一份，没有第二个入口，也没有「表现层自己再判一次距离」。

**这条要有反证钉住** —— 把可见集清空之后 AI 必须找不到目标。没有这条反证，「两边读同一份」在代码里就只是一句注释：实现时最省事的做法正是让 AI 直接遍历世界里的实体表，而那不报错。

### 视野组件要声明什么

一个角色挂一个视野组件。**缺任一字段载入报错**（[ADR-0009](../decisions/ADR-0009-编辑器主导的开发模式.md)：不留能用的默认值）。

| 声明项 | 说明 | 值归谁 |
| --- | --- | --- |
| 阵营 | 它的产出并进哪一份并集 | 不是数值 |
| 朝向扇形的角度 | 以角色当前朝向为轴的张角 | `GP-7` |
| 可见距离 | 扇形的半径 | `GP-7` |
| **近身半径** | 一个**不看朝向**的圆，半径内一律看得见 | `GP-7` |

**近身半径为什么必须存在**：没有它，一个角色贴在另一个角色背后动手，被打的那个还不知道 —— 而那看起来不像「他没发现」，看起来像坏了。这与[战斗与关卡 · 基础 AI 不分档，人人从第一天就有](../canon/gameplay/战斗与关卡.md)那条判据同源：**低档不该长得像 bug。**

**朝向取角色当前朝向**，本页不另设一个「视线方向」。多一个方向就多一处能和角色朝向对不上的地方，而玩家看到的只有角色朝向 —— 这与[战斗与关卡 · 防御与失衡](../canon/gameplay/战斗与关卡.md)那条「防御朝向就是角色朝向」是同一条处理。

### 两个阵营各取并集，共用同一套组件

- **友方的可见集是全部友方角色视野的并集**；**敌方独立取自己的并集**。
- **两边共用同一种视野组件** —— 没有「玩家专用视野」与「敌人专用视野」两套。正典点名要求这一条，而它也是「一份数据」能成立的前提：两套组件就是两套判定，迟早给出两个答案。
- **队友倒下时友方的并集会缩**，这是刻意的：队伍被打散之后看得见的范围变小，那是倒下的一部分代价。
- **主角与队友没有区别对待。** 玩家看到的画面读的是**友方那一份并集**，不是主角一个人的视野。

### 遮挡按片段声明的区域判

**作者在片段里摆遮挡区域，视线只问一件事：两点之间有没有跨过一个遮挡区域。**

- **这是一条几何判定，不是引擎射线。** 没选射线的理由有实测依据：`ENG-19` 那个缺陷是影子射线取了一个既是地形也是角色的碰撞层，于是一个角色跳过另一个角色时影子跳到对方头顶，**而且不报错**；视野若走同一条路会踩同一个层掩码问题，带纵深之后还要在三个轴上判。
- **在纵深上怎么成立**：遮挡区域占一段纵深（与[战斗与关卡 · 感知与视野](../canon/gameplay/战斗与关卡.md)那句「场景物件在纵深上作阻挡与掩体」同一段），所以「贴远排绕过去」既绕开了阻挡也绕开了视线 —— 两件事用同一段声明，不是两套数据。
- **作者看得出哪里能躲。** 这是选声明式的第二个理由：遮挡区域在编辑器里是看得见的东西，而射线判定只能靠跑起来试。
- **可破坏通路打通之后遮挡也跟着消失**（正典保留可破坏通路），所以遮挡区域是随片段状态变的，不是静态烘焙的。

### 目标离开可见集之后：记忆项与残影

**目标离开可见集时，在它的最后已知位置留一条记忆项**，带一个时长；时长走完就消失。

- **残影是记忆项的表现，不是另一件东西。** 正典那句「在最后已知位置留残影并淡出」说的就是它 —— 一份数据、一种呈现。
- **记忆项不跟着目标移动。** 它记的是「它刚才在这」，跟着走就等于偷偷给了跟踪能力，而那正是「没有角色全知」要挡的。
- **AI 读记忆集是允许的**，这是它的用处：被叫来的同伴走向那个位置再重新感知（[`战斗AI系统`](./战斗AI系统.md)的警戒传播）。
- **记忆项进临时续玩点，不进正式存档** —— 它活不过一关，而正式存档只在睡眠时写（[玩法定位 · 存档](../canon/gameplay/玩法定位.md)）。

### 什么显形，什么不显形

| 对象 | 规则 |
| --- | --- |
| 地形 | **始终可见**，未被感知的区域只以统一灰调压暗 |
| 敌人与其他角色 | 只在本阵营可见集内显形；离开后按记忆项留残影 |
| 可交互物件与资源点 | 同上（[`关卡系统` · 资源点受视野约束](./关卡系统.md)） |

**地形始终可见是为了不让玩家卡住** —— 看不见地板会变成走路都要试。而压暗**长什么样归表现**，作者实机看，本页不判。

**由此有一条不许做的事**：不给「这一关还剩几个资源点」的读口。那等于把视野约束绕掉，而视野约束正是「要不要把这片走一遍」这个决策的全部来源。

### 在一屏一清的房间模型下，它兑现的是什么

[ADR-0013](../decisions/ADR-0013-战斗片段就是一屏.md) 定下战斗片段就是一屏、清干净才能往前，所以**「摸过去」不是绕开整个房间** —— 这一点要写明，否则会被当成本系统失去了用处。

**它兑现的是一屏之内的分批**：先摸到哨兵身边把它处理掉，还是站到中间被一群一起围。正典给感知的那句理由本来说的就是这件事 —— **不先处理掉哨兵，接下来要面对被叫来的一群** —— 那是警戒传播的后果，与「绕不绕得过去」无关。

**另外两样在这个模型下照样成立**：资源点漏采（摸黑走过去就是没看见）、以及**先看清这一屏有几只再决定从哪边进**。

## 接口

### 谁读它、谁喂它

| 方向 | 对方 | 形状 |
| --- | --- | --- |
| 入 | 角色 | 位置、朝向与那一个视野组件；**缺组件的角色不产出视野**，但它自己照样可以被看见 |
| 入 | 片段（[`关卡系统`](./关卡系统.md)） | 遮挡区域；作者摆的导出字段 |
| 出 | [`战斗AI系统`](./战斗AI系统.md) | 可见集与记忆集，**那是它选目标的唯一来源** |
| 出 | 表现层 | 同一份可见集与记忆集 —— 显形、压暗与残影都从它画 |
| 出 | [`关卡系统`](./关卡系统.md) | 资源点与可交互物件的显形读同一份 |
| 出 | 统计事件总线 | 发现目标与失去目标各一条事件 |
| 出 | [`图鉴系统`](./图鉴系统.md) | 一个敌人**首次**进入阵营可见集时报一次。它**不读这份可见集**（读了就等于让它持有一份缓存），所以**它不是上面那几类读者之一** —— 它拿到的是一次转变，不是一份数据 |
| 存档 | 临时续玩点 | 记忆项进它；**可见集不进** —— 它每帧重算，存它等于存一份缓存 |

**挂载点到此为止。** 将来加一样「要不要看得见」的东西时，先问它挂在上面哪一格，而不是顺手插一个新钩子。

### 它不碰的三条接缝

- **警戒不在本页。** 「看见了」是本页的产出，「看见了之后怎么办」（进警戒、追击、向同伴传播）全在[`战斗AI系统`](./战斗AI系统.md)。这条切开的理由是本页对两个阵营完全对称，而警戒只有敌人那一侧有。
- **姿态不在本页。** 「发现」不是一个姿态，本页不往[`角色动作状态系统`](./角色动作状态系统.md)递任何东西。
- **压暗与残影的画法不在本页。** 本页只回答「这一块算不算被感知」，画成什么样是作者在 Godot 里配的事（[ADR-0009](../decisions/ADR-0009-编辑器主导的开发模式.md)）。

## 边界与非目标

- **不定任何数值**：视野角度、可见距离、近身半径、记忆项时长全归 `GP-7`；残影淡出时长归 `GP-6`（它是表现手感，只能实机看）。
- **不做三态迷雾**：正典已排掉 —— 三态管的是「这块地走没走过」，而要约束的对象是实体。
- **不做迷雾贴图与压暗的画法**：作者配。
- **不做 AI 的决策与行为**：[`战斗AI系统`](./战斗AI系统.md)持有。
- **不在俯视场景生效**：基地与城区不做迷雾（正典）。
- **不做遮挡区域的内容**：摆在哪是作者搭片段的事。
- **不做可见性计算的优化手段**：正典写明超出同屏上限的量级需要空间划分，**怎么划归实现** —— 本页只定「一份数据、一次计算」，那条不因优化手段而变。
- **不给「还剩几个资源点」的计数器**：它会把视野约束绕掉。
- **不做听觉与足音**：正典只写了视野。将来真要加，它是本页多一个产出通道（一份「听得见谁」的集合），不是另一份可见性数据 —— 现在写下这条是为了那天不会长出第二个读口。
- **不做「玩家专用视野」**：两个阵营共用同一种组件，这是「一份数据」的前提。
- **同名不同物，两处都要写全名**：
  - **「视野」与「感知」**：本页里它们指同一件事 —— 视野是那个几何范围，感知是「读它得出的结果」。**不许把其中一个当成另一套机制**；文档里两个词可以混用，因为它们没有第二个所指。
  - **「可见集」与「显形」**：前者是数据，后者是它在画面上的一种用法。资源点显形读可见集，但可见集不知道什么叫显形。
  - **「记忆项」与[`战斗AI系统`](./战斗AI系统.md)的「最后已知位置」**：是同一个字段的两个说法 —— 记忆项持有它，警戒传播传的是它。
  - **「遮挡区域」与[战斗与关卡 · 感知与视野](../canon/gameplay/战斗与关卡.md)那条「场景物件在纵深上作阻挡与掩体」**：同一段声明的两个用处（挡视线、挡走位），不是两份数据。

## 理由与取舍

- **没选让 AI 与表现各算一遍。** 正典已经把后果写死（屏幕全黑却已被攻击、敌人站在亮处装作没发现你）。它是本批唯一的防重构点，所以本页给它配了一条反证而不是只写一句话。
- **没选三态迷雾。** 它管的是地形走没走过，而要约束的是实体在不在场 —— 用它就得同时接受「没走过的地方看不见地板」，而那让玩家连走路都要试。
- **没选只有朝向扇形、不要近身半径。** 贴在背后动手而对方毫无反应，玩家不会读成「他没发现」，只会读成坏了。这一条与「低档队友不该长得像 bug」是同一条判据。
- **没选给视野组件一个独立的视线方向。** 多一个方向就多一处能和角色朝向对不上的地方，而玩家看到的只有角色朝向。
- **没选引擎射线判遮挡。** `ENG-19` 实测过同一个坑（射线取了一个既是地形也是角色的层，影子跳到对方头顶且不报错）；带纵深之后还要在三个轴上判；而声明式的遮挡区域让作者在编辑器里直接看得出哪里能躲。代价是作者搭片段时多一样要摆的东西。
- **没选让记忆项跟着目标走。** 那等于偷偷给了跟踪能力，而正典要的是「没有角色全知」。留在最后已知位置才是「它刚才在这」。
- **没选把残影做成另一条数据。** 它是记忆项的表现 —— 分成两份的话「记多久」与「淡多久」会各自漂移。
- **没选让玩家看到的只是主角一个人的视野。** 正典写明友方取并集，而并集还顺带给了「队友倒下视野会缩」这个可感的代价。
- **没选在本页处理警戒。** 本页对两个阵营完全对称，而警戒只有敌人那一侧有 —— 混在一起会让这份数据长出一半只对一个阵营成立的字段。
- **没选现在就定优化手段。** 正典给的是一条量级上限，而空间划分怎么做取决于实现时的实测；本页只保证「一份数据、一次计算」这条不因优化而破。
- **没选现在做听觉。** 正典只写了视野；加一条听觉会立刻带出「脚步声算不算」「墙挡不挡声」两串问题，而它们都没有上游。写明它将来是本页多一个产出通道，是为了那天不会长出第二个读口。

## 验收

**可自动验证项**（纯规则层，与引擎无关）：

- **一份数据、一次计算**（承重项）：读口只有一个；**把可见集清空之后 AI 找不到目标**（反证）。
- 视野组件缺任一字段时**载入被拒**，且报错说清缺哪一项（[ADR-0009](../decisions/ADR-0009-编辑器主导的开发模式.md)）。
- 朝向：目标在扇形内看得见、转身之后同一个目标看不见 —— 各一条。
- 距离：超出可见距离看不见；**贴在背后但在近身半径内照样看得见**（这一条钉住近身半径不是可选项）。
- 阵营并集：友方任一角色看得见就进友方可见集；**敌方那一份不受友方影响**；两边用的是同一种组件（同一个类型，不是两个）。
- 队友倒下：友方并集相应缩小。
- 遮挡：**跨过遮挡区域的两点互相看不见**；绕开之后看得见；**遮挡区域在纵深上错开时不挡**（它占一段纵深，不是一面无限高的墙）。
- 可破坏通路打通后，原来挡视线的那一段不再挡。
- 记忆项：目标离开可见集时在**最后已知位置**生成一条；**目标继续移动时那条不跟着动**；时长走完消失。
- 资源点显形读的是同一份可见集（与敌人走同一条路，不是另一条）。
- 读口里**没有**「这一关还剩几个资源点」这一项。
- 记忆项进临时续玩点并读得回来；**可见集不进存档**。
- 本系统在俯视场景不产出任何可见性约束。

**必须实机确认项**：压暗读不读得出边界（又不至于让人以为地形没画完）、残影看不看得出是「刚才在这」而不是「现在在这」、资源点在视野边缘显形的那一下醒不醒目、近身半径大到多少才不显得迟钝。按 [ADR-0009](../decisions/ADR-0009-编辑器主导的开发模式.md) 的分工由作者实机看，本页不判。

**完成边界**：视野组件按字段声明且缺字段报错、两个阵营各取并集且共用同一种组件、遮挡按声明区域判且在纵深上成立、记忆项与残影是同一件事的两半、四类读者都从同一份读且有反证钉住。做到这些，「走进一个片段 → 看见几只 → 摸过去还是正面进 → 漏采还是走一遍」这条链在文档层面就走得通了。

## 下游同步

- **代码**：可见性计算落**规则层**（它只吃位置、朝向与遮挡区域，产出两个集合 —— 规则层不能引用 Godot，而这份计算不需要引擎）；显形、压暗与残影落引擎层，**只读不产**。相邻的既有件是 `rules/Combat/DepthOverlap.cs` 与 `rules/Combat/DepthBand.cs`（纵深那一侧已经有了，本页读它、不改它）。**遮挡区域要作者搭，所以落地排在 `ENG-21` 之后或与它同批。**
- **数值**：视野角度、可见距离、近身半径与记忆项时长进 `GP-7` 的参数表；残影淡出时长归 `GP-6`。
- **台账**：立项在 [`GP-51`](../spec/issues/README.md)；落地顺序按「视野组件字段与缺字段报错 → 阵营并集与共用组件 → 遮挡按声明区域判（含纵深错开不挡）→ 记忆项与残影 → 四类读者接同一份（含清空可见集的反证）→ 记忆项进续玩点」。
- **该上升进正典的**：一条 —— **近身半径必须存在**。正典现在只写了「从背后摸过去敌人真的不知道」，而那句单独读会推出「背后一律看不见」；等实机确认过半径的量级之后，把这一条补上去。其余字段与接口按定义不进正典。
