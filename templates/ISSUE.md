---
type: template
status: active
owner: project
last_verified: 2026-08-26
---

> **模板说明**：可实现条目。由 `/to-issues` 技能从 PRD／SPEC 拆出，复制到 `spec/issues/issue-<ID>-<slug>.md`，并在[台账索引](../spec/issues/README.md)加一行。
>
> `<ID>` 用领域前缀加序号（`GP` 玩法 / `NR` 叙事 / `UI` 界面 / `ART` 美术 / `ENG` 工程 / `DOC` 文档），例如 `issue-ENG-2-godot-project.md`。**编号是跨文档引用的稳定标识，不重编号、不复用已删除的号。**
>
> 一句话说得完的条目只留台账一行，不建文件。

```yaml
type: workdoc
status: draft
owner: project
last_verified: YYYY-MM-DD
```

# ENG-N：短标题

## 目标

一句话说清要达到什么结果。**不写实现办法。**

## 来源

- PRD：`spec/prd-<slug>.md` 的 `US-001`
- SPEC：`spec/spec-<slug>.md` 的 §3.1、§4.2
- 或：在哪个需求的实现过程中发现的（超边界记账）

## 依赖

依赖哪个编号，以及为什么。例如本项目多数工程条目依赖 `ENG-2`（工程未建），涉及数值的依赖 `GP-2`。

无依赖就写「无」，不要留空让人猜。

## 验收标准

- [ ] 可观察或可测试的条目
- [ ] ……
- [ ] **界面类必须加**：作者实机确认

每条都要能观察或复现。这些勾选框就是进度视图 —— 会话恢复时靠它判断做到哪一步，所以**做完一条就勾一条**，不要攒着最后一起勾。

## 实现笔记

> 由 `/note-it` 在实现和评审之后填写。四类都要过一遍，某类没有就写「无」并简要说明。

### 设计决策

规格模糊或没写的地方做了什么选择，以及理由。

- **模糊点**：
- **选择**：
- **理由**：

### 偏离

有意偏离已确认方案的地方。**这一节最容易被跳过，也最有价值** —— 归档时过程叙述会删掉，偏离如果没记在这里就彻底消失了。

- **方案怎么说**：
- **实际怎么做**：
- **为什么**：

### 权衡

考虑过的备选方案，以及为什么选了现在这个。

### 待确认

不确定的假设、需要作者验证的项、需要跟进的事。

## 验证结果

> 由 `/verify-round` 填写。**只写实际运行过的内容。**

| 命令 | 结果 | 判定 |
| --- | --- | --- |
|  |  |  |

沿用旧结果必须注明日期，例如「导出冒烟沿用 08-20 存证，本轮未复跑」。**「跑了回归」和「应该跑回归」是两件事，只报前者。**
