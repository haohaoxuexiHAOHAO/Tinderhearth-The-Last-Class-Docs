---
type: workdoc
status: draft
owner: project
last_verified: 2026-09-25
---

# GP-61：招式倍率让位，判据 C4 换量测点

## 目标

主角的削弱只有一个来源，而判据量的是那个来源 —— 不是量倍率之比然后对状态那一层视而不见。

## 来源

- PRD：[`spec/prd-canon-hardening.md`](../prd-canon-hardening.md) 的 `US-003`
- 起因：现在倍率表里主角轻击 0.35、学生 0.90，如果再挂一条降他属性的状态，他就被削弱两次；而判据 C4 量的是那张表，**它看不见状态那一层**，于是脚本会一直报「通过」而实际比值更低

## 依赖

依赖 `GP-60` —— C4 的新量测点要引用那条状态，状态的定义得先在。

## 验收标准

- [ ] 可核对：[`数值模型`](../../design/数值模型.md)招式倍率表里**主角与学生的同名招式取同值**，正文写明削弱不在这一层。
- [ ] 可核对：参数表 `move_multipliers` 跟着改，且[`数值模型`](../../design/数值模型.md)正文抄的那两个数与参数表一致（`--check-doc` 判得到）。
- [ ] 可核对：判据 C4 的定义改成「**「薪尽火传」在场时主角实际输出 ÷ 学生实际输出**」，并写明这不是新约束、是把同一条约束量到对的地方。
- [ ] 可核对：[`数值模型`](../../design/数值模型.md)的尚未给值表**新增一行**（那条状态两侧的幅度），并写明两条配对要求：给值那一轮必须同时把 C4 的判定点接到新量测点；满编加成不得撑破判据 C1 那个战力差区间。
- [ ] 可测试：`python tools/simulate_week.py` 全部判据成立，C4 按新量测点打出实测值。
- [ ] 可测试：**新判据真的判得到** —— 用 `--set` 把那两侧的幅度撞到违例，C4 必须失败；**参数缺失时判失败而不是静默通过**。
- [ ] 可核对：改了 `tools/simulate_week.py` 之后自己复核了一遍。**它没有自证入口**（[WORKFLOW § 6](../../WORKFLOW.md)），坏了没有东西能发现。
- [ ] 可核对：`python tools/simulate_week.py --check-doc` 无分叉；`python tools/check_docs.py` 0 项必须修复。

## 实现笔记

> 由实现与评审之后填写。四类都要过一遍，某类没有就写「无」并简要说明。

### 设计决策

- **模糊点**：
- **选择**：
- **理由**：

### 偏离

- **方案怎么说**：
- **实际怎么做**：
- **为什么**：

### 权衡

### 待确认

## 验证结果

> 只写实际运行过的内容。

| 命令 | 结果 | 判定 |
| --- | --- | --- |
|  |  |  |
