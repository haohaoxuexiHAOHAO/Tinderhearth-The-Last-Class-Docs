---
type: workdoc
status: draft
owner: project
last_verified: 2026-08-30
---

# UI-4：`canvas_items` 下 12px 像素字的实测与结论回写

## 目标

确认 12px 中文在 `stretch/mode="canvas_items"` 下仍是块状像素而不是被按屏幕尺寸重新光栅化成平滑字形，并把结论写回 [ADR-0008](../../decisions/ADR-0008-中文像素字体选型.md)。

## 来源

- PRD：[`spec/prd-ui-framework.md`](../prd-ui-framework.md) 的 `US-002`
- 起因：ADR-0008 的十项字体属性与整数网格实测是在 `stretch/mode="viewport"` 下做的（2026-08-30，`tools/font_preview.py`），而工程用 `canvas_items`。两者的文字光栅化路径不同。

## 依赖

`UI-3`（要先有 640×360 与整数缩放才谈得上验它）。

**这是本轮唯一的技术未知项**，且不成立会推翻整套渲染配置，所以排在相机与界面之前。

## 验收标准

- [ ] 同一段中文在 ×2／×3／×4 下截图，字形轮廓的每个像素块边长恰好等于缩放倍数
- [ ] 不出现 1 物理像素宽的笔画（那是被重新光栅化的证据）
- [ ] 所用的字体属性取值逐项记下来，不靠"设了就行"
- [ ] 结论写回 ADR-0008 的"未验证（一）"段：成立就划掉，不成立就写明改用哪条路径
- [ ] 不成立时，替代路径（回退 `viewport` 模式）同样跑一遍上面两条，并确认 `aspect="expand"` 的可视区域伸缩行为仍满足正典那条零成本约束
- [ ] 作者实机确认

## 实现笔记

> 由 `/note-it` 填写。

### 设计决策

### 偏离

### 权衡

### 待确认

## 验证结果

> 由 `/verify-round` 填写。只写实际运行过的内容。

| 命令 | 结果 | 判定 |
| --- | --- | --- |
|  |  |  |
