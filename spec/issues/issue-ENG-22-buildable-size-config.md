---
type: workdoc
status: draft
owner: project
last_verified: 2026-09-22
---

# ENG-22：新的可建造区尺寸落到配置与相机判据

## 目标

把 80×60 落进配置，并把相机那条按旧宽度算的判据重算 —— 代码里一个尺寸都不写死。

## 来源

- PRD：[`spec/prd-base-and-production.md`](../prd-base-and-production.md) 的 `US-001`
- 现状：尺寸已经是配置项（`rules/Foundation/Config/GameConfig.cs` 的两个字段，无默认值、缺字段当场报错），所以扩大是改数加延伸地图。

## 依赖

依赖 `GP-25` —— 正典先定尺寸，配置才有依据可改。

## 验收标准

- [ ] `data/config/game.json` 的两个尺寸字段改成新值
- [ ] 规则层测试比对**装进相机的可建造区**与配置一致（不是只比对配置读到了什么）
- [ ] `rules/Ui/CameraFeel.cs` 里建造滚动速度那条「640px 宽走完 2.5 秒」的判据按新宽度重算，注释与值一起改
- [ ] 边缘推镜的余量（格）复看一遍：画布变大之后旧值是否还符合「让玩家看清边界外还有多少地方」
- [ ] `python tools/verify.py` 五步全绿
- [ ] 作者实机确认：打开基地场景看取景与滚动手感

## 实现笔记

> 由 `/note-it` 在实现和评审之后填写。

### 设计决策

- **模糊点**：
- **选择**：
- **理由**：

### 偏离

- **方案怎么说**：
- **实际怎么做**：
- **为什么**：

### 权衡

### 待确认

## 验证结果

> 由 `/verify-round` 填写。**只写实际运行过的内容。**

| 命令 | 结果 | 判定 |
| --- | --- | --- |
|  |  |  |
