---
type: backlog
status: active
owner: project
last_verified: 2026-08-26
---

# 待办台账

全库**唯一**待办索引。一条待办一个编号，编号是跨文档引用的稳定标识 —— 正典、制作规格、参考资料和归档三问都会引它，所以**不重编号、不复用已删除的号**。

条目详情写成 `issue-<ID>-<slug>.md` 放本目录，由 `/to-issues` 产出。只有一句话就说得完的条目可以只留本表一行，不建文件。

规则见 [WORKFLOW §3](../../WORKFLOW.md#3-配额)（本页配额 200／250 行，每格 ≤360 字符）。

## 编号前缀

| 前缀 | 领域 |
| --- | --- |
| `GP` | 玩法与数值 |
| `NR` | 叙事 |
| `UI` | 界面 |
| `ART` | 美术与音频 |
| `ENG` | 工程 |
| `DOC` | 文档 |

## 玩法与数值

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `GP-6` | 低 | 战斗手感与帧数的实机校准 | 依赖 `ENG-6`（工具选型）与战斗实现。四项：顿帧时长、精准防御判定窗口、连段帧数、读条时长。**`GP-2` 明确排出**，理由是[战斗与关卡](../../canon/gameplay/战斗与关卡.md)写明这些只能实机逐帧调，靠感觉调违反「结论必须有依据」。`GP-2` 会给出各自的量纲、允许区间与校准办法，本条只做实测收敛 |
| `GP-7` | 低 | 等对应系统存在才能定的其余参数 | 依赖各自系统的实现。四项：异常状态持续时间与叠加规则、队友 AI 四档实战值阈值、警戒传播范围与延迟、星火积分兑换上限。`GP-2` 只给量纲与「上限必须存在」这类约束形式，具体值要等能实测出行为差异时才定得出来 —— 跟着实现分批推进，与 `ENG-5` 同一模式 |
| `GP-9` | 中 | 闪避的方向要取「按下瞬间的方向」还是「短窗口内最后一次有效方向」 | `UI-7` 定完键位后从键盘几何推出来的，依赖闪避实现。形状：`E`（闪避）与 `D`（右移）**同一根食指**，`Q`（防御）与 `A`（左移）同一根无名指（作者 2026-08-30 定维持这两个键位）。于是向右跑时按闪避，食指离开 `D` 那一瞬方向输入就变了 —— **翻滚方向可能不是玩家要的那个**，而正典的闪避是带方向的翻滚。窄修法是闪避取方向时改用「短窗口内最后一次有效方向」而不是按下瞬间的方向，比 `GP-4` 的完整输入缓冲小得多，不必等它。手柄不受影响 —— 左摇杆与面键不共用手指 |
| `GP-8` | 低 | 基地可建造区是否要扩大 | 依赖装饰性建造是否要做。正典定 40×30 格（640×480px，[时间与经营](../../canon/gameplay/时间与经营.md)），2026-08-30 比过一次：星露谷标准农场可耕地 3,427 格、最大整块 63×31（[Wiki](https://stardewvalleywiki.com/Farm_Maps)），我们约是它可用面积的三分之一。**第一版不挤**（6 类建筑加种植区实估约 250 格，占两成），长期偏小 —— 而需要大画布的正是被排出第一版的"装饰性建造"。现在扩要多画三到四倍基地图块，在"建造好不好玩"验证之前不该花。`UI-1` 已要求可建造区尺寸从配置读，所以将来扩大是改数加延伸地图 |
| `GP-5` | 低 | 场景物件可拿起、投掷与持物战斗 | **玩法定位已就绪。** 玩法正典明确第一版不做：物件「可踩」是多层平台的一部分必须有，「可拿」要一整套持物移动、持物攻击与投掷动画，乘上主角与各类敌人。属于「看着很小、动画量很大」那一类，与 `GP-4` 同级 |
| `GP-4` | 低 | 招式取消与派生（连击附加项） | 依赖 `ENG-6`。**玩法定位已就绪**，其中明确本条为附加项、第一版不做。核心机制只是「每个动作的取消窗口表 + 输入缓冲」，是配置数据不是代码复杂度；**成本在动画分支与手感调优**，且窗口帧数只能实机逐帧试。一条省钱结论：玩家只操作主角，学员与敌人用固定连段玩家感知不到差别，所以只需给主角做一套。攻击不得取消到防御 —— 否则收招硬直失去意义 |
| `GP-3` | 低 | 物品偷窃与销赃 | 依赖玩法正典已定的物品所属权基础。那里定了「偷窃不转移所属权」，但那条留了个洞：偷来的物品既然操作受所属权限制就无法使用，偷窃需要销赃或洗白途径才有意义。牵扯发现判定、执法反应、信任度后果与[世界观 · 数据权限与问责](../../canon/world/世界观.md)的问责条款，明确排出玩法定位范围 |

## 叙事

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `NR-1` | 中 | 新故事主线立项 | **玩法定位已就绪**（见 [canon/gameplay/](../../canon/gameplay/玩法定位.md)），剧情结构现在有约束可依。`canon/narrative/` 暂空 |
| `NR-2` | 中 | 学员群像重新设计 | 依赖 `NR-1`。前作九名学员**不继承**（[人物](../../canon/characters/人物.md)）。主角设定已定并已去除章节绑定 |
| `NR-3` | 低 | 其余角色设计：教官团、议会、反派 | 依赖 `NR-1`。前作的这三组**一律不继承**。命名按[人物 · 命名规则](../../canon/characters/人物.md)的两层规则确定 |

## 界面

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `UI-3` | **最高** | 逻辑分辨率与缩放链路落进工程 | [文件](./issue-UI-3-logical-resolution.md)。640×360、`scale_mode="integer"`、最近邻过滤三项都还没设（2026-08-30 读 `project.godot` 实测），不设第一张素材进去就是糊的且不报错。`UI-1` 其余条目都依赖它 |
| `UI-4` | 高 | `canvas_items` 下 12px 像素字的实测与结论回写 | [文件](./issue-UI-4-pixel-text-canvas-items.md)。依赖 `UI-3`。**本轮唯一技术未知项**：[ADR-0008](../../decisions/ADR-0008-中文像素字体选型.md) 那套字体实测是在 `stretch/mode="viewport"` 下做的，工程用 `canvas_items`，后者按屏幕尺寸光栅化字形。不成立会推翻整套渲染配置，所以排在相机与界面之前 |
| `UI-5` | 中 | 相机五项行为 | [文件](./issue-UI-5-camera.md)。依赖 `UI-3`。[玩法定位](../../canon/gameplay/玩法定位.md)点名俯视跟随、侧视 2 倍、边界钳制、震屏、演出镜头并写明"行为不定下来两种视角会各写一套"。建造靠滚动与边缘推镜，不做缩放（作者 2026-08-30 定） |
| `UI-6` | 中 | 界面层级、导航栈与手环容器骨架，含栅格与边距规范 | [文件](./issue-UI-6-ui-skeleton.md)。依赖 `UI-3`。手环取"统一容器带标签页"（作者 2026-08-30 定），关卡内操作页禁用、查看页可用。交出栅格、内边距、间距、安全边距四个数与图标档位给 `DOC-2` |
| `UI-7` | 中 | 输入映射：键鼠与手柄，6 技能位走修饰键组合 | [文件](./issue-UI-7-input-mapping.md)。依赖 `UI-3`，`UI-8` 依赖本条。修饰键取两个扳机、闪避与冲刺改为两个键（作者 2026-08-30 定，正典同日已改）。技能轮盘已排除 —— 它要么暂停要么减速 |
| `UI-8` | 中 | 关卡 HUD 的屏幕空间部分 | [文件](./issue-UI-8-hud-screen-space.md)。依赖 `UI-6`、`UI-7`、`ART-4`。它是 `UI-1` 选定的样板界面，落在全游戏最挤的地方：640×360 在侧视 2 倍缩放下有效视野只有 320×180 |
| `UI-9` | 中 | 世界空间 UI：读条、精英血条、伤害数字 | [文件](./issue-UI-9-world-space-ui.md)。依赖 `UI-6`，与 `UI-8` 可并行。[战斗与关卡](../../canon/gameplay/战斗与关卡.md)明确否掉"读条画在界面角落"，要求画在执行者身上 |
| `UI-10` | 中 | `UI-1` 的端到端验证 | [文件](./issue-UI-10-e2e.md)。依赖本轮全部条目 |
| `UI-11` | 中 | 不暂停的面板打开时玩法动作该不该屏蔽 | `UI-7` 实现时撞出来的，跨 `UI-6` 的导航栈与 `UI-7` 的输入门面，故不在 `UI-7` 顺手定。形状：手柄的确认与返回补在下面键与右面键上，而那两个位同时是跳跃与闪避；面板里由拿到焦点的控件消费 `ui_accept`，**但消费不清轮询状态**（`UI-7` 实测）。于是关卡内打开背包（正典要求不暂停）时按确认会既点按钮又跳。正确行为不显然：世界还在跑，跳跃该不该仍然可用？定了之后落点是 `InputRouter` 的遮挡判定，与修饰键那套共用一个门面 |
| `UI-1` | 中 | 确定逻辑分辨率与界面结构 | **进行中。** PRD 为 [`prd-ui-framework.md`](../prd-ui-framework.md)（2026-08-30 作者确认 `approved`），上游约束、六项立项决定与非目标都在那份，本行不复述。已拆成 `UI-3`–`UI-10` 与 `ART-4`、`ENG-10`、`ENG-12`。取 640×360、骨架加一个样板界面、手柄修饰键、手环统一容器四项已定。`DOC-2` 卡在本条 |
| `UI-2` | 低 | 其他宽高比与设备的适配（Steam Deck 等） | 依赖 `UI-1`。**明确后置**：第一优先只保证 Steam 的 Windows PC 端。Steam Deck 是 16:10（1280×800），只支持 16:9 的游戏在上面会有黑边。两条零成本前提已在[玩法定位](../../canon/gameplay/玩法定位.md)守住：`project.godot` 的 `aspect="expand"` 保持不动、界面用锚点与容器而非绝对坐标 —— 做到这两条，本条就是调参数而不是重排全部界面 |

## 美术与音频

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `ART-3` | 中 | 字体随发行必须附 OFL 许可证与版权声明的守卫 | 依赖字体进代码仓（随 `UI-1`）。[ADR-0008](../../decisions/ADR-0008-中文像素字体选型.md) 定的主字体是 SIL OFL 1.1，第 2 条要求**每份拷贝都带许可证与版权声明** —— 也就是发行包里必须有 `LICENSE-OFL`，而漏了不会报错，只会变成上架后的法律问题。机器可判：包里出现字体文件却没有对应许可证就判失败，落点是代码仓 `tools/verify.py` 解包清单那一步（它已经在逐条看 `.pck` 内容）。同时钉住 OFL 第 3 条：将来做了子集化或改字形，输出的字体名不得沿用原名 |
| `ART-4` | 高 | 占位素材生成器与素材槽位登记表 | [文件](./issue-ART-4-placeholder-assets.md)。作者 2026-08-30 定的工作方式：占位素材先跑通逻辑，最终由他用自己的像素资源按槽位替换（他画图需要时间）。三条约束：开发期不限授权（私有单人仓库不构成再分发），**发行包只允许自有与 CC0**（守卫见 `ENG-12`）；能生成的优先生成，零授权风险且一眼假；**占位件的尺寸帧数锚点必须就是最终规格**，否则替换等于重做。阻塞 `UI-8` 与 `ENG-10` |
| `ART-1` | 中 | 音频：BGM 与音效 | **玩法定位已就绪。** 连击类动作战斗对打击音反馈依赖很高，没有音效手感是残的 —— 但音频不属于玩法定位，已明确排出其范围。需覆盖：打击与受击反馈、技能、界面、环境与昼夜、季节 BGM |

## 工程

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `ENG-4` | 中 | 代码仓建立 `CONVENTIONS.md` | 依赖 `ENG-2`。[C# 学习](../../reference/学习CSharp-Java程序员向.md)的命名与风格部分以它为准 |
| `ENG-10` | 高 | 假像素守卫：半透明像素、错误缩放与槽位登记比对 | [文件](./issue-ENG-10-fake-pixel-guard.md)。**已实现待归档**：落成代码仓 `tools/check_assets.py`，接成 `verify.py` 的第一步。四类检查（半透明像素、整图放大件、登记表双向比对、纹理导入参数）加一个误判方向（纯色图不得判成放大件），自证 16/16 条按预期拦下。校验只有一份实现 —— 生成器改为调它 |
| `ENG-12` | 中 | 发行包不得含占位素材的导出守卫 | [文件](./issue-ENG-12-placeholder-export-guard.md)。依赖 `ART-4`。代码仓是私有单人仓库，私有持有不构成再分发，**真正的分发是发行包** —— 所以守卫落在导出而不是入仓。落点已存在：`verify.py` 的导出那步本来就解 `.pck` 逐条看清单（`ENG-3`）。未登记的素材也判失败，否则漏登记等于绕过守卫 |
| `ENG-13` | 中 | 界面节点不得局部覆盖 `texture_filter` 的守卫 | 在 `UI-4` 实现中发现，按"超边界不许顺手修"记账。实测结论：`canvas_items` 下文字清晰**唯一依靠最近邻纹理过滤** —— 把过滤改成线性，12px 中文的最小同色跑长立刻从 2／3／4 塌到 1（2026-08-30，`python tools/font_preview.py --measure --canvas-items --linear-filter`）。而 `CanvasItem.texture_filter` 可以逐节点覆盖并向下继承，一次手滑就静默毁掉整棵子树的文字与素材，且不报错。机器可判：扫 `.tscn`／`.tres` 里非 `Inherit` 的 `texture_filter` 覆盖。落点候选是代码仓 `verify.py` 或 `ENG-10` 那条素材守卫 |
| `ENG-11` | 中 | 行尾守卫覆盖到代码仓 | 依赖 `ENG-2`。代码仓 `.gitattributes` 也声明 `* text=auto eol=lf`，但 `ENG-9` 的守卫长在设计仓 `check_docs.py` 里、`ROOT` 钉死在设计仓，**代码仓现在没有任何行尾守卫**。同一机制在代码仓更危险：`.sh` 与 git 钩子带 `\r` 会静默不执行（[踩坑记录 28](../../reference/踩坑记录.md)）。2026-08-30 用一次性命令核过两次（最近一次 40 个已跟踪文本文件、含 `\r` 的 0 个），但一次性命令不是守卫。二选一：抽成两仓共用的入口，或在代码仓建等价入口、接进已就位的 `tools/verify.py`（`ENG-3`） |
| `ENG-8` | 中 | Steam 集成选型与兼容性验证 | 依赖 `ENG-2`。上架 Steam 需要成就、云存档，可能还有创意工坊（mod 分发）。**不自造绑定**，候选：`Godot.Steamworks.NET`（对 C# 工程最贴，但只声明支持 4.4+）、`Godot-Facepunch.Steamworks`、`GodotSteam`（最知名但面向 GDScript，从 C# 调不顺手）、`Foundation for Steamworks`。**四者均未在 Godot 4.7.2 上验证**，须先实测再选。另需定存档路径与云同步冲突处理 |
| `ENG-7` | 中 | 存档要能容忍缺失的 mod 内容，并明确提示玩家 | 依赖 `ENG-2` 与 `ENG-5`。玩家装 mod 加了角色、存档、再卸载 mod，那个角色在存档里就是悬空引用。**不许静默丢数据，也不许崩档** —— 载入时要列出「因缺少 mod 而不可用」的角色与物品，让玩家自己决定是装回去还是继续。同一套容错也覆盖 mod 版本升级导致的字段变化 |
| `ENG-6` | 中 | 帧级调优工具选型：判定框、硬直与无敌帧、防御窗口、输入缓冲的可视化与逐帧步进 | 依赖 `ENG-2`。玩法正典定的精准防御窗口与连段帧数只能实机逐帧调，靠感觉调违反「结论必须有依据」。**先评估现成方案再决定自己补哪一层**：Godot 自带运行时碰撞框可视化；逐帧判定框编辑见 `animated-shape-2d`；战斗状态机与输入缓冲见 `Fray`。三者**均未实测**，Fray 是 GDScript 插件、从 C# 调用需互操作，4.7.2 兼容性待验证 |
| `ENG-5` | 中 | 全部内容数据外置，角色驱动统一成可替换控制器 | **接缝已就位、内容类型还没有。** 四条零成本预留（角色定义外置、名册容量从配置读、控制器接口、文本外置）已随工程底座落地并各有测试，见代码仓 `ARCHITECTURE.md`。本条**剩下的是把正典点名的其余内容类型都外置**：宝物、宝石、技能、特质、天赋、作物、加工配方、委托模板、敌人 —— 每类都要等对应系统存在才做得了，所以本条会跟着玩法实现分批推进而不是一次做完。边界不变：**内容外置、规则不外置**。**联机未立项**，届时玩家用自定义角色，不扮演主角或学员 |

## 文档

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `DOC-1` | 中 | 复核[踩坑记录](../../reference/踩坑记录.md)前 25 条并补 C# 13／14 特性 | 依赖 `ENG-2`。第 1–25 条描述前作的具体 API 与脚本，本项目尚不存在，需逐条确认「机制类」直接生效、「待复核类」是否仍适用、不适用就删。第 26 条起都是本项目自己踩的，直接生效不必复核。C# 学习文档基线是 C# 12／.NET 8，13 与 14 新增特性未补入 |
| `DOC-2` | 低 | 补界面色彩、尺寸与放置清单 | 依赖 `UI-1`。**尺寸那一半已经交付**：栅格 8、内边距 4、间距 4／8、安全边距 8、图标 16／32、侧视有效视野 320×180，连依据一起列在 [`issue-UI-6`](./issue-UI-6-ui-skeleton.md) 的交付节，实现在代码仓 `rules/Ui/UiMetrics.cs` 且关系有测试盯着。本条剩下的是**色彩与放置**。[像素绘制原则](../../production/像素绘制原则.md)已写通用像素方法，缺的是界面专用部分 |
| `DOC-3` | 低 | 补齐代码侧技能：`code-to-spec`、`refactor`、`smell`、`modern-csharp`、`graph` | 依赖 `ENG-2`。现在写只能凭空假设代码结构，违反「结论必须有依据」。`modern-csharp` 对应上游 `modern-go`，921 行 Go 规则需全部重写 |
| `DOC-5` | 低 | 配额表的行数上限对列表型文档校准不准 | [WORKFLOW §3](../../WORKFLOW.md#3-配额) 的行数与字符数上限隐含假设了散文密度。实测：`spec/prd-gameplay-positioning.md` 曾 349 行仅 12,693 字符、一度触发硬上限 FAIL（软上限 20,000），平均 36 字／行 —— 列表与表格型文档必然先撞行数而字符数还剩一半。修法候选：表格与列表行折算、或按字符数为主判定。改的是配额表定义，须走 [ADR 模板](../../templates/ADR.md)，不许为了让某份文档过关而抬数字 |
| `DOC-8` | 中 | 给「管道读中文输出」加执行体 | [踩坑记录 27](../../reference/踩坑记录.md) 的守卫是「只能人工留意」，理由是拦 `\| Select-String` 会大量误报。**新证据推翻了那个代价估算**：`GP-2` 这一轮我在同一个会话里犯了 3 次，每次都拿到乱码才发现。人工留意在这里实测不管用。落点候选：`.kiro/hooks/command-guard.json` 已经在拦 `git add .` 与批量替换，加一条「命令里同时有管道和 python／git → 转作者确认」即可。误报率要作者权衡 |
| `DOC-7` | 低 | 代码仓 `selfcheck_verify.py` 的日志同秒覆盖 | 在 `GP-2` 评审中发现，按「超边界不许顺手修」撤回后记账。日志名用 `%H%M%S`，同一秒内跑两次时后一次会静默覆盖前一次的证据。设计仓的 `simulate_week.py` 已在本轮修掉（同名时加序号），代码仓那份**未修**。一行改动，可搭 `ENG-11` 那轮顺带做 |
| `DOC-6` | 低 | 推演脚本的退役判断 | 依赖 `GP-2` 与规则层公式实现。`GP-2` 的七天推演做成设计仓 `tools/` 下的 py 入口，公式因此有两处（推演脚本与将来的 C# 实现），存在漂移风险。届时判断：退役脚本，还是改成读同一份参数表对实现做交叉验证。**判断依据是那时才有的**（实现是否真的读同一份参数），所以现在只记账不预判 |
| `DOC-4` | 低 | 复查[C# 学习](../../reference/学习CSharp-Java程序员向.md)规模 | 现为 700 行，超软上限 600、未达硬上限 900，**本轮判定可接受**：它是查阅型文档，按语法主题拆开会让「遇到看不懂的写法来这里查」变难。补入 C# 13／14 特性（`DOC-1`）后接近 900 时再按主题拆分 |

## 已归档编号

已完成并归档的编号登记在这里，**它们不是待办**。

为什么留登记而不是删干净：编号是跨文档引用的稳定标识，本页开头那条「不重编号、不复用已删除的号」需要一个能查的清单才落得住 —— 这就是它的执行体。另有一条硬约束：已接受的 ADR **不得**用今天的理由改写当时的背景（[WORKFLOW §4](../../WORKFLOW.md)），所以 ADR-0006 里对 `ENG-2` 的引用会永久存在，那个编号必须始终解析得出来。

结论去处见[变更日志](../../archive/history/变更日志归档.md)（**历史背景·非依据**）。

| id | 归档日期 | 现行事实落在哪 |
| --- | --- | --- |
| `GP-1` | 2026-08-29 | [canon/gameplay/](../../canon/gameplay/玩法定位.md) 四份正典 |
| `ENG-9` | 2026-08-30 | [WORKFLOW §6](../../WORKFLOW.md) 执行体表；[踩坑记录 28](../../reference/踩坑记录.md)；守卫本体是 `tools/check_docs.py` 的 `check_line_endings` |
| `ENG-2` | 2026-08-30 | [ADR-0007](../../decisions/ADR-0007-测试底座选型.md)；[ADR-0005 验证进展](../../decisions/ADR-0005-技术基线.md)；代码仓 `README.md` 与 `ARCHITECTURE.md` |
| `ENG-1` | 2026-08-30 | [ADR-0005 验证进展](../../decisions/ADR-0005-技术基线.md) 三半全部通过，`net9.0` 回退不再需要；[踩坑记录 32、33](../../reference/踩坑记录.md)；导出办法见代码仓 `README.md` |
| `ENG-3` | 2026-08-30 | [WORKFLOW §6](../../WORKFLOW.md) 执行体表两行；[踩坑记录 29、33 的守卫字段与新增 35、36](../../reference/踩坑记录.md)；用法见代码仓 `README.md`「怎么验收」，入口是 `tools/verify.py` 与 `tools/selfcheck_verify.py` |
| `GP-2` | 2026-08-30 | [数值模型](../../design/数值模型.md) 的公式与 16 条判据（**永久留 `design/`，不上升进正典**）；值在 `design/numeric-model-params.json`；[WORKFLOW §6](../../WORKFLOW.md) 执行体表加一行；[踩坑记录 37](../../reference/踩坑记录.md)。当前实测值跑 `python tools/simulate_week.py` |
| `ART-2` | 2026-08-30 | [ADR-0008](../../decisions/ADR-0008-中文像素字体选型.md)（选型、授权、渲染配置、子集化与给 `UI-1` 的三条输入）；[玩法定位 · 像素基准](../../canon/gameplay/玩法定位.md) 的字号与整数缩放两句；[WORKFLOW §6](../../WORKFLOW.md) 执行体表加一行。守卫跑 `python tools/audit_fonts.py`，看字体跑 `python tools/font_preview.py`。遗留在 `ART-3` |
