---
type: backlog
status: active
owner: project
last_verified: 2026-09-18
---

# 待办台账

全库**唯一**待办索引。一条待办一个编号，编号是跨文档引用的稳定标识 —— 正典、制作规格与参考资料都会引它，所以**不重编号、不复用已删除的号**。

条目详情写成 `issue-<ID>-<slug>.md` 放本目录，由 `/to-issues` 产出。只有一句话就说得完的条目可以只留本表一行，不建文件。

**本页是全库唯一的状态所在处**，理由见 [WORKFLOW §3](../../WORKFLOW.md)：散文只写理由，状态只写在这里，别处写链接不复述。一格写不下就把解释移进该行链接的条目文件。**只写结果** —— 已完成的行不留日期、测试条数与提交号，那些会静默过期，而且 git 里查得到。

## 编号前缀

| 前缀 | 领域 |
| --- | --- |
| `GP` | 玩法与数值 |
| `NR` | 叙事 |
| `UI` | 界面 |
| `ART` | 美术与音频 |
| `ENG` | 工程 |
| `DOC` | 文档 |

## 玩法与数值

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `GP-14` | 高 | 训练房：作者在编辑器里搭场景，代理接线 | **新分工的第一次实跑，所以先做它再推广。** 作者在 Godot 里搭训练房（地面、主角节点、碰撞区域、两个沙包、障碍物、参数），代理把 `TrainingRoom.cs` 改成读作者搭的节点与导出参数，不再自己 `AddChild`。原「48px 够不够」并入：作者摆满编场景自己看（`ENG-16`）。手感确认见 `GP-6`。详见 [`issue-GP-14`](./issue-GP-14-training-room-e2e.md) |
| `GP-18` | 中 | 完整角色状态机：三层分层 + 状态效果系统扩展 | 依赖 `GP-10`／`GP-11`／`GP-15`。把散在各切片的「完整状态系统」收敛成一个立项：运动／动作叠加／状态效果三层 + 生命周期终态；控制类效果（含冻结、眩晕）归状态效果层，不进动作机。设计与流转表在 [`design/角色状态机.md`](../../design/角色状态机.md)。实现按切片推进（防御/失衡 → 异常状态 → 技能与读条 → 死亡/倒下 → 精准闪避与空中攻击帧），不一次做完。数值归 `GP-6` |
| `GP-6` | 中 | 战斗手感与帧数的实机校准 | **依赖 `ENG-20`** —— 校准方式是作者在检查器里改数、立刻看结果，所以得先有导出属性。当前值在代码仓 `rules/Combat/CombatFeel.cs` 与 `rules/Ui/CameraFeel.cs`，全部是未校准初值、各自写了取值算式。待收敛项：顿帧时长、连段帧数、无敌窗、击退量、纵深两个速度、命中容差、阻挡阈值、跳跃峰高、白闪帧数、震屏两档、音高抖动。**这些值的默认值不重要，可调才重要。** 正典写明只能实机逐帧调，不进数值模型 |
| `GP-7` | 低 | 等对应系统存在才能定的其余参数 | 依赖各自系统的实现。四项：异常状态持续时间与叠加规则、队友 AI 四档实战值阈值、警戒传播范围与延迟、星火积分兑换上限。`GP-2` 只给量纲与「上限必须存在」这类约束形式，具体值要等能实测出行为差异时才定得出来 —— 跟着实现分批推进，与 `ENG-5` 同一模式 |
| `GP-8` | 低 | 基地可建造区是否要扩大 | 依赖装饰性建造是否要做。正典定 40×30 格（640×480px，[时间与经营](../../canon/gameplay/时间与经营.md)），约是星露谷标准农场可用面积的三分之一（那边可耕地 3,427 格、最大整块 63×31，[Wiki](https://stardewvalleywiki.com/Farm_Maps)）。**第一版不挤**：6 类建筑加种植区实估约 250 格，占两成。长期偏小，而需要大画布的正是被排出第一版的装饰性建造。现在扩要多画三到四倍基地图块，在「建造好不好玩」验证之前不该花。可建造区尺寸已从配置读，所以将来扩大是改数加延伸地图 |
| `GP-5` | 低 | 场景物件可拿起、投掷与持物战斗 | 正典明确第一版不做。物件在纵深模型下的职责是阻挡与掩体，不是可踩平面（多层平台已删，见[战斗与关卡 · 空间模型](../../canon/gameplay/战斗与关卡.md)）。「可拿」要一整套持物移动、持物攻击与投掷动画，乘上主角与各类敌人，属于「看着很小、动画量很大」那一类，与 `GP-4` 同级 |
| `GP-4` | 低 | 招式取消与派生（连击附加项） | 正典明确第一版不做。核心机制只是「每个动作的取消窗口表 + 输入缓冲」，是配置数据不是代码复杂度；**成本在动画分支与手感调优**，窗口帧数只能实机逐帧试。一条省钱结论：玩家只操作主角，学员与敌人用固定连段玩家感知不到差别，所以只需给主角做一套。攻击不得取消到防御 —— 否则收招硬直失去意义 |
| `GP-3` | 低 | 物品偷窃与销赃 | 正典定了「偷窃不转移所属权」，但那条留了个洞：偷来的物品既然操作受所属权限制就无法使用，偷窃需要销赃或洗白途径才有意义。牵扯发现判定、执法反应、信任度后果与[世界观 · 数据权限与问责](../../canon/world/世界观.md)的问责条款，明确排出玩法定位范围 |
| `GP-10` | 中 | 规则层：主角运动学与轻重连段状态机 | **已完成。** 现行事实在代码仓 `rules/Combat/`（`CombatInput`／`CombatFeel`／`MotorState`／`ComboStateMachine`）与 `tests/Combat/`。详见 [`issue-GP-10`](../../archive/spec/A1-combat-feel-core/issue-GP-10-combat-motion-combo-rules.md) |
| `GP-11` | 高 | 规则层：统一状态载体与命中结算 | **已完成。** 统一状态载体、轻重命中结算、纯规则顿帧计时与 Motor 无敌接入。详见 [`issue-GP-11`](../../archive/spec/A1-combat-feel-core/issue-GP-11-status-carrier-hit-resolution.md) |
| `GP-12` | 中 | 引擎层：主角节点驱动 + 动画接入 | **已完成，作者实机确认手感。** 主角驱动、`ART-6` 自绘表接入与缺图几何占位已落地。详见 [`issue-GP-12`](../../archive/spec/A1-combat-feel-core/issue-GP-12-player-actor-engine.md) |
| `GP-13` | 中 | 引擎层：命中检测 + 打击反馈四件套 + 木桩 | **已完成，作者实机确认手感**（含 `GP-20` 强化后的组合）。判定框按轻重分开。详见 [`issue-GP-13`](../../archive/spec/A1-combat-feel-core/issue-GP-13-hit-detection-feedback.md) |
| `GP-15` | 高 | 规则层：纵深轴运动（连续、48px 带、三轴分离） | **已完成。** 连续纵深、钳在 `DepthBand` 的 48px 带内、离地锁与落地解锁、出招三轴一同定身、闪避方向改二维。纵深两个速度是初值，归 `GP-6`。详见 [`issue-GP-15`](../../archive/spec/A1-combat-feel-core/issue-GP-15-depth-axis-motion.md) |
| `GP-16` | 中 | 纵深容差命中判定 | **已完成。** 命中要横向与纵深两个条件都成立，判定落 `DepthOverlap`（容差由调用方传入）；纵深经 `IDepthActor` 取；去重排在纵深之后（打空不是打过）；击退仍只沿横向。容差 8 是初值，归 `GP-6`。详见 [`issue-GP-16`](../../archive/spec/A1-combat-feel-core/issue-GP-16-depth-hit-tolerance.md) |
| `GP-17` | 中 | 带纵深的实体阻挡 | **已完成。** 阻挡＝阵营 ＋ 纵深两个条件，复用 `DepthOverlap` 但传自己的阈值。修掉两个缺陷：挡在已重叠时生效会被引擎一帧脱插（改「已重叠先不挡」）；纵深方向走进去没人拦（引擎碰撞只覆盖两个轴，纵深要自己夹，另加绕行正面判据防过紧）。阈值 8 是初值，归 `GP-6`。详见 [`issue-GP-17`](../../archive/spec/A1-combat-feel-core/issue-GP-17-depth-aware-blocking.md) |
| `GP-19` | 中 | [受击框按角色取实测本体](../../archive/spec/A1-combat-feel-core/issue-GP-19-hurtbox-per-actor.md) | **已完成。** 原先写死 18×32，而 32 是木桩柱子高、主角实测只有 28，且与 `PlayerActor` 碰撞框那份 28 是两份真相（不报错，表现为头顶掠过也命中）。改成按角色必填、各绑自己的实测本体。逐帧自适应明确不做（会让命中学不会），按状态分归 `GP-18` |

## 叙事

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `NR-1` | 中 | 新故事主线立项 | **已完成。** 正典在 [`canon/narrative/主线故事.md`](../../canon/narrative/主线故事.md)：20 节点、普通／完美两结局、完美结局四条件、四条贯穿线索 |
| `NR-2` | 中 | 学员群像重新设计 | **已完成。** 正典在 [`canon/narrative/学员群像.md`](../../canon/narrative/学员群像.md)：九名学员的身份、背景、视觉锚点、个人弧线与节点 15 的个人选择；加入时机分布在节点 2／3／4 |
| `NR-3` | 低 | 其余角色设计：教官团、议会、反派 | **已完成。** 正典在 [`canon/narrative/反派与要角.md`](../../canon/narrative/反派与要角.md)：最终 BOSS 夙渊、四将首、失踪高手望渚、议会两派代表、商会追查人、穹盾学院教官、断岳 |
| `NR-4` | 低 | 常驻 NPC 设计 | **已完成。** 正典在 [`canon/narrative/常驻NPC.md`](../../canon/narrative/常驻NPC.md)：12 名 NPC，覆盖锈月酒馆、配给枢纽、医疗站、市场、居民区、基地周边与流动商人。命名刻意混合语体，与学员群像、反派要角的意名形成语体分层 |

## 界面

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `UI-12` | 低 | 相机手感数值的后续调整 | **第一版已由作者逐项试过**（跟随、震动、推镜、演出归还六个数）。值在代码仓 `rules/Ui/CameraFeel.cs`，量纲与允许区间有规则层测试盯着，所以调值不改结构。**校准办法与 `GP-6` 同**，随 `ENG-20` 变成检查器里可调。本条留着是因为手感会随战斗实现变 —— 顿帧与击退一进来，震动幅度大概要重调。刻意**不进** `design/数值模型.md`：那份持有的是进得了不等式判据的平衡量 |
| `UI-2` | 低 | 其他宽高比与设备的适配（Steam Deck 等） | **明确后置**：第一优先只保证 Steam 的 Windows PC 端。Steam Deck 是 16:10（1280×800），只支持 16:9 的游戏在上面会有黑边。两条零成本前提已在[玩法定位](../../canon/gameplay/玩法定位.md)守住：`project.godot` 的 `aspect="expand"` 保持不动、界面用锚点与容器而非绝对坐标 —— 做到这两条，本条就是调参数而不是重排全部界面 |
| `UI-11` | 中 | 不暂停的面板打开时玩法动作该不该屏蔽 | **已完成。** 裁定：面板打开时屏蔽跳跃与闪避（仅手柄）。实现在 `rules/Ui/PanelInputBlock.cs` 与 `src/UI/InputRouter.cs`。详见 [`issue-UI-11`](./issue-UI-11-panel-input-block.md) |

## 美术与音频

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `ART-6` | 中 | [主角动作帧与闪白口径](./issue-ART-6-role-action-frames.md) | 裁切与导入归作者在 Godot 里做。**仍然成立的结论**：自绘逐帧件接成 15 表 102 帧、帧框 44×32；**闪白由代码持有**（`modulate` 是乘法，彩色贴图乘白不变白，只能走着色器），所以进仓表不要出现整帧单色的帧；判定框按段分开（轻击三段宽 15／12／16px、踢腿最远最低、重击 21px），数值归 `GP-6`。待作者：实机确认三段可读性；发行前替换正式美术 |
| `ART-1` | 中 | 音频：BGM 与音效 | 连击类动作战斗对打击音反馈依赖很高，没有音效手感是残的 —— 但音频不属于玩法定位，已明确排出其范围。需覆盖：打击与受击反馈、技能、界面、环境与昼夜、季节 BGM |
| `ART-5` | 中 | 下载素材里的柔和投影怎么处理 | **裁定仍然有效**：借来的素材允许带柔和投影，自绘件不允许，例外说明在 `production/像素绘制原则.md` §9。原先靠守卫开例外，现在是人工标准、靠作者实机看。详见 [`issue-ART-5`](./issue-ART-5-soft-shadow-policy.md) |

## 工程

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `ENG-20` | 高 | 手感数值改成引擎层导出属性，检查器里可调 | [ADR-0009](../../decisions/ADR-0009-编辑器主导的开发模式.md) 的落地大头，**`GP-6` 与 `UI-12` 都依赖它**；排在文档收尾之后动手。规则层**不能**引用 Godot（编译器强制），所以 `[Export]` 加不到 `CombatFeel` 上：导出面要落在引擎层节点或 `Resource` 上，值传进规则层。因此 `MotorState`／`ComboStateMachine` 要从「静态读常量」改成「接收参数」—— 这是规则层形状的重构，不小。**一条硬约束**：场景／资源是唯一来源，代码不留能用的默认值（缺配置当场报错），否则两处并存时场景那份会静默胜出 |
| `ENG-21` | 中 | 内容场景改成编辑器可编辑的节点树 | 现状：场景文件每个只有 1 个节点 6 行，地形、碰撞框、精灵、摆位全在 `_Ready()` 里 `AddChild`，编辑器里什么都看不见。要改成作者搭树、脚本挂上去读。**训练房先行**（见 `GP-14`），其余随各自需求推进。脚本缺子节点时要报错说清缺谁，不许自己补建 —— 否则「作者忘了摆」会被静默兜底掉 |
| `ENG-16` | 中 | 纵深带 48px 够不够（满编时读不读得清、打起来挤不挤） | **问题未答。** 作者两次实机都反馈活动区域偏窄、上下留白多。正典那笔几何账只证明「放得下、看得清」，**没证明「打起来爽」**（见[战斗与关卡 · 空间模型](../../canon/gameplay/战斗与关卡.md)）。验法：作者在编辑器里摆满编场景（15 敌 + 5 角色 + 互动物件）自己看。结论若是不够，回改 `rules/Combat/DepthBand.cs` 的 48 —— 那会连带重算「一排装得下几个、几排装得下 20 个」那笔账。随 `GP-14` 一起看 |
| `ENG-19` | 中 | 影子射线把角色当地面，地形需要专属碰撞层 | `GP-17` 带出，按「超边界不许顺手修」记账。`DepthVisual` 的影子射线取 `Mask=1`，而层 1 既是地形也是角色层，于是一个角色跳过另一个角色时射线先打到对方顶面、把它当地面 —— 影子跳到对方头顶，**不报错**。`GP-17` 让角色贴在一起、还能同一横向位置分处两排，这条更容易撞到。修法是给地形专属层并收窄射线掩码，**但那要动 `GP-13` 的碰撞关系**（木桩靠层 1 实现「击退被主角挡住」） |
| `ENG-8` | 中 | Steam 集成选型与兼容性验证 | 上架 Steam 需要成就、云存档，可能还有创意工坊（mod 分发）。**不自造绑定**，候选：`Godot.Steamworks.NET`（对 C# 工程最贴，但只声明支持 4.4+）、`Godot-Facepunch.Steamworks`、`GodotSteam`（最知名但面向 GDScript，从 C# 调不顺手）、`Foundation for Steamworks`。**四者均未在 Godot 4.7.2 上验证**，须先实测再选。另需定存档路径与云同步冲突处理 |
| `ENG-7` | 中 | 存档要能容忍缺失的 mod 内容，并明确提示玩家 | 依赖 `ENG-5`。玩家装 mod 加了角色、存档、再卸载 mod，那个角色在存档里就是悬空引用。**不许静默丢数据，也不许崩档** —— 载入时要列出「因缺少 mod 而不可用」的角色与物品，让玩家自己决定是装回去还是继续。同一套容错也覆盖 mod 版本升级导致的字段变化 |
| `ENG-5` | 中 | 全部内容数据外置，角色驱动统一成可替换控制器 | **接缝已就位、内容类型还没有。** 四条零成本预留（角色定义外置、名册容量从配置读、控制器接口、文本外置）已落地并各有测试，见代码仓 `ARCHITECTURE.md`。**剩下的是把正典点名的其余内容类型都外置**：宝物、宝石、技能、特质、天赋、作物、加工配方、委托模板、敌人 —— 每类都要等对应系统存在才做得了，所以跟着玩法实现分批推进。边界不变：**内容外置、规则不外置**。**联机未立项** |
| `ENG-4` | 中 | 代码仓建立 `CONVENTIONS.md` | **已完成。** 文件在代码仓 `CONVENTIONS.md`：命名、Allman 大括号、属性与字段、表达式体、记录类型、可空、LINQ、注释规范、架构层边界、事件订阅配对、测试约束 |
| `ENG-6` | 中 | 帧级调优工具：判定框/受击框叠层 + 帧步进 | **已完成，作者实机确认无问题；第一份产出就抓到 `GP-19`。** 自建 `CombatDebugOverlay`（世界空间 Node2D、默认关）：判定框与受击框读 `Hitbox.ActiveBoxLocal`／`Hurtbox.BoxLocal`（单一真相），帧步进复用顿帧的「当帧不推进」gate。三候选取自建（内置可视化不够语义、两个 GDScript 插件未装）；顿帧是手动 hitstop 而非 TimeScale；GdUnit4 不采用（踩坑 30）。详见 [`issue-ENG-6`](../../archive/spec/A1-combat-feel-core/issue-ENG-6-frame-tuning-tools.md) |
| `ENG-11` | 中 | 行尾守卫覆盖到代码仓 | **已完成。** `tools/check_eol.py` 是独立入口，`verify.py` 里作为第一步 `step_eol`。用法见代码仓 `README.md` |
| `ENG-15` | 中 | 纵深排序与代码影子 | **已完成，作者已实机看过影子并裁定保持现状。** 规则层两键排序写进 `z_index`、代码画影子（跳起留地面、随高度只缩不淡）。纵深带够不够归 `ENG-16`。详见 [`issue-ENG-15`](../../archive/spec/A1-combat-feel-core/issue-ENG-15-depth-sorting-shadow.md) |
| `ENG-18` | 中 | 素材守卫按「全部文件」判完整性，不按扩展名白名单 | **载体已删**（素材登记表整条链取消，缺图由作者在 Godot 里看出来）。**留下来的是判断方向**：守卫的默认面要是「全部」，白名单是每次新类型都要再补一格的债。起因是磁盘比对只 `rglob("*.png")`，往 `assets/` 丢 `.wav`／`.gdshader`／`.psd` 既不登记也不被拦。这条思路后来又用了一次：`ENG-12` 的发行素材判据改成按目录黑名单判，同样不随新增文件类型失效 |

## 文档

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `DOC-2` | 低 | 界面色彩定稿并推广到其余界面 | **尺寸那一半已交付**：栅格 8、内边距 4、间距 4／8、安全边距 8、图标 16／32、侧视视野 320×180，实现在代码仓 `rules/Ui/UiMetrics.cs`。HUD 有一套标为占位的色板（`rules/Ui/HudPalette.cs`），放置定为四角贴边加技能位横排。**剩下的是把色板定稿** —— 这是作者在编辑器里看着定的事，不需要机器判据。一条硬约束仍在：色值**不带 alpha**（像素只许全透明或全不透明），冷却遮罩用不透明色遮而不是压暗 |
| `DOC-6` | 低 | 推演脚本的退役判断 | 依赖 `GP-2` 公式在规则层落地。`tools/simulate_week.py` 持有七天推演，公式因此将有两处（它与将来的 C# 实现），有漂移风险。届时判断：退役脚本，还是改成读同一份参数表对实现做交叉验证。**判断依据是那时才有的**（实现是否真读同一份参数），现在只记账不预判。它不属于交还编辑器的那批 —— 平衡账实机看不出来，与字体许可证同类 |
| `DOC-1` | 中 | 复核[踩坑记录](../../reference/踩坑记录.md)前 25 条并补 C# 13／14 特性 | **已完成。** 踩坑记录已深复核（前作类名换成通用描述）；C# 学习文档补入 §19（C# 13）与 §20（C# 14）；`CONVENTIONS.md` 已建立 |
| `DOC-7` | 低 | 日志文件同秒覆盖 | **已完成。** 同一秒内跑两次会让第二份日志覆盖第一份，所以 `log_path()` 同名时加 `-2`、`-3` 序号。现在设计仓 `simulate_week.py` 与代码仓 `loglib.py` 都是这个写法 |
| `DOC-8` | 中 | 给「管道读中文输出」加执行体 | **守卫已删，结论已内化。** 中文输出走管道会按非 UTF-8 编码解码、打第一个中文就崩，所以工具一律自己 `reconfigure(encoding="utf-8")` 并自己写日志文件，不靠 shell 重定向攒日志。现在这条只有纪律、没有守卫，见[踩坑记录 27](../../reference/踩坑记录.md) |
| `DOC-9` | 低 | 「`GP-2` 尚未设计」的过期说法 | **已完成。** 全库 12 处（代码仓 6、设计仓 5、`.kiro` 2），改法是写链接指台账、不复述状态。顺带修掉 `.kiro/skills/prd` 里更严重的一句「代码仓是空仓，没有 Godot 工程」。**它自己就是反漂移规则的来源**：立账时只盘了代码仓，因为记账的人只搜了他当时在改的那个仓 —— 根治办法写进 [ADR-0009](../../decisions/ADR-0009-编辑器主导的开发模式.md) 与 [WORKFLOW §3](../../WORKFLOW.md) |

## 已归档编号

已完成并归档的编号登记在这里，**它们不是待办**。

留登记而不删干净，是因为「不重编号、不复用已删除的号」需要一个能查的清单才落得住。另有一条硬约束：已接受的 ADR 不得用今天的理由改写当时的背景（[WORKFLOW §4](../../WORKFLOW.md)），所以 ADR-0006 里对 `ENG-2` 的引用会永久存在，那个编号必须始终解析得出来。

结论去处见[变更日志](../../archive/history/变更日志归档.md)（**历史背景·非依据**）。

| id | 归档日期 | 现行事实落在哪 |
| --- | --- | --- |
| `GP-1` | 2026-08-29 | [canon/gameplay/](../../canon/gameplay/玩法定位.md) 四份正典 |
| `ENG-9` | 2026-08-30 | [WORKFLOW §6](../../WORKFLOW.md) 执行体表；[踩坑记录 28](../../reference/踩坑记录.md)；守卫本体是 `tools/check_docs.py` 的 `check_line_endings` |
| `ENG-2` | 2026-08-30 | [ADR-0007](../../decisions/ADR-0007-测试底座选型.md)；[ADR-0005](../../decisions/ADR-0005-技术基线.md)；代码仓 `README.md` 与 `ARCHITECTURE.md` |
| `ENG-1` | 2026-08-30 | [ADR-0005](../../decisions/ADR-0005-技术基线.md) 三半全部通过，`net9.0` 回退不再需要；[踩坑记录 32、33](../../reference/踩坑记录.md)；导出办法见代码仓 `README.md` |
| `ENG-3` | 2026-08-30 | [WORKFLOW §6](../../WORKFLOW.md) 执行体表；[踩坑记录 29、33、35、36](../../reference/踩坑记录.md)；入口是 `tools/verify.py`，用法见代码仓 `README.md`「怎么验收」 |
| `GP-2` | 2026-08-30 | [数值模型](../../design/数值模型.md) 的公式与 16 条判据（**永久留 `design/`，不上升进正典**）；值在 `design/numeric-model-params.json`；实测值跑 `python tools/simulate_week.py` |
| `ART-2` | 2026-08-30 | [ADR-0008](../../decisions/ADR-0008-中文像素字体选型.md)（选型、授权、渲染配置、子集化）；[玩法定位 · 像素基准](../../canon/gameplay/玩法定位.md) 的字号与整数缩放两句。授权核验跑 `python tools/audit_fonts.py`，看字体跑 `python tools/font_preview.py`。遗留在 `ART-3` |
| `UI-1` | 2026-09-01 | [`archive/spec/UI-1-ui-framework/`](../../archive/spec/UI-1-ui-framework/)（**历史背景·非依据**）；逻辑分辨率 640×360 在 `project.godot`；规则层在代码仓 `rules/Ui/`；色板待 `DOC-2` 定稿 |
| `UI-3` | 2026-09-01 | 同 `UI-1`。`project.godot` 四项缩放配置 |
| `UI-4` | 2026-09-01 | 同 `UI-1`。结论在 [ADR-0008](../../decisions/ADR-0008-中文像素字体选型.md)「承重项是最近邻纹理过滤」 |
| `UI-5` | 2026-09-01 | 同 `UI-1`。实现在代码仓 `rules/Ui/CameraRig.cs`、`src/World/GameCamera.cs` |
| `UI-6` | 2026-09-01 | 同 `UI-1`。层级定义在 `rules/Ui/UiLayer.cs`；排版单位在 `rules/Ui/UiMetrics.cs` |
| `UI-7` | 2026-09-01 | 同 `UI-1`。实现在代码仓 `rules/Ui/InputBindings.cs` 等五文件 |
| `UI-8` | 2026-09-01 | 同 `UI-1`。HUD 实现 `rules/Ui/HudLayout.cs`、`src/UI/LevelHud.cs`；占位色板 `rules/Ui/HudPalette.cs` 待 `DOC-2` 定稿 |
| `UI-9` | 2026-09-01 | 同 `UI-1`。实现在 `rules/Ui/WorldUiLayout.cs`、`src/UI/WorldSpaceUi.cs` |
| `UI-10` | 2026-09-01 | 同 `UI-1`。端到端测试 `tests/Ui/E2ETests.cs` |
| `ART-3` | 2026-09-01 | 同 `UI-1`。判据在 `tools/verify.py` 的导出步：包里有字体数据就必须有 `LICENSE-OFL.txt` |
| `ART-4` | 2026-09-01 | 同 `UI-1`。**占位素材生成器与登记表已随 `ADR-0009` 删除**，缺图由作者在 Godot 里看出来 |
| `ENG-10` | 2026-09-01 | 同 `UI-1`。**假像素守卫已随 `ADR-0009` 删除**；像素规范 §9 转为人工标准 |
| `ENG-12` | 2026-09-01 | 同 `UI-1`。发行素材判据仍在，改成按目录黑名单判（`assets/downloaded/`、`assets/placeholder/` 下的不许进包），在 `tools/verify.py` 的 `audit_release_assets`。保留理由是授权与法律风险实机看不出来 |
| `ENG-13` | 2026-09-01 | 同 `UI-1`。**texture_filter 守卫已随 `ADR-0009` 删除**；不覆盖 `TextureFilter` 转为 `CONVENTIONS.md` 的约定，覆盖了像素会糊、实机看得出来 |
| `ENG-14` | 2026-09-11 | 输入专项探针互不污染。**结论与载体已随 `ADR-0009` 一并作废** —— 图形探针全部删除，「探针跑起来后不许再执行前台命令」没有适用对象了。留号只为让 `GP-12`／`GP-13`／`GP-15`／`GP-17` 的历史引用解析得出来 |
| `GP-9` | 2026-09-01 | 闪避取方向读「按下瞬间的输入状态」（否掉「短窗口最后有效方向」）。现行事实在[战斗与关卡 · 按键与连击](../../canon/gameplay/战斗与关卡.md)；实现是闪避状态机直取 `InputRouter` 当前轮询状态，不加缓冲窗口 |
| `GP-20` | 2026-09-13 | 命中当帧的爆点：打击火花、极短白闪、占位音效、震屏轻重两档，顿帧未动。现行事实在代码仓 `rules/Combat/CombatFeel.cs`、`rules/Ui/CameraFeel.cs`、`src/World/`（`HitSpark`／`CombatAudio`／`PlayerActor`／`TrainingRoom`）。数值归 `GP-6`、正式音效归 `ART-1`。归档件 [`GP-20-hit-feedback-juice/`](../../archive/spec/GP-20-hit-feedback-juice/issue.md) |
| `ENG-17` | 2026-09-13 | 代码仓 `CONVENTIONS.md` 新增三节（局部变量与 `var`、三元与多路分派、构造与 `new`），并定点重构了 `PlayerActor.UpdateVisual` 等处。归档件 [`ENG-17-readability-conventions/`](../../archive/spec/ENG-17-readability-conventions/issue.md) |
| `A1` | 2026-09-18 | 战斗手感核心（进攻侧竖切片 + 帧调优工具）。归档件 [`A1-combat-feel-core/`](../../archive/spec/A1-combat-feel-core/prd.md)（**历史背景·非依据**）。玩法结论在 [canon/gameplay/](../../canon/gameplay/战斗与关卡.md)，实现在代码仓 `rules/Combat/`、`src/World/`；未完部分是本页的 `GP-6`、`GP-14`、`ENG-16`、`ENG-20`、`ART-6` |
