---
type: backlog
status: active
owner: project
last_verified: 2026-09-21
---

# 待办台账

全库**唯一**待办索引。一条待办一个编号，编号是跨文档引用的稳定标识 —— 正典、制作规格与参考资料都会引它，所以**不重编号、不复用已删除的号**。

条目详情写成 `issue-<ID>-<slug>.md` 放本目录，由 `/to-issues` 产出。只有一句话就说得完的条目可以只留本表一行，不建文件。

**本页是全库唯一的状态所在处**，理由见 [WORKFLOW §3](../../WORKFLOW.md)：散文只写理由，状态只写在这里，别处写链接不复述。一格写不下就把解释移进该行链接的条目文件。**只写结果** —— 已完成的行不留日期、测试条数与提交号，那些会静默过期，而且 git 里查得到。

## 编号前缀

| 前缀 | 领域 |
| --- | --- |
| `GP` | 玩法与数值 |
| `NR` | 叙事 |
| `UI` | 界面 |
| `ART` | 美术与音频 |
| `ENG` | 工程 |
| `DOC` | 文档 |

## 系统深化顺序

**这是设计阶段的顺序，不是实现顺序。** 待办按编号记在下面各域表里；本节只回答「接下来该深化哪个系统的设计」—— 切窗口后先看这里，不用重新推一遍。

排序判据四条，权重递减：**缺了它「第 1 天 → 结局」这条链就断** ＞ 下游依赖数 ＞ 防重构（结构选错要重做一大片）＞ 当前设计深度浅。最后一条只在前三条成立时才加分。

| 档 | 系统 | 进度 |
| --- | --- | --- |
| 0 | 推进骨架与长期时间轴 | **设计已交付** [`design/主线推进系统.md`](../../design/主线推进系统.md)，取舍在 [ADR-0016](../../decisions/ADR-0016-推进挂在曾经发生过的事上.md)，规则与理由都在那两份、本格不复述。**它同时交付了档 1 那一项（主线任务与结局判定）** —— 同一份文档，所以只有 `NR-11` 一条登记行。排在全部基础文档之后是刻意的：前几批留给它的口子要先各有对象。落地顺序与那几条反证在条目文件里。数值归 `GP-7`（**零个新量**，理由在那一份），界面归 `UI-21` |
| 0 | 活跃失衡：升降途径、全城总量与结局阈值 | 设计已交付 [`design/活跃失衡系统.md`](../../design/活跃失衡系统.md)：总量是「社区组 ＋ 被点名角色 ＋ 环境项」三层之和（不给一千个居民各算一遍）、境况事件声明多组增减且**代价付在别人身上的必须双向**、越低越难再降是缩放不是硬下限、**不按天推进所以每日结算不加步**、永久增益落[状态载体](../../design/状态效果系统.md)的永久项、主角那份走显示与实际分离。落地顺序：三层存储与读口 → 境况事件与双向判据 → 递减 → 增益接载体 → 总量读口接魔源与结局。数值归 `GP-2`（已登记在[数值模型](../../design/数值模型.md)的尚未给值表） |
| 0.5 | `canon/` 四域正典补全 | **已完成。** [`世界观`](../../canon/world/世界观.md)（活跃失衡、变异、外域、学院治理、公共建设）、[`人物`](../../canon/characters/人物.md)（教学是主角与世界的主要接口）、[玩法三份](../../canon/gameplay/玩法定位.md)（教育、失衡作为第三条长期线）、[`主线故事`](../../canon/narrative/主线故事.md)（完美结局条件与那几条重要支线、判定时点落节点 17、节点 19 告别与 20 彩蛋）。`第一版` 分档与全部「前作」说法都已从活文档移除 |
| 1 | 基地画布与生产渠道 · 经济（`GP-43`）· 建造（`GP-44`）· 主线任务与结局判定 · 人际 · 任务与关卡片段拼装 · 存档与推进状态序列化 | **生产、人际、任务与关卡的文档侧都已交付**（编号见各域表与下面「已归档编号」）。任务与关卡按**任务 ＋ 关卡**两份写完了，拼装规则归关卡侧、出征采集也收在那一份。**经济与建造的文档侧也已交付** —— 它们排这一档的理由：还清债务是离场的前置（见档 0 那三条长期线），而建造被仓库扩容与建筑室内两处等着。经济那份顺带还掉了两笔欠账（任务的报酬引用、物品的品质系数），建造那份接上了两个别人在等的挂点。**存档的文档侧也已交付** —— 它排这一档的理由是要等各系统的字段定型，而那些声明到本批才齐。**这一档的文档侧全部已交付**：主线任务与结局判定随档 0 那一份一起交（同一份文档，编号 `NR-11`），它的形状本来就卡在节点解锁条件 |
| 2 | 成长（`GP-30`）· 技能（`GP-31`）· 伤害（`GP-32`）· 装备（`GP-24`）· 战斗 AI 与感知视野（`GP-36`／`GP-51`）· 角色状态剩余切片（`GP-18`）· 剧情演出与对话（`NR-7`）· 事件与持续引导（`GP-52`）· 经营侧动作机与每日结算 | **这一档的文档侧全部已交付**（`design/` 根下，落地顺序与各自的承重反证在每个编号的备注里，本格不复述）。分批的理由留在这里：成长、技能、伤害**互相咬**所以一批写，装备紧跟着它们（那三份都写着加成的大头来自宝物、宝石、天赋与状态，而生活技能的专长用的是同一个永久载体）；那三样 AI 一起看是正典的要求；演出与事件同批，是因为事件要走通知与日记回看，而日记与对话共用同一条文本管线。**经营侧动作机与每日结算跟着存档那一批走**：结算与自动存档在同一时刻发生，分两批写会让「跑一半退出」在两份文档里各答一次；而经营侧那台机器并进了动作机那一份，不新开编号 |
| 3 | 定价三层扩展 · 建造种植加工的数据格式 · 其余内容类型外置（`ENG-5`）· 统计事件总线 · 图鉴与地图导航 · 气泡对话（`NR-8`）· 日记（`NR-9`） | 分批的理由留在这里：**那两样叙事载体提到了这一档的最前面做**，因为它们各自要往存档里放什么是存档那一批的输入 —— 排在存档之后就要回头改那一批。**统计事件总线同理提前了**，理由是正典要求发事件从第一天就做，而到存档那一批会发埋点的系统全都已经写完 —— 再晚一批就会出现「已经写完却没留挂点」的系统。其余各项的状态在各自编号的备注里，本格不复述 |
| 4 | 二周目与星火积分 · 真实形态 · 成就与 Steam（`ENG-8`）· 装饰性建造（`GP-8`）· 天气与节日 · 学生日程 AI | 明确后置 |

两条边界写在这里，免得每次重新讨论：

- **设计按完整形态写，不按批次写。** 正典各系统清单里原来的「第一版 是／否」那一列已删除 —— 做不做、先做哪个是状态，而那一列让同一个状态有五个家。「先做哪一块」只在本节，[玩法定位](../../canon/gameplay/玩法定位.md)不复述。
- **唯一保留的量级约束是[内容规模基准](../../canon/gameplay/玩法定位.md)那张表**：它不是范围承诺，而是[数值模型](../../design/数值模型.md)全部平衡判据的算账基准，改它要重跑 `simulate_week.py`。

## 玩法与数值

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `GP-54` | 高 | 存档落地 | 设计已交付 [`design/存档系统.md`](../../design/存档系统.md)，规则与理由都在那份，本格不复述。**它同时交付了 `ENG-7` 的文档那一半。** 落地顺序、反证与那条「各系统自己声明、本页不持有字段清单」的判断过程都在条目文件里。格式与版本口径在 [ADR-0015](../../decisions/ADR-0015-存档的版本与缺字段口径.md)。界面归 `UI-20`，体积实测归 `GP-7`（并进日记那一行，不新开） |
| `GP-55` | 中 | 统计事件总线落地 | 设计已交付 [`design/统计事件总线系统.md`](../../design/统计事件总线系统.md)，规则与理由都在那份，本格不复述。**排在各发出者自己落地之前或与它们同批** —— 发出者那一格是一行调用，总线不在时那一行写不出来。落地顺序在条目文件里。**给参数表加零个量**，理由写在那一份 |
| `GP-56` | 中 | 每日结算落地 | 设计已交付 [`design/每日结算系统.md`](../../design/每日结算系统.md)，规则与理由都在那份，本格不复述。**那串步序一个字不改、一步不增**（家在[时间与经营](../../canon/gameplay/时间与经营.md)）。**排在各步持有者自己落地之后或与它们同批**：它调的每一步都得先存在。它对代码提的唯一一条结构要求是**世界状态要复制得出来**，实现时先答它。落地顺序在条目文件里 |
| `GP-57` | 中 | `ADR-0015` 的落地反证 | [ADR-0015](../../decisions/ADR-0015-存档的版本与缺字段口径.md) 已接受。落地那四条（旧版本补默认值、**同版本缺字段报错**、更新的版本显式拒绝、字段粒度丢弃而分片粒度不丢）各要一条用例，外加一条把承重项钉住的反证。跟着 `GP-54` 走 |
| `GP-43` | 高 | 经济系统落地 | 设计已交付 [`design/经济系统.md`](../../design/经济系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：钱包与支出管道（含「只拒不赊」的反证）→ 三层定价与每层的原因读口 → **点差落在三层之外的反证**（改点差时三层一个字不变，承重项）→ 债务三个字段与单利计息（接已有那一步，不新增结算步）→ 报酬引用解析 → 收购与商店（上架读所在区、刷新惰性算）→ 事件调价入口 → 存档。数值归 `GP-2` 与 `GP-7` |
| `GP-44` | 高 | 建造系统落地 | 设计已交付 [`design/建造系统.md`](../../design/建造系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：两套网格与同一份冲突判定 → 建筑定义缺字段报错 → 摆位四条拒绝路径（含门前一格）→ 施工状态机与取消（**施工中就占地**是承重项）→ 升级／拆除／移动三条路径（**移动仍过全部判定**的反证）→ 仓库格数与室内摆位两个挂点 → 存档。**建筑要作者搭，所以排在 `ENG-21` 之后或与它同批**；尺寸落配置在 `ENG-22`。数值归 `GP-2` |
| `GP-41` | 中 | 任务系统落地 | 设计已交付 [`design/任务系统.md`](../../design/任务系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：任务实例与目标条目的存储 → 进度折算并接上已有的 HUD 目标视图（`rules/Ui/HudViewModel.cs`，现在由演示数据喂）→ 委托板的两步接进每日结算（含无门槛保底与「先作废后补满」的反证）→ 达成／撤退／失败三条结算 → 额度与精力两道闸门 → 事件入口与统计事件 → 存档。数值归 `GP-2` 与 `GP-7` |
| `GP-42` | 中 | 关卡系统落地 | 设计已交付 [`design/关卡系统.md`](../../design/关卡系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：片段的两条轴与缺声明报错 → 接得上的两条拒绝路径 → **种子与「拼装是纯函数」的反证**（同一个种子拼出同一张图，承重项，**关卡事件也在这条之内**）→ 节奏约束与放宽顺序 → 转场三个方向 → 关卡进度进续玩点 → 资源点分类与采集管道 → 敌人预算交 `GP-36`。**转场现在多一道门**（[ADR-0013](../../decisions/ADR-0013-战斗片段就是一屏.md)）。**片段要作者搭，排在 `ENG-21` 之后或与它同批**。数值归 `GP-2` 与 `GP-6` |
| `GP-28` | 高 | [`design/生产系统.md`](./issue-GP-28-production-system-doc.md) | 文档已交付 [`design/生产系统.md`](../../design/生产系统.md)，规则与理由都在那份，本格不复述。剩下的实机确认等实现，清单在 `DOC-16` |
| `GP-32` | 高 | 伤害系统落地 | 设计已交付 [`design/伤害系统.md`](../../design/伤害系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：结算顺序落成一条线（在 `rules/Combat/HitResolution.cs` 旁新增，A1 的命中反馈行为不变）→ 两种伤害类型各对一份防御 → 穿透与暴击各就各位 → 状态线的钳制与两条硬约束 → 阵营判定（同阵营不进结算）。数值归 `GP-2` 与 `GP-7` |
| `GP-31` | 中 | 技能系统落地 | 设计已交付 [`design/技能系统.md`](../../design/技能系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：字段表落成外置数据（缺字段当场报错）→ 三种形态的槽位规则 → 每技能各自的冷却下限 → 光环三条约束里能判的两条 → **技能孔**按实战值档位解锁（宝物孔那一侧归 `GP-24`，两种孔的分名见 [ADR-0012](../../decisions/ADR-0012-宝石的两个镶嵌目标.md)）。「负面疼不疼」永远没有机器判据，是内容审查的事 |
| `GP-30` | 中 | 成长系统落地 | 设计已交付 [`design/成长系统.md`](../../design/成长系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：派生表落成下游只读的视图（基底与来源分两栏取得到）→ 三条防挂机纪律各一条反证 → 天赋点总量上限 → 实战值按「角色 × 技能」记且汇总量是派生的（**队友的操作档读它**）→ 修饰统一接状态载体 → 精准防御回报量那一行派生量（它可被天赋与状态提升）。**生活技能的档位与专长不在本条，归 `GP-45`。** 数值归 `GP-2` 与 `GP-7` |
| `GP-45` | 高 | 生活技能的档位与专长落地 | 设计已交付：阶梯与规则在[角色与成长 · 档位与专长：练上去是在做选择，不只是数字变大](../../canon/gameplay/角色与成长.md)，结构在[`成长系统`](../../design/成长系统.md)，本格不复述。**落地顺序**：档位阈值与升档 → 专长条目缺字段报错 → **第一档不给专长与「没选不给加成也不受罚」各一条反证** → 选定不可更改 → 效果接[`状态效果系统`](../../design/状态效果系统.md)的永久项（不新建载体）→ 轴按技能各自声明的两条拒绝路径 → 学生自动选与玩家覆盖。界面归 `UI-15`，阈值与幅度归 `GP-7` |
| `GP-38` | 中 | 人际系统落地 | 设计已交付 [`design/人际系统.md`](../../design/人际系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：无序对的存储与档位读口（不存在的对读作最低档）→ 涨法接同一条只收正值的增量管道并带来源标签 → **单调性反证**（结局判定省掉闩锁全靠它）→ 每对每日涨幅上限 → 恋爱资格字段（缺了载入报错）→ 接队友 AI 配合轴、索取与结局判定，外加[`事件系统`](../../design/事件系统.md)那边小事件读的那个档位口。数值归 `GP-2` 与 `GP-7` |
| `GP-52` | 中 | 事件落地（含持续引导与关系小事件） | 设计已交付 [`design/事件系统.md`](../../design/事件系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：字段表与缺字段报错 → 钩子只被询问（含不轮询的反证）→ 结算之后那一处（含**委托板补满仍然满**的反证）→ 后果表逐个接上已有入口（含「没有『自己算』那一格」的反证）→ 记账与存档 → 一次性与冷却 → **关卡事件由种子定**（反证）→ 关系小事件 → 引导两条出口（含**清单是读口**的反证）→ 教学关那批预置条目。界面归 `UI-18`，数值归 `GP-7` |
| `GP-51` | 中 | 感知与视野落地 | 设计已交付 [`design/感知与视野系统.md`](../../design/感知与视野系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：视野组件字段与缺字段报错 → 阵营并集与共用组件 → 遮挡按声明区域判（含纵深错开不挡）→ 记忆项与残影 → 四类读者接同一份（**含清空可见集的反证**，承重项）→ 记忆项进续玩点。**遮挡区域要作者搭，所以排在 `ENG-21` 之后或与它同批。** 数值归 `GP-7`，残影淡出归 `GP-6` |
| `GP-36` | 中 | 战斗 AI 落地 | 设计已交付 [`design/战斗AI系统.md`](../../design/战斗AI系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：决策循环落成一条链（只从声明的动作集里选）→ 敌人警戒状态机与脱战两条件 → 警戒传播只一跳 → 预警与对策两条载入校验 → 分级面板取子集 → 两个档号各自的阈值与行为门 → 指令档加自由作战默认档 → 难度那两个量接决策循环 → 存档。**依赖 `GP-18` 的载体扩容**（难度那两个量要落永久项）。界面归 `UI-17`，阈值归 `GP-7`、与行为分配同一轮落地 |
| `GP-14` | 高 | 训练房：作者在编辑器里搭场景，代理接线 | **新分工的第一次实跑，所以先做它再推广。** 作者在 Godot 里搭训练房（地面、主角节点、碰撞区域、两个沙包、障碍物、参数），代理把 `TrainingRoom.cs` 改成读作者搭的节点与导出参数，不再自己 `AddChild`。原「48px 够不够」并入：作者摆满编场景自己看（`ENG-16`）。手感确认见 `GP-6`。详见 [`issue-GP-14`](./issue-GP-14-training-room-e2e.md) |
| `GP-23` | 中 | 背包与仓库重新设计 | 设计已交付 [`design/背包系统.md`](../../design/背包系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：堆标识（种类 ＋ 品质）与满载判定 → 四条拒绝路径（都只拒不丢）→ 排序筛选与一键整理接界面（`UI-13`）→ 关卡内翻包暂停接顿帧闸门 → **C7 按堆数重算**（预期堆数与重算后的格数归 `GP-7`，已登记在[数值模型](../../design/数值模型.md)的尚未给值表）→ 扩容接[`建造系统`](../../design/建造系统.md)（仓储级数是**唯一**入口）与背包等级。除品质这一维，正典的背包规则一条没改 |
| `GP-24` | 高 | 装备系统落地 | 设计已交付 [`design/装备系统.md`](../../design/装备系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：两张字段表落成外置数据（缺字段报错）→ 词条掷定一次且不可变（含「无重掷入口」的反证）→ **装上加永久项、卸下按来源整批撤**（承重项，反证是「另一件那批一条不少」）→ 两种孔按声明与档位各自开 → 强化走那条唯一的支出管道（含只拒不赊）→ 纯增益那条窄例外的三条反证 → 谁来配与偏好轴 → 存档。**依赖 `GP-18` 的载体扩容** —— 永久项那一档现在还不存在。界面归 `UI-16`，数值归 `GP-7` |
| `GP-22` | 高 | 物品：稀有度与品质两条轴落地 | 设计已交付 [`design/物品系统.md`](../../design/物品系统.md)：两条轴正交且四个角都要存在、品质每个实例各带一个、并进基础价而不当第四个乘数、**必须在物品上看得见**（否则价格锚点查不到原因）、高品质只从制作／变异／掉落来而**不许合成**、第一版加工不传递原料品质。明确不做耐久与品质衰减。**品质是实例属性，所以堆要按「种类 ＋ 品质」分** —— 但堆是容器的概念，**格位、满载判定与判据 C7 的重算都在 `GP-23`**，这条只到「品质是实例属性」为止。落地顺序：两条轴进物品定义 → 品质进基础价并可见 → 商店按失衡档位上架；容器那一侧跟 `GP-23` 走。**上架清单与刷新节奏不在这条** —— 等真有物品了才定得出来。顺带：高级的「特殊渠道」里若含黑市，那是 `GP-3` 的载体，跟着它走 |
| `GP-18` | 中 | [完整角色状态：动作机姿态 + 状态效果载体扩容](./issue-GP-18-actor-state-slices.md) | 依赖 `GP-10`／`GP-11`／`GP-15`。设计已交付两份（[`角色动作状态系统`](../../design/角色动作状态系统.md)与[`状态效果系统`](../../design/状态效果系统.md)），判据是**一次只能一个的归动作机、能并存的归效果**。**载体扩容不依赖任何玩法，可以先做**；落地顺序与姿态切片见条目文件。动作集按角色声明、流转表是全集、两条连通性载入校验（有些怪物没有跳跃）都并进本条。**经营侧那一台动作机也并进本条**（两台机器、共用判据与载体，俯视无竖轴、劳作一律读条、养伤不是姿态）—— 它与战斗侧分两个编号会让同一条判据有两处进度。数值归 `GP-6`（含俯视移动速度） |
| `GP-21` | 中 | 教育系统落地：课堂建筑、两类书与掌握度、授课与考试 | 设计已交付 [`design/教育系统.md`](../../design/教育系统.md)：三个零件各自的归宿、两个新量（书籍掌握度按「角色 × 书」记，知识吸收效率按学生记、满值按角色给且可被特质修饰）、授课产出各接哪个已有系统且每项都乘效率、防刷靠效率连日走低而不是一道开关、考试挂季末与年末。**依赖 `GP-18` 的载体扩容** —— 授课增益与考试评语都是按天项，而现有载体只有帧一种时长单位。落地六步：两类书共用一条阅读管道 → 两个自持量存下来并进存档 → 课堂建筑（作者在编辑器里搭，同 `ENG-21` 的路子）→ 授课动作（一天一次额度、两种形态共用一条产出管道）→ 吸收效率接进收益判定与每日回升（满值按角色取、含「单次下降大于每日回升」的载入校验）→ 考试结算。阅读管道与两个量不依赖任何玩法，可先做；授课动作等载体扩容；课堂建筑等作者。收益量、效率两个幅度与成绩区间归 `GP-2`，要等能实测出行为差异才定得出来的那些跟 `GP-7` 一起走 |
| `GP-6` | 中 | 战斗手感与帧数的实机校准 | **依赖 `ENG-20`** —— 校准方式是作者在检查器里改数、立刻看结果，所以得先有导出属性。当前值在代码仓 `rules/Combat/CombatFeel.cs` 与 `rules/Ui/CameraFeel.cs`，全部是未校准初值、各自写了取值算式。待收敛项：顿帧时长、连段帧数、无敌窗、击退量、纵深两个速度、命中容差、阻挡阈值、跳跃峰高、白闪帧数、震屏两档、音高抖动，外加演出那几样（跳过要按住多久、快进倍率、对话推进的最短间隔）。**这些值的默认值不重要，可调才重要。** 正典写明只能实机逐帧调，不进数值模型 |
| `GP-7` | 低 | 等对应系统存在才能定的其余参数 | 依赖各自系统的实现。**清单不在本格** —— 它在[数值模型](../../design/数值模型.md)的尚未给值表，每一项连校准办法与归属一起写在那里，本格复述只会多一份会过期的副本。`GP-2` 只给量纲与「上限必须存在」这类约束形式，具体值要等能实测出行为差异时才定得出来 —— 跟着实现分批推进，与 `ENG-5` 同一模式 |
| `GP-8` | 低 | 基地画布够不够用的复看 | **问题已被 `GP-25` 回答**：可建造区定 80×60 格（1280×960px，[时间与经营](../../canon/gameplay/时间与经营.md)），约是星露谷标准农场可耕地的 1.4 倍（那边 3,427 格、最大整块 63×31，[Wiki](https://stardewvalleywiki.com/Farm_Maps)）。路线是**小建筑加大画布**：功能建筑外观封在 8×6 格，容量靠室内承担。本条降级成复看 —— 等装饰性建造与畜牧真摆进去，再看画布是不是还要动（装饰摆件与设备走[`建造系统`](../../design/建造系统.md)同一套室内摆位，不新增结构）。尺寸从配置读，改数加延伸地图即可 |
| `GP-5` | 低 | 场景物件可拿起、投掷与持物战斗 | 正典明确第一版不做。物件在纵深模型下的职责是阻挡与掩体，不是可踩平面（多层平台已删，见[战斗与关卡 · 空间模型](../../canon/gameplay/战斗与关卡.md)）。「可拿」要一整套持物移动、持物攻击与投掷动画，乘上主角与各类敌人，属于「看着很小、动画量很大」那一类，与 `GP-4` 同级 |
| `GP-4` | 低 | 招式取消与派生（连击附加项） | 正典明确第一版不做。核心机制只是「每个动作的取消窗口表 + 输入缓冲」，是配置数据不是代码复杂度；**成本在动画分支与手感调优**，窗口帧数只能实机逐帧试。一条省钱结论：玩家只操作主角，学生与敌人用固定连段玩家感知不到差别，所以只需给主角做一套。攻击不得取消到防御 —— 否则收招硬直失去意义 |
| `GP-3` | 低 | 物品偷窃与销赃 | 正典定了「偷窃不转移所属权」，但那条留了个洞：偷来的物品既然操作受所属权限制就无法使用，偷窃需要销赃或洗白途径才有意义。牵扯发现判定、执法反应、关系后果与[世界观 · 数据权限与问责](../../canon/world/世界观.md)的问责条款，明确排出玩法定位范围 |
| `GP-10` | 中 | 规则层：主角运动学与轻重连段状态机 | **已完成。** 现行事实在代码仓 `rules/Combat/`（`CombatInput`／`CombatFeel`／`MotorState`／`ComboStateMachine`）与 `tests/Combat/`。详见 [`issue-GP-10`](../../archive/spec/A1-combat-feel-core/issue-GP-10-combat-motion-combo-rules.md) |
| `GP-11` | 高 | 规则层：统一状态载体与命中结算 | **已完成。** 统一状态载体、轻重命中结算、纯规则顿帧计时与 Motor 无敌接入。详见 [`issue-GP-11`](../../archive/spec/A1-combat-feel-core/issue-GP-11-status-carrier-hit-resolution.md) |
| `GP-12` | 中 | 引擎层：主角节点驱动 + 动画接入 | **已完成，作者实机确认手感。** 主角驱动、`ART-6` 自绘表接入与缺图几何占位已落地。详见 [`issue-GP-12`](../../archive/spec/A1-combat-feel-core/issue-GP-12-player-actor-engine.md) |
| `GP-13` | 中 | 引擎层：命中检测 + 打击反馈四件套 + 木桩 | **已完成，作者实机确认手感**（含 `GP-20` 强化后的组合）。判定框按轻重分开。详见 [`issue-GP-13`](../../archive/spec/A1-combat-feel-core/issue-GP-13-hit-detection-feedback.md) |
| `GP-15` | 高 | 规则层：纵深轴运动（连续、48px 带、三轴分离） | **已完成。** 连续纵深、钳在 `DepthBand` 的 48px 带内、离地锁与落地解锁、出招三轴一同定身、闪避方向改二维。纵深两个速度是初值，归 `GP-6`。详见 [`issue-GP-15`](../../archive/spec/A1-combat-feel-core/issue-GP-15-depth-axis-motion.md) |
| `GP-16` | 中 | 纵深容差命中判定 | **已完成。** 命中要横向与纵深两个条件都成立，判定落 `DepthOverlap`（容差由调用方传入）；纵深经 `IDepthActor` 取；去重排在纵深之后（打空不是打过）；击退仍只沿横向。容差 8 是初值，归 `GP-6`。详见 [`issue-GP-16`](../../archive/spec/A1-combat-feel-core/issue-GP-16-depth-hit-tolerance.md) |
| `GP-17` | 中 | 带纵深的实体阻挡 | **已完成。** 阻挡＝阵营 ＋ 纵深两个条件，复用 `DepthOverlap` 但传自己的阈值。修掉两个缺陷：挡在已重叠时生效会被引擎一帧脱插（改「已重叠先不挡」）；纵深方向走进去没人拦（引擎碰撞只覆盖两个轴，纵深要自己夹，另加绕行正面判据防过紧）。阈值 8 是初值，归 `GP-6`。详见 [`issue-GP-17`](../../archive/spec/A1-combat-feel-core/issue-GP-17-depth-aware-blocking.md) |
| `GP-19` | 中 | [受击框按角色取实测本体](../../archive/spec/A1-combat-feel-core/issue-GP-19-hurtbox-per-actor.md) | **已完成。** 原先写死 18×32，而 32 是木桩柱子高、主角实测只有 28，且与 `PlayerActor` 碰撞框那份 28 是两份真相（不报错，表现为头顶掠过也命中）。改成按角色必填、各绑自己的实测本体。逐帧自适应明确不做（会让命中学不会），按状态分归 `GP-18` |

## 叙事

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `NR-11` | 高 | [主线推进落地](./issue-NR-11-progression-system-doc.md) | 设计已交付 [`design/主线推进系统.md`](../../design/主线推进系统.md)，规则与理由都在那份，本格不复述。**它同时收下「系统深化顺序」档 0 与档 1 那两项** —— 同一份文档，不新开第二条登记行。落地顺序、那几条反证与拆篇判断过程都在条目文件里。取舍在 [ADR-0016](../../decisions/ADR-0016-推进挂在曾经发生过的事上.md)。**给参数表加零个量**，理由写在那一份；界面归 `UI-21` |
| `NR-12` | 中 | [`ADR-0016` 的落地反证](./issue-NR-12-adr-0016-progression-axis.md) | [ADR-0016](../../decisions/ADR-0016-推进挂在曾经发生过的事上.md) 已接受：推进挂在「曾经发生过的事」上，不挂日期。落地那几条（两类条件各一条正反例、**带天数的节点声明被拒**、带天数的关闭条件被拒、未知条件类型抛错）各要一条用例，外加一条「只推天数不置位标记、解锁集合不变」的反证。跟着 `NR-11` 走 |
| `NR-7` | 中 | 剧情演出落地 | 设计已交付 [`design/剧情演出系统.md`](../../design/剧情演出系统.md) 与 [ADR-0014](../../decisions/ADR-0014-对话是演出脚本的一类指令.md)，规则与理由都在那两份，本格不复述。**落地顺序**：状态机四条流转 → 指令字段表与缺字段报错 → 正式对话那一类加选项跳转 → 文本按键取（含缺键报错）→ 暂停接既有闸门（含 AI 不产出动作的反证）→ 角色与相机交还由脚本声明 → **跳过不漏状态变更**（承重项）→ 接触发记账与存档。**脚本要作者搭，排在 `ENG-21` 之后或与它同批。** 界面归 `UI-18`，手感量归 `GP-6` |
| `NR-8` | 中 | 气泡对话落地 | 设计已交付 [`design/气泡对话系统.md`](../../design/气泡对话系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：语料条目字段表与缺字段报错 → 上下文键按声明严格匹配 → 两级排序（先优先级档、再声明键数）→ **额度闸门排在选句之前**（含「被挡下的不进冷却池」的反证，承重项）→ 冷却池按「角色 × 条目」记且只进临时续玩点 → 一小串与整串中止 → 文本按键取 → 与引导分属两条呈现通道。数值归 `GP-7` |
| `NR-9` | 中 | 日记落地 | 设计已交付 [`design/日记系统.md`](../../design/日记系统.md)，规则与理由都在那份，本格不复述。**落地顺序**：按角色各一本与「谁有日记」那个声明项 → 条目类别表与缺字段报错 → 纪事类存成模板键加参数快照（含「不存渲染结果」）→ 一件事往多本各写一条与声明式回落 → 心声类条目自带声明 → 需求标记与已发现集合 → 解锁按本判（**主角那本无门**）→ 存档。界面归 `UI-19`，数值归 `GP-7` |
| `NR-5` | 中 | 对话与剧情文本的落点与批次 | **台词本身不进设计仓** —— 内容一律数据外置，所以它是代码仓 `data/` 下的文本。**数据格式这一半已答**：演出脚本与正式对话在[`剧情演出系统`](../../design/剧情演出系统.md)，写作口径在[文案写作规范](../../production/文案写作规范.md)（沿用来的示例与兑现锚点已还掉）。**剩下的只卡节点定义**：第一批必写教学关与开局，其余跟着各节点实现分批走，与 `ENG-5` 同一模式 |
| `NR-1` | 中 | 新故事主线立项 | **已完成。** 正典在 [`canon/narrative/主线故事.md`](../../canon/narrative/主线故事.md)：节点、普通与完美两种结局、完美结局条件、重要支线与贯穿线索都在那一份，**本格不复述那几个数**。解锁与判定的结构见 [`design/主线推进系统.md`](../../design/主线推进系统.md) |
| `NR-2` | 中 | 学生群像重新设计 | **已完成。** 正典在 [`canon/narrative/学生群像.md`](../../canon/narrative/学生群像.md)：九名学生的身份、背景、视觉锚点、个人弧线与节点 15 的个人选择；加入时机分布在节点 2／3／4 |
| `NR-3` | 低 | 其余角色设计：教官团、导师层、反派 | **已完成。** 正典在 [`canon/narrative/反派与要角.md`](../../canon/narrative/反派与要角.md)：最终 BOSS 夙渊、四将首、失踪高手望渚、立场对立的两名学院导师、商会追查人、星空学院教官、断岳 |
| `NR-4` | 低 | 常驻 NPC 设计 | **已完成。** 正典在 [`canon/narrative/常驻NPC.md`](../../canon/narrative/常驻NPC.md)：12 名 NPC，覆盖锈月酒馆、配给枢纽、医疗站、市场、居民区、基地周边与流动商人。命名刻意混合语体，与学生群像、反派要角的意名形成语体分层 |

## 界面

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `UI-21` | 中 | 主线那几处界面：主线列表与解锁通知、告别的二次确认、节点关闭的预警、穹盾改造进度、结局之后那份说明 | **未开始，本批立项记账。** 与 `UI-18`／`UI-19`／`UI-20` 的分工照那三条的形状 ——**按「它属于哪一批的产出」分**，本条只装推进那几处。硬约束落在本批那份新文档里（链接随它同轮补）：解锁提示**一次告知不催促**、未解锁的节点**不列条件**、告别要二次确认并说清之后会怎么样、关闭预警**不点名是哪一条**、改造进度**查得到还差哪一项**、那三条各自达成没有**结局之后才说**。**存档位摘要那一格不在本条** —— 它归 `UI-20`。排版单位沿用 `rules/Ui/UiMetrics.cs`、色板跟 `DOC-2` |
| `UI-20` | 中 | 存档与结算那几处界面：存档位列表与摘要、载入时那份不可用清单、结算摘要弹窗与手环里的完整明细 | **未开始，本批立项记账。** 与 `UI-18`／`UI-19` 的分工：那两条装演出、对话、提示、气泡与日记，本条只装存档与结算。四条硬约束落在本批两份新文档里：**存档位列表要读得出「这一位是哪条世界线」**（否则一屏存档位是一堆一样的格子）、**不可用清单要说得出「装回哪个 mod 就能恢复」与「继续之后会怎么样」**、**存档写入失败要说清这一天有没有推进**、**简版摘要与完整明细要看得出是同一天的两种粒度而不是两份账**。**读档界面不挂手环** —— 那条边界在[`存档系统`](../../design/存档系统.md)。排版单位沿用 `rules/Ui/UiMetrics.cs`、色板跟 `DOC-2` |
| `UI-19` | 中 | 气泡与日记那几处界面：气泡框、按角色上色、日记按本与按类别翻阅 | **未开始，本批立项记账。** 与 `UI-18` 的分工：那一条装演出、正式对话与引导提示，本条只装气泡框与日记。三条硬约束落在本批两份新文档里：**气泡与引导提示要在形状上不一样**（不能只靠颜色 —— 气泡按说话者上色会把颜色这一维用满，而引导提示不许被漏掉）、**说话者靠位置认而颜色只是辅助**、**被锁住的那本要说得出为什么锁着**。排版单位沿用 `rules/Ui/UiMetrics.cs`、色板跟 `DOC-2`（**色值不带 alpha** 那条硬约束在那一条里） |
| `UI-18` | 中 | 演出与提示那几处界面：跳过与对话推进、引导提示与它的可回查清单、事件通知与日记回看的入口 | **未开始，本批立项记账。** 与 `UI-13`／`UI-15`／`UI-16`／`UI-17` 的分工：那四条各装背包、手环两个新界面、装备三处与 AI 那几处，本条只装演出、对话与提示。三条硬约束落在本批两份新文档里：跳过要按住而不是点一下、引导提示与气泡要长得不一样（前者不许被丢弃）、引导清单要查得到条目正文而不只是标题。排版单位沿用 `rules/Ui/UiMetrics.cs`、色板跟 `DOC-2` |
| `UI-17` | 中 | AI 那几处界面：两个档号怎么看得见、指令怎么下、感知怎么表示 | **未开始，本批立项记账。** 与 `UI-13`／`UI-15`／`UI-16` 的分工：那三条各装背包、手环两个新界面与装备三处，本条只装战斗 HUD 上与 AI 有关的部分。四条本批定下的硬约束落在[`战斗AI系统`](../../design/战斗AI系统.md)与[`感知与视野系统`](../../design/感知与视野系统.md)：**两个档号要各看得见一格**（不合成的全部收益在这里）、**升档提示要说出解锁了哪条行为**、**残影要读得出是「刚才在这」**、**打断反馈要与命中反馈分得开**。控件与排版由作者实机定，排版单位沿用 `rules/Ui/UiMetrics.cs`、色板跟 `DOC-2` |
| `UI-16` | 中 | 装备那几处界面：宝物栏、镶嵌与强化预览 | **未开始，本批立项记账。** 与 `UI-13` 的分工：那一条只装背包的排序、筛选与一键整理，装备栏挂在角色面板上。三条本批定下的硬约束落在[`装备系统`](../../design/装备系统.md)：词条的正负面要看得全、换装的差值要当场看得出来、强化的拒绝原因要分得清是缺钱还是缺材料。镶嵌界面挂技能那一侧而不是宝物栏 —— 孔是技能的属性。控件与排版由作者实机定，排版单位沿用 `rules/Ui/UiMetrics.cs`、色板跟 `DOC-2` |
| `UI-15` | 中 | 手环里两个新界面：专长选择与价格因子明细 | **未开始，本批立项记账。** 两个界面各带一条本批定下的硬约束，落点写在各自的系统文档里：**专长选定后不可更改，所以三个选项的效果必须说全、不许藏**（`GP-45`）；**每个价格因子都要查到当前值与一条原因**（`GP-43`，那是正典对定价的硬要求）。控件与排版由作者实机定，排版单位沿用 `rules/Ui/UiMetrics.cs`、色板跟 `DOC-2`。形状与 `UI-13` 同类 |
| `UI-14` | 中 | [键鼠战斗键位重排](./issue-UI-14-combat-keybinding-remap.md) | 依赖 `GP-34`（先删掉正典那句具体映射）。起因：技能位现在在数字键 1–6，而左手按住 `WASD` 时那一排要抬手。**先走 `/to-design` 出提案**，因为它是受硬约束的布局问题：技能位挪到 `J`／`K` 周边、手柄侧不动（组合已铺满）、左手五指已被移动与冲刺跳跃占满、鼠标接攻击已排掉（右手上鼠标就按不到技能键）。落点 `rules/Ui/InputBindings.cs`，布局好不好只能作者实机按 |
| `UI-13` | 中 | 背包界面：排序、筛选与一键整理 | **未开始，作者已定要做。** 品质分堆的真代价是一屏同名多行、找东西变慢，所以这三样是 `GP-23` 能落地的前提，不是锦上添花。形状与约束在[`背包系统`](../../design/背包系统.md)：排序三个键（按品质、按类别、按最近获得）、筛选按类别与品质且筛选态下照常操作、一键整理只把同标识散堆合起来再按当前排序重排（**不丢弃、不跨容器、不改数量**），且不在拾取时自动跑、不做撤销（格位不承载语义；真出现按格号读背包的东西时这条要翻过来）。控件与排版由作者实机定，排版单位沿用 `rules/Ui/UiMetrics.cs`、色板跟 `DOC-2`。**关卡里那一套也归本条**，规则在[`背包系统`](../../design/背包系统.md)的关卡那一节 |
| `UI-12` | 低 | 相机手感数值的后续调整 | **第一版已由作者逐项试过**（跟随、震动、推镜、演出归还六个数）。值在代码仓 `rules/Ui/CameraFeel.cs`，量纲与允许区间有规则层测试盯着，所以调值不改结构。**校准办法与 `GP-6` 同**，随 `ENG-20` 变成检查器里可调。本条留着是因为手感会随战斗实现变 —— 顿帧与击退一进来，震动幅度大概要重调。**跟随那一侧的适用范围本批收窄了一次**：战斗片段内相机不平移，非战斗片段照常跟随（[ADR-0013](../../decisions/ADR-0013-战斗片段就是一屏.md)）；震动、推镜与演出归还那几个数不受影响。刻意**不进** `design/数值模型.md`：那份持有的是进得了不等式判据的平衡量 |
| `UI-2` | 低 | 其他宽高比与设备的适配（Steam Deck 等） | **明确后置**：第一优先只保证 Steam 的 Windows PC 端。Steam Deck 是 16:10（1280×800），只支持 16:9 的游戏在上面会有黑边。两条零成本前提已在[玩法定位](../../canon/gameplay/玩法定位.md)守住：`project.godot` 的 `aspect="expand"` 保持不动、界面用锚点与容器而非绝对坐标 —— 做到这两条，本条就是调参数而不是重排全部界面 |

## 美术与音频

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `ART-6` | 中 | [主角动作帧与闪白口径](./issue-ART-6-role-action-frames.md) | 裁切与导入归作者在 Godot 里做。**仍然成立的结论**：自绘逐帧件接成 15 表 102 帧、帧框 44×32；**闪白由代码持有**（`modulate` 是乘法，彩色贴图乘白不变白，只能走着色器），所以进仓表不要出现整帧单色的帧；判定框按段分开（轻击三段宽 15／12／16px、踢腿最远最低、重击 21px），数值归 `GP-6`。待作者：实机确认三段可读性；发行前替换正式美术 |
| `ART-1` | 中 | 音频：BGM 与音效 | 连击类动作战斗对打击音反馈依赖很高，没有音效手感是残的 —— 但音频不属于玩法定位，已明确排出其范围。需覆盖：打击与受击反馈、技能、界面、环境与昼夜、季节 BGM |
| `ART-5` | 中 | 下载素材里的柔和投影怎么处理 | **裁定仍然有效**：借来的素材允许带柔和投影，自绘件不允许，例外说明在 `production/像素绘制原则.md` §9。原先靠守卫开例外，现在是人工标准、靠作者实机看。详见 [`issue-ART-5`](./issue-ART-5-soft-shadow-policy.md) |

## 工程

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `ENG-22` | 中 | [新的可建造区尺寸落到配置与相机判据](./issue-ENG-22-buildable-size-config.md) | 依赖 `GP-25`。尺寸本来就是配置项（`rules/Foundation/Config/GameConfig.cs`，无默认值、缺字段报错），所以是改 `data/config/game.json` 加延伸地图。**一处会静默过期的散文要跟着改**：`rules/Ui/CameraFeel.cs` 里建造滚动速度的判据是按 640px 宽算的 |
| `ENG-20` | 高 | 手感数值改成引擎层导出属性，检查器里可调 | [ADR-0009](../../decisions/ADR-0009-编辑器主导的开发模式.md) 的落地大头，**`GP-6` 与 `UI-12` 都依赖它**；排在文档收尾之后动手。规则层**不能**引用 Godot（编译器强制），所以 `[Export]` 加不到 `CombatFeel` 上：导出面要落在引擎层节点或 `Resource` 上，值传进规则层。因此 `MotorState`／`ComboStateMachine` 要从「静态读常量」改成「接收参数」—— 这是规则层形状的重构，不小。**一条硬约束**：场景／资源是唯一来源，代码不留能用的默认值（缺配置当场报错），否则两处并存时场景那份会静默胜出 |
| `ENG-21` | 中 | 内容场景改成编辑器可编辑的节点树 | 现状：场景文件每个只有 1 个节点 6 行，地形、碰撞框、精灵、摆位全在 `_Ready()` 里 `AddChild`，编辑器里什么都看不见。要改成作者搭树、脚本挂上去读。**训练房先行**（见 `GP-14`），其余随各自需求推进。脚本缺子节点时要报错说清缺谁，不许自己补建 —— 否则「作者忘了摆」会被静默兜底掉。**`GP-42` 的片段拼装依赖本条**：片段是作者搭的场景，所以拼装落地排在它之后或与它同批 |
| `ENG-16` | 中 | 纵深带 48px 够不够（满编时读不读得清、打起来挤不挤） | **问题未答。** 作者两次实机都反馈活动区域偏窄、上下留白多。正典那笔几何账只证明「放得下、看得清」，**没证明「打起来爽」**（见[战斗与关卡 · 空间模型](../../canon/gameplay/战斗与关卡.md)）。验法：作者在编辑器里摆满编场景（15 敌 + 5 角色 + 互动物件）自己看；**取景现在是固定的一屏**（[ADR-0013](../../decisions/ADR-0013-战斗片段就是一屏.md)），不再是随机一段。结论若是不够，回改 `rules/Combat/DepthBand.cs` 的 48 —— 那会连带重算那笔几何账，而且[`关卡系统`](../../design/关卡系统.md)把「宽度一致」当片段接得上的条件，改它等于同时改全部片段。随 `GP-14` 一起看 |
| `ENG-19` | 中 | 影子射线把角色当地面，地形需要专属碰撞层 | `GP-17` 带出，按「超边界不许顺手修」记账。`DepthVisual` 的影子射线取 `Mask=1`，而层 1 既是地形也是角色层，于是一个角色跳过另一个角色时射线先打到对方顶面、把它当地面 —— 影子跳到对方头顶，**不报错**。`GP-17` 让角色贴在一起、还能同一横向位置分处两排，这条更容易撞到。修法是给地形专属层并收窄射线掩码，**但那要动 `GP-13` 的碰撞关系**（木桩靠层 1 实现「击退被主角挡住」）。**本批没让它更难，反而少了一个未来的撞点**：视野遮挡刻意**不走引擎射线**、改成读片段声明的遮挡区域（[`感知与视野系统`](../../design/感知与视野系统.md)），理由正是这一条 |
| `ENG-8` | 中 | Steam 集成选型与兼容性验证 | 上架 Steam 需要成就、云存档，可能还有创意工坊（mod 分发）。**不自造绑定**，候选：`Godot.Steamworks.NET`（对 C# 工程最贴，但只声明支持 4.4+）、`Godot-Facepunch.Steamworks`、`GodotSteam`（最知名但面向 GDScript，从 C# 调不顺手）、`Foundation for Steamworks`。**四者均未在 Godot 4.7.2 上验证**，须先实测再选。另需定存档路径与云同步冲突处理 |
| `ENG-7` | 中 | 存档要能容忍缺失的 mod 内容，并明确提示玩家 | **文档那一半已交付**，形状在[`存档系统` · 缺 mod 的内容：隔离，不删除](../../design/存档系统.md)：存的一律是标识所以「悬空」＝解析不到、解析统一发生在全部分片读完之后、存档里要记「写档时启用了哪些 mod」（否则说不出缺哪个）、那份清单是一次载入的产物而不是存储。玩家选「继续」之后**隔离而不是删除** —— 标成不可用、**不参与任何结算与任何计数**，装回 mod 一件不少（承重反证）。**落地跟着 `GP-54` 走**，顺序在它的条目文件里。版本降级不是同一件事，它走 [ADR-0015](../../decisions/ADR-0015-存档的版本与缺字段口径.md) |
| `ENG-5` | 中 | 全部内容数据外置，角色驱动统一成可替换控制器 | **接缝已就位、内容类型还没有。** 四条零成本预留（角色定义外置、名册容量从配置读、控制器接口、文本外置）已落地并各有测试，见代码仓 `ARCHITECTURE.md`。**剩下的是把正典点名的其余内容类型都外置**：宝物、宝石、技能、特质、天赋、作物、加工配方、委托模板、关卡片段、资源点、敌人 —— 每类都要等对应系统存在才做得了，所以跟着玩法实现分批推进。边界不变：**内容外置、规则不外置**。**联机未立项** |
| `ENG-4` | 中 | 代码仓建立 `CONVENTIONS.md` | **已完成。** 文件在代码仓 `CONVENTIONS.md`：命名、Allman 大括号、属性与字段、表达式体、记录类型、可空、LINQ、注释规范、架构层边界、事件订阅配对、测试约束 |
| `ENG-6` | 中 | 帧级调优工具：判定框/受击框叠层 + 帧步进 | **已完成，作者实机确认无问题；第一份产出就抓到 `GP-19`。** 自建 `CombatDebugOverlay`（世界空间 Node2D、默认关）：判定框与受击框读 `Hitbox.ActiveBoxLocal`／`Hurtbox.BoxLocal`（单一真相），帧步进复用顿帧的「当帧不推进」gate。三候选取自建（内置可视化不够语义、两个 GDScript 插件未装）；顿帧是手动 hitstop 而非 TimeScale；GdUnit4 不采用（踩坑 30）。详见 [`issue-ENG-6`](../../archive/spec/A1-combat-feel-core/issue-ENG-6-frame-tuning-tools.md) |
| `ENG-11` | 中 | 行尾守卫覆盖到代码仓 | **已完成。** `tools/check_eol.py` 是独立入口，`verify.py` 里作为第一步 `step_eol`。用法见代码仓 `README.md` |
| `ENG-15` | 中 | 纵深排序与代码影子 | **已完成，作者已实机看过影子并裁定保持现状。** 规则层两键排序写进 `z_index`、代码画影子（跳起留地面、随高度只缩不淡）。纵深带够不够归 `ENG-16`。详见 [`issue-ENG-15`](../../archive/spec/A1-combat-feel-core/issue-ENG-15-depth-sorting-shadow.md) |
| `ENG-18` | 中 | 素材守卫按「全部文件」判完整性，不按扩展名白名单 | **载体已删**（素材登记表整条链取消，缺图由作者在 Godot 里看出来）。**留下来的是判断方向**：守卫的默认面要是「全部」，白名单是每次新类型都要再补一格的债。起因是磁盘比对只 `rglob("*.png")`，往 `assets/` 丢 `.wav`／`.gdshader`／`.psd` 既不登记也不被拦。这条思路后来又用了一次：`ENG-12` 的发行素材判据改成按目录黑名单判，同样不随新增文件类型失效 |

## 文档

| id | 优先级 | 待办 | 备注 |
| --- | --- | --- | --- |
| `DOC-53` | 中 | [正典那几处挂上链接](./issue-DOC-53-canon-wire-in.md) | **已完成。** 正典的意图一个字不改，只挂链接并把几处「形状还没定」改成指落点 —— 改哪几处在条目文件里。**结算顺序那串整份一行未改，也没在它上面加链接**：那串是承重项，动它的形状会让人以为它可以被讨论 |
| `DOC-54` | 中 | [这一批的连带修正](./issue-DOC-54-followup-fixes.md) | **已完成。** 落在已交付文档上：那几处「等推进那一批」改成指落点，外加三处按词核对查出来的真错（[`每日结算系统`](../../design/每日结算系统.md)里「不列那份名单」紧挨着把名单列了出来、[`数值模型`](../../design/数值模型.md) C9 那句用了第三个结局名、本表 `NR-1` 那行复述了一个过期的数）|
| `DOC-55` | 中 | [处理掉那份标着「不得作为依据」的提案](./issue-DOC-55-retire-proposal.md) | **已完成，结论是删。** 判据是「结论迁出之后它还回答什么问题」—— 逐节判下来是零，而仍然有用的那一半（被放弃的那条路）迁进了 [ADR-0016](../../decisions/ADR-0016-推进挂在曾经发生过的事上.md)的候选表。`proposals/` 由此空着，而**体裁规则不变**（理由在条目文件里）。[ADR-0010](../../decisions/ADR-0010-文档体裁与系统文档归位.md)一个字未改 |
| `DOC-56` | 中 | [这一批的新量登记与规模基准复看](./issue-DOC-56-progression-registry.md) | **已完成。** 本批给参数表加的量是**零个**，理由是本页持有的全是「有没有发生过」这一位、**而位没有量纲**（结论与逐样判断在条目文件里）。**结局阈值不新开行** —— 它在活跃失衡那一行。[内容规模基准](../../canon/gameplay/玩法定位.md)**逐样重判后都不补行**，理由各不相同；判据前提的复核是**人看的** |
| `DOC-57` | 中 | [新文档接进索引，本批记账收口](./issue-DOC-57-wire-in-and-bookkeeping.md) | **已完成。** [系统文档与提案索引](../../design/README.md)补一行、[ADR 索引](../../decisions/README.md)补一行，本表按完成情况改写并量过字符数（超限的仍只有 `DOC-17` 点名那三格）。界面侧另立 `UI-21` 一条记账行，而**存档位摘要那一格归 `UI-20`、不新开** |
| `DOC-58` | 低 | [这一批的端到端与交叉核对](./issue-DOC-58-batch-end-to-end.md) | **已完成。** 两个入口全绿（都看了脚本自报的覆盖量，数字取收尾那一轮）。**交叉核对三轮** —— 按词那一轮抓到一处真错：那份「不加步」的名单不只在已知那一页展开，总线那一份也列了并各抄了一句理由，而清单当时已经漏了一项。本批的反证记「不适用（已记账）」而不是「通过」 |
| `DOC-35` | 中 | 角色定义的完整字段表拼不出来 | **未开始，上一批发现后记账。** 一个角色定义上现在挂着的字段散在各处：恋爱资格在[`人际系统`](../../design/人际系统.md)、偏好轴与专属栏格数在[`装备系统`](../../design/装备系统.md)与正典、名册容量在配置里。`ENG-5` 把角色定义外置那一轮要先有一张完整字段表，而现在要拼它得翻遍全部系统文档。**落点是[`成长系统`](../../design/成长系统.md)** —— 它已经持有「角色身上会涨的量」，再多一节「角色定义上不会变的声明项」是同一侧。各字段的规则仍归各自那份文档，这张表只列「有哪些、缺了报不报错、规则在哪」，**不复述规则**。排在 `ENG-5` 之前、不与任何设计批次同批 |
| `DOC-14` | 高 | [新量登记与新渠道收益判据](./issue-DOC-14-numeric-registry-and-c10.md) | 内容规模基准补行与新量登记**已做**。**判据换了基准**：原写「不得高于同期委托」，实测种植已是委托的约三倍（124 对 378 铜每点精力），改成「新渠道的每点精力收益不得高于种植」。**实现与工资进现金流等给值** —— 现在写出来会一条都判不到，配对要求已写进[数值模型](../../design/数值模型.md)的尚未给值表 |
| `DOC-15` | 中 | [这一批的连带修正](./issue-DOC-15-followup-fixes.md) | **已完成。** `教育系统` 的「第 7 类建筑」改按名引用、`物品系统` 补「工序＝配方＋制作者生活技能」、`背包系统` 补「仓储室内单一交互点」、`GP-8` 整条重写。最后一条「数值模型四项属性表复看」**升级成了重写**（攻防分离要把参数路径分成两套），落在 `GP-35` |
| `DOC-16` | 中 | 这一批的端到端与交叉核对 | **文档侧已做**：三个入口全绿；交叉核对查出并修掉两处清单副本 —— 付酬工位改成「只列不付的例外」、生产那节的标题不再列渠道名。**剩下的是实机那半，都要等实现**：看 80×60 的取景与滚动、进一次建筑室内、摆一台设备并添料、钓一次鱼、跑一次每日结算看工钱行。归各自的玩法切片 |
| `DOC-21` | 中 | 「明确不做的系统」是一张会静默过期的状态清单 | **已完成。** 现行事实在[玩法定位 · 明确不做的系统](../../canon/gameplay/玩法定位.md)：那一节只装**永久立场**，判据是「它的理由时间无关」；「现在不做、以后可能做」是状态，家在本台账。原来那行「钓鱼、挖矿这类独立支线活动」**整行删掉** —— 两样都在做：钓鱼是[`生产系统`](../../design/生产系统.md)的一条渠道，挖矿的家是出征采集（[世界观](../../canon/world/世界观.md)的死亡矿山、宝物的采集图挖掘、燃料与高级物品的来源都靠它）|
| `DOC-17` | 低 | 台账备注超长，解释该回 issue 文件 | 仍超过每格 360 字符上限的是 `GP-22`、`GP-21`、`DOC-10`（上限写在 `/to-issues` 技能里）。`NR-5` 原来也在这张名单上，它随剧情演出那一批整段改写后已经缩到限内。**这条上限只有纪律、没有守卫** —— 配额判定随 [ADR-0009](../../decisions/ADR-0009-编辑器主导的开发模式.md) 删了，所以它不会报错、只会继续长。改法是把解释移进各自的 issue 文件，台账只留链接与落地顺序；`GP-18` 与 `UI-13` 已按这个办法收掉 |
| `DOC-13` | 低 | 沿用来的示例换成本项目的 | [C# 学习文档](../../reference/学习CSharp-Java程序员向.md)的示例里引的是别处的类名。语法说明本身没问题，所以这条只是可读性 —— 但它会让人以为那些类在本仓里找得到。代码仓已经有真实代码可引，换掉即可。同类的文案侧遗留在 `NR-5` |
| `DOC-12` | 中 | 设计文件抄的数字都要能判 | **已完成。** `simulate_week.py` 现在判两层：`--check-doc` 核参数路径存在 ＋ **路径旁抄的数字与参数表一致**（值是列表或嵌套结构的不判，脚本把不判的列出来）；主流程另有一张登记表，把正文抄的**算出来的量**（战力差、练满与还债天数、回蓝效率、背包占用……）与本轮算的逐处比一遍。**抓不到也算失败** —— 否则把句子改个说法就能静默关掉一条判定。选「加守卫」而不是「从正文删数字」，是因为摘要那句要靠那个倍数才立得住。`--set` 现在在 `--check-doc` 路径上也生效，撞失败路径用它 |
| `DOC-10` | 中 | 四份系统文档按八节骨架收编，并同批打开骨架判定 | **已完成。** [ADR-0010](../../decisions/ADR-0010-文档体裁与系统文档归位.md) 的剩余落地：[`数值模型`](../../design/数值模型.md)、[`角色动作状态系统`](../../design/角色动作状态系统.md)、[`状态效果系统`](../../design/状态效果系统.md)、[`教育系统`](../../design/教育系统.md) 四份删掉一次性的四节（背景与动机、当前事实与证据、兼容性、实现与过渡），仍然成立的内容并进「结构」「接口」「验收」，分步落地搬进 `GP-18`／`GP-21` 的备注；`数值模型` 补齐了缺的五节；`角色动作状态机` 按命名规则改名 `角色动作状态系统`；`数值模型` 不是一个系统，改成自己的体裁 `type: model`（后缀「模型」同样可机检，不靠白名单开例外）。骨架判定同批打开，自证 6 个用例（少一节、顺序错、多一节、旧骨架都报，八节与八节加附录不报）。**搬运那一步没有守卫**，是逐份对照做的 |
| `DOC-11` | 中 | 交叉引用过期的守卫 | **已完成。** 引用纪律在 [WORKFLOW §3](../../WORKFLOW.md) 第 5 条，执行体在 `tools/check_docs.py`：① 节名引用必须指得到目标文件里真有的标题或加粗小标题（实测 99 处里 8 处已失效）；② 不许用「第 N 步」引用别处的编号清单（插一步就全体平移）；③ 反引号里的代码路径两个仓都找不到就报错（实测 45 处里 4 处指向随 `ADR-0009` 删掉的工具，其中两处写在正典与 README 里）；④ 自计数必须与紧随的清单一样长（实测 2 处不符）。**量级事实的散文副本判不了** —— 机器分不出两个数字说的是不是同一件事，理由与对策写在 WORKFLOW §3 那两条边界里 |
| `DOC-2` | 低 | 界面色彩定稿并推广到其余界面 | **尺寸那一半已交付**：栅格 8、内边距 4、间距 4／8、安全边距 8、图标 16／32、侧视视野 320×180，实现在代码仓 `rules/Ui/UiMetrics.cs`。HUD 有一套标为占位的色板（`rules/Ui/HudPalette.cs`），放置定为四角贴边加技能位横排。**剩下的是把色板定稿** —— 这是作者在编辑器里看着定的事，不需要机器判据。一条硬约束仍在：色值**不带 alpha**（像素只许全透明或全不透明），冷却遮罩用不透明色遮而不是压暗 |
| `DOC-6` | 低 | 推演脚本的退役判断 | 依赖 `GP-2` 公式在规则层落地。`tools/simulate_week.py` 持有七天推演，公式因此将有两处（它与将来的 C# 实现），有漂移风险。届时判断：退役脚本，还是改成读同一份参数表对实现做交叉验证。**判断依据是那时才有的**（实现是否真读同一份参数），现在只记账不预判。它不属于交还编辑器的那批 —— 平衡账实机看不出来，与字体许可证同类 |
| `DOC-1` | 中 | 复核[踩坑记录](../../reference/踩坑记录.md)前 25 条并补 C# 13／14 特性 | **已完成。** 踩坑记录已深复核（沿用的类名换成通用描述）；C# 学习文档补入 §19（C# 13）与 §20（C# 14）；`CONVENTIONS.md` 已建立 |
| `DOC-7` | 低 | 日志文件同秒覆盖 | **已完成。** 同一秒内跑两次会让第二份日志覆盖第一份，所以 `log_path()` 同名时加 `-2`、`-3` 序号。现在设计仓 `simulate_week.py` 与代码仓 `loglib.py` 都是这个写法 |
| `DOC-8` | 中 | 给「管道读中文输出」加执行体 | **守卫已删，结论已内化。** 中文输出走管道会按非 UTF-8 编码解码、打第一个中文就崩，所以工具一律自己 `reconfigure(encoding="utf-8")` 并自己写日志文件，不靠 shell 重定向攒日志。现在这条只有纪律、没有守卫，见[踩坑记录 27](../../reference/踩坑记录.md) |
| `DOC-9` | 低 | 「`GP-2` 尚未设计」的过期说法 | **已完成。** 全库 12 处（代码仓 6、设计仓 5、`.kiro` 2），改法是写链接指台账、不复述状态。顺带修掉 `.kiro/skills/prd` 里更严重的一句「代码仓是空仓，没有 Godot 工程」。**它自己就是反漂移规则的来源**：立账时只盘了代码仓，因为记账的人只搜了他当时在改的那个仓 —— 根治办法写进 [ADR-0009](../../decisions/ADR-0009-编辑器主导的开发模式.md) 与 [WORKFLOW §3](../../WORKFLOW.md) |

## 已归档编号

已完成并归档的编号登记在这里，**它们不是待办**。

留登记而不删干净，是因为「不重编号、不复用已删除的号」需要一个能查的清单才落得住。另有一条硬约束：已接受的 ADR 不得用今天的理由改写当时的背景（[WORKFLOW §4](../../WORKFLOW.md)），所以 ADR-0006 里对 `ENG-2` 的引用会永久存在，那个编号必须始终解析得出来。

结论去处见[变更日志](../../archive/history/变更日志归档.md)（**历史背景·非依据**）。

| id | 归档日期 | 现行事实落在哪 |
| --- | --- | --- |
| `GP-1` | 2026-08-29 | [canon/gameplay/](../../canon/gameplay/玩法定位.md) 四份正典 |
| `ENG-9` | 2026-08-30 | [WORKFLOW §6](../../WORKFLOW.md) 执行体表；[踩坑记录 28](../../reference/踩坑记录.md)；守卫本体是 `tools/check_docs.py` 的 `check_line_endings` |
| `ENG-2` | 2026-08-30 | [ADR-0007](../../decisions/ADR-0007-测试底座选型.md)；[ADR-0005](../../decisions/ADR-0005-技术基线.md)；代码仓 `README.md` 与 `ARCHITECTURE.md` |
| `ENG-1` | 2026-08-30 | [ADR-0005](../../decisions/ADR-0005-技术基线.md) 三半全部通过，`net9.0` 回退不再需要；[踩坑记录 32、33](../../reference/踩坑记录.md)；导出办法见代码仓 `README.md` |
| `ENG-3` | 2026-08-30 | [WORKFLOW §6](../../WORKFLOW.md) 执行体表；[踩坑记录 29、33、35、36](../../reference/踩坑记录.md)；入口是 `tools/verify.py`，用法见代码仓 `README.md`「怎么验收」 |
| `GP-2` | 2026-08-30 | [数值模型](../../design/数值模型.md) 的公式与平衡判据（**永久留 `design/`，不上升进正典**）；值在 `design/numeric-model-params.json`；实测值跑 `python tools/simulate_week.py` |
| `ART-2` | 2026-08-30 | [ADR-0008](../../decisions/ADR-0008-中文像素字体选型.md)（选型、授权、渲染配置、子集化）；[玩法定位 · 像素基准](../../canon/gameplay/玩法定位.md) 的字号与整数缩放两句。授权核验跑 `python tools/audit_fonts.py`，看字体跑 `python tools/font_preview.py`。遗留在 `ART-3` |
| `UI-1` | 2026-09-01 | [`archive/spec/UI-1-ui-framework/`](../../archive/spec/UI-1-ui-framework/)（**历史背景·非依据**）；逻辑分辨率 640×360 在 `project.godot`；规则层在代码仓 `rules/Ui/`；色板待 `DOC-2` 定稿 |
| `UI-3` | 2026-09-01 | 同 `UI-1`。`project.godot` 四项缩放配置 |
| `UI-4` | 2026-09-01 | 同 `UI-1`。结论在 [ADR-0008](../../decisions/ADR-0008-中文像素字体选型.md)「承重项是最近邻纹理过滤」 |
| `UI-5` | 2026-09-01 | 同 `UI-1`。实现在代码仓 `rules/Ui/CameraRig.cs`、`src/World/GameCamera.cs` |
| `UI-6` | 2026-09-01 | 同 `UI-1`。层级定义在 `rules/Ui/UiLayer.cs`；排版单位在 `rules/Ui/UiMetrics.cs` |
| `UI-7` | 2026-09-01 | 同 `UI-1`。实现在代码仓 `rules/Ui/InputBindings.cs` 等五文件 |
| `UI-8` | 2026-09-01 | 同 `UI-1`。HUD 实现 `rules/Ui/HudLayout.cs`、`src/UI/LevelHud.cs`；占位色板 `rules/Ui/HudPalette.cs` 待 `DOC-2` 定稿 |
| `UI-9` | 2026-09-01 | 同 `UI-1`。实现在 `rules/Ui/WorldUiLayout.cs`、`src/UI/WorldSpaceUi.cs` |
| `UI-10` | 2026-09-01 | 同 `UI-1`。端到端测试 `tests/Ui/E2ETests.cs` |
| `UI-11` | 2026-09-21 | 裁定作废后实现已删（面板遮挡层与 `src/UI/InputRouter.cs` 里对它的接线、对应测试）。现行事实是[玩法定位](../../canon/gameplay/玩法定位.md)那条**弹占屏界面一律暂停** —— 世界停着，手柄上「跳跃／闪避与 UI 确认共用一个键」的歧义不存在。留号是因为 `UI-1` 那批归档件引用得到它。归档件 [`issue-UI-11`](../../archive/spec/UI-1-ui-framework/issue-UI-11-panel-input-block.md)（**历史背景·非依据**） |
| `ART-3` | 2026-09-01 | 同 `UI-1`。判据在 `tools/verify.py` 的导出步：包里有字体数据就必须有 `LICENSE-OFL.txt` |
| `ART-4` | 2026-09-01 | 同 `UI-1`。**占位素材生成器与登记表已随 `ADR-0009` 删除**，缺图由作者在 Godot 里看出来 |
| `ENG-10` | 2026-09-01 | 同 `UI-1`。**假像素守卫已随 `ADR-0009` 删除**；像素规范 §9 转为人工标准 |
| `ENG-12` | 2026-09-01 | 同 `UI-1`。发行素材判据仍在，改成按目录黑名单判（`assets/downloaded/`、`assets/placeholder/` 下的不许进包），在 `tools/verify.py` 的 `audit_release_assets`。保留理由是授权与法律风险实机看不出来 |
| `ENG-13` | 2026-09-01 | 同 `UI-1`。**texture_filter 守卫已随 `ADR-0009` 删除**；不覆盖 `TextureFilter` 转为 `CONVENTIONS.md` 的约定，覆盖了像素会糊、实机看得出来 |
| `ENG-14` | 2026-09-11 | 输入专项探针互不污染。**结论与载体已随 `ADR-0009` 一并作废** —— 图形探针全部删除，「探针跑起来后不许再执行前台命令」没有适用对象了。留号只为让 `GP-12`／`GP-13`／`GP-15`／`GP-17` 的历史引用解析得出来 |
| `GP-9` | 2026-09-01 | 闪避取方向读「按下瞬间的输入状态」（否掉「短窗口最后有效方向」）。现行事实在[战斗与关卡 · 按键与连击](../../canon/gameplay/战斗与关卡.md)；实现是闪避状态机直取 `InputRouter` 当前轮询状态，不加缓冲窗口 |
| `GP-20` | 2026-09-13 | 命中当帧的爆点：打击火花、极短白闪、占位音效、震屏轻重两档，顿帧未动。现行事实在代码仓 `rules/Combat/CombatFeel.cs`、`rules/Ui/CameraFeel.cs`、`src/World/`（`HitSpark`／`CombatAudio`／`PlayerActor`／`TrainingRoom`）。数值归 `GP-6`、正式音效归 `ART-1`。归档件 [`GP-20-hit-feedback-juice/`](../../archive/spec/GP-20-hit-feedback-juice/issue.md) |
| `ENG-17` | 2026-09-13 | 代码仓 `CONVENTIONS.md` 新增三节（局部变量与 `var`、三元与多路分派、构造与 `new`），并定点重构了 `PlayerActor.UpdateVisual` 等处。归档件 [`ENG-17-readability-conventions/`](../../archive/spec/ENG-17-readability-conventions/issue.md) |
| `A1` | 2026-09-18 | 战斗手感核心（进攻侧竖切片 + 帧调优工具）。归档件 [`A1-combat-feel-core/`](../../archive/spec/A1-combat-feel-core/prd.md)（**历史背景·非依据**）。玩法结论在 [canon/gameplay/](../../canon/gameplay/战斗与关卡.md)，实现在代码仓 `rules/Combat/`、`src/World/`；未完部分是本页的 `GP-6`、`GP-14`、`ENG-16`、`ENG-20`、`ART-6` |
| `GP-25` | 2026-09-22 | [玩法定位 · 场景规模](../../canon/gameplay/玩法定位.md)（可建造区 80×60 格、建筑室内一类场景）与[时间与经营 · 建造](../../canon/gameplay/时间与经营.md)（尺寸与八类建筑按名列）。归档件 [`GP-25-base-and-production/`](../../archive/spec/GP-25-base-and-production/prd.md)（**历史背景·非依据**） |
| `GP-26` | 2026-09-22 | [时间与经营 · 学生与派工](../../canon/gameplay/时间与经营.md)（新工位、设备添料不是派工）与[角色与成长 · 战利品归属](../../canon/gameplay/角色与成长.md)（产出归主角、按基础价比例付酬、付酬不改关系、索取加件数额度）。归档件同 `GP-25` |
| `GP-27` | 2026-09-22 | [角色与成长 · 技能分两类：放出来的动作，与做事的熟练度](../../canon/gameplay/角色与成长.md)（技能分战斗与生活两类、只在做成时涨、交易只影响点差）与[玩法定位 · 开局](../../canon/gameplay/玩法定位.md)（开局物资档与战斗难度各管一侧）。**生活技能那张清单后来被 `GP-39` 重排过**，项数看那一节、不看本行。归档件同 `GP-25` |
| `GP-29` | 2026-09-22 | [`生产系统`](../../design/生产系统.md) 的地块与开荒那一节（未清理 → 可耕 → 已播种 → 待收、清理物只产初级材料且只在未开垦区重生）。篇幅没超过其余渠道之和，所以没拆 `种植系统`。归档件同 `GP-25` |
| `GP-33` | 2026-09-23 | [角色与成长 · 四项属性的作用方向](../../canon/gameplay/角色与成长.md)：力量只给物攻、智力给魔攻并驱动屏障与治疗量、体质给 HP／体力条／韧性上限／精力上限与两份防御的基底、敏捷给攻速暴击与闪避冷却。「受击硬直抗性」已改名**韧性上限**。约束形式登记在[数值模型](../../design/数值模型.md)的尚未给值表。归档件 [`GP-30-growth-skill-damage/`](../../archive/spec/GP-30-growth-skill-damage/prd.md)（**历史背景·非依据**） |
| `GP-34` | 2026-09-23 | [战斗与关卡 · 防御与失衡](../../canon/gameplay/战斗与关卡.md)（朝向＝角色朝向、按下即定身、普通与精准是同一次按下的两段、连点不刷精准窗口）与[战斗与关卡 · 按键与连击](../../canon/gameplay/战斗与关卡.md)（闪避冷却有下限、技能位只装主动、具体键位映射已交回界面侧）。窗口帧数与连点间隔归 `GP-6`。归档件同 `GP-33` |
| `GP-35` | 2026-09-23 | [数值模型](../../design/数值模型.md)：参数键分成 `patk`／`matk`／`pdef`／`mdef` 四套（软化分母两份防御共用），减伤公式补上穿透与暴击且与[`伤害系统`](../../design/伤害系统.md)的结算顺序逐字对齐。魔法那两份是**未校准的起步值**，只有量级自检 C6 在判，配对判据登记在尚未给值表。归档件同 `GP-33` |
| `DOC-18` | 2026-09-23 | 三个名字的分工在[角色与成长 · 四项属性的作用方向](../../canon/gameplay/角色与成长.md)（体质是属性、体力条是战斗资源、精力条是当日预算），参数键 `vigor.*` 在[数值模型](../../design/数值模型.md)，HUD 标签在代码仓 `data/text/zh-CN.json`。`HudGaugeKind.Stamina` 与 `derived.sp_*` 是别名，刻意没动。归档件同 `GP-33` |
| `DOC-19` | 2026-09-23 | 三份文档的条目在[系统文档与提案索引](../../design/README.md)，正典反向链接在[角色与成长](../../canon/gameplay/角色与成长.md)与[战斗与关卡](../../canon/gameplay/战斗与关卡.md)，战斗中用道具那一套在[`背包系统`](../../design/背包系统.md)。归档件同 `GP-33` |
| `DOC-20` | 2026-09-23 | 核对结论已经落在被核对的那几份文档里，没有单独的落点。查出并修掉的是一处正典自相矛盾（技能位一边写「主动与被动都占位」、一边写「被动不设上限」）。归档件同 `GP-33` |
| `NR-6` | 2026-09-23 | [角色与成长 · 关系只有一个量，档名说出它走到哪一步](../../canon/gameplay/角色与成长.md)：量就叫关系、一条五档阶梯（陌生→了解→熟悉→认可→信赖）、十颗爱心每档两颗且连续填充、恋爱时最高档显示托付、爱心是刻度而档位是接口。结构与读口在[`人际系统`](../../design/人际系统.md)。归档件 [`NR-6-relationship-system/`](../../archive/spec/NR-6-relationship-system/prd.md)（**历史背景·非依据**） |
| `GP-37` | 2026-09-23 | [战斗与关卡 · 队友 AI 分档：学生「变强」必须看得见](../../canon/gameplay/战斗与关卡.md)：五档改成生手→熟练→老练→精通→大师，判据是**做不做是地板、什么时候做与跟谁一起做是分档**，基础自保行为不占档。**每档解锁哪几条行为与那两个档号的结构现在在[`战斗AI系统`](../../design/战斗AI系统.md)**（两条轴不合成，各成一个档号）。归档件同 `NR-6` |
| `DOC-22` | 2026-09-23 | 没有单独落点 —— 结论在被改过的那些文档自己身上：量名与档名在[角色与成长](../../canon/gameplay/角色与成长.md)，「断粮只降精力上限、不降关系」在[玩法定位 · 开局](../../canon/gameplay/玩法定位.md)，档位当气泡上下文键在[玩法定位 · 对话：气泡与正式对话是两套东西](../../canon/gameplay/玩法定位.md)。归档件同 `NR-6` |
| `DOC-23` | 2026-09-23 | [数值模型](../../design/数值模型.md)的尚未给值表（档位边界、各条涨法的增量、每日涨幅上限，校准方向偏慢）与[玩法定位 · 内容规模基准](../../canon/gameplay/玩法定位.md)的关系档位与小事件两行。**十颗爱心与五档不在那张表** —— 接口常量，家在正典。归档件同 `NR-6` |
| `DOC-24` | 2026-09-23 | [系统文档与提案索引](../../design/README.md)里 `人际系统` 那一行，加本页各行。留下一条判据：**条目文件还在 `spec/issues/` 就必须被本页链着**，归档之后才改成纯文本。归档件同 `NR-6` |
| `DOC-25` | 2026-09-23 | 核对结论落在被核对的那几份文档里（[`数值模型`](../../design/数值模型.md)、[`成长系统`](../../design/成长系统.md)、[`人际系统`](../../design/人际系统.md)），没有单独落点。顺带留下一条预警：生活技能熟练度档最顺口的名字正是技艺那一套，命名时得另起。归档件同 `NR-6` |
| `GP-40` | 2026-09-23 | [时间与经营 · 结算顺序（固定，不得改动）](../../canon/gameplay/时间与经营.md)（委托超期作废与委托板补满各一步，先作废后补满）与[战斗与关卡 · 委托只是任务的一类](../../canon/gameplay/战斗与关卡.md)（任务与事件的判据、主线不占额度也无截止、一次出征只结一条、个人请求无失败、「采集」是总称不是技能名）。截止从上板那天算起写在[战斗与关卡 · 委托与关卡](../../canon/gameplay/战斗与关卡.md)。归档件 [`GP-40-quest-and-level/`](../../archive/spec/GP-40-quest-and-level/prd.md)（**历史背景·非依据**） |
| `DOC-26` | 2026-09-23 | [数值模型](../../design/数值模型.md)的尚未给值表（拼装与结算那一组，**片段的结构类数与职责类数不在表里** —— 接口常量，家在[`关卡系统`](../../design/关卡系统.md)），转场读条并进已有的 `GP-6` 那一行而不新开一行。[内容规模基准](../../canon/gameplay/玩法定位.md)补了关卡片段与资源点种类两行。归档件同 `GP-40` |
| `DOC-27` | 2026-09-23 | [系统文档与提案索引](../../design/README.md)里任务系统与关卡系统两行，加本页各行。留下一条判据：**条目文件还在 `spec/issues/` 就必须被本页链着**，归档之后才改成纯文本。归档件同 `GP-40` |
| `DOC-28` | 2026-09-23 | 核对结论落在被核对的那几份文档里（[`任务系统`](../../design/任务系统.md)、[内容规模基准](../../canon/gameplay/玩法定位.md)），没有单独落点。查出并修掉的是两处量级事实副本与基准表内读得出的一处自相矛盾。留下一条判据：**纯文档批次里跑不了的反证记「不适用（已记账）」，不记「通过」**。归档件同 `GP-40` |
| `GP-39` | 2026-09-23 | [角色与成长 · 技能分两类：放出来的动作，与做事的熟练度](../../canon/gameplay/角色与成长.md)那张表：**按作业对象分项，项数只在那里写一次**，「采集」是总称不是技能名。连带落点是[`生产系统`](../../design/生产系统.md)开荒那一格（木头读伐木、石头读挖矿、杂草不读技能）与[`成长系统`](../../design/成长系统.md)。归档件 [`GP-43-economy-and-building/`](../../archive/spec/GP-43-economy-and-building/prd.md)（**历史背景·非依据**） |
| `DOC-29` | 2026-09-23 | [数值模型](../../design/数值模型.md)的尚未给值表（定价与债务一组、建造一组、生活技能档位阈值与专长幅度一组）与[内容规模基准](../../canon/gameplay/玩法定位.md)（生活技能两行改成指链接、专长起步量一行）。**层数、货币级数、建筑类数、档数与节点数不在那张表里** —— 接口常量，家在各自那份文档。归档件同 `GP-39` |
| `DOC-30` | 2026-09-23 | [系统文档与提案索引](../../design/README.md)里经济系统与建造系统两行，加本页各行。留下一条判据：**只搜自己在改的那个仓会漏一半**，按编号搜要把两个仓加 `.kiro` 一起搜。归档件同 `GP-39` |
| `DOC-31` | 2026-09-23 | 核对结论落在被核对的那几份文档里（[`经济系统`](../../design/经济系统.md)、[`建造系统`](../../design/建造系统.md)、[`背包系统`](../../design/背包系统.md)），没有单独落点。查出的真矛盾是「订货延迟一天送到」没说货落在哪，而仓库只有一个自动写入点 —— 由此留下一条判据：**交叉核对必须包含「新加的动作有没有踩别人写死的不变式」**，只按「同一件事在几处出现」核会漏掉它。归档件同 `GP-39` |
| `GP-46` | 2026-09-24 | [ADR-0011](../../decisions/ADR-0011-纯增益装备的窄例外.md)：纯增益只对宝物开、只从变异品质与剧情给予出、且带唯一性标记不可叠装；正典那一句已按它改写（[角色与成长 · 装备：宝物与宝石](../../canon/gameplay/角色与成长.md)），载入校验的形式在[`装备系统`](../../design/装备系统.md)。落地反证跟着 `GP-24` 走。归档件 [`GP-46-equipment/`](../../archive/spec/GP-46-equipment/prd.md)（**历史背景·非依据**） |
| `GP-47` | 2026-09-24 | [ADR-0012](../../decisions/ADR-0012-宝石的两个镶嵌目标.md)：宝石定义声明属性侧与机制侧，属性侧进宝物孔、机制侧进技能孔，缺哪侧就进不了对应的孔；「宝石孔」由此分成**宝物孔**与**技能孔**两个名字。正典宝石那一条、[`技能系统`](../../design/技能系统.md)的结构与理由两节、[`成长系统`](../../design/成长系统.md)派生表那一栏都已跟着改。落地反证跟着 `GP-24` 与 `GP-31` 走。归档件同 `GP-46` |
| `GP-48` | 2026-09-24 | [角色与成长 · 装备：宝物与宝石](../../canon/gameplay/角色与成长.md)（补上强化、写清品质与副词条是两件事、专属栏改成每个角色各一个且格数按角色定义声明）与[玩法定位 · 系统清单](../../canon/gameplay/玩法定位.md)装备那几行。顺带统一了一个名字：**「饰品」是「宝物」的旧说法，活文档里已全部改掉**。归档件同 `GP-46` |
| `DOC-32` | 2026-09-24 | [数值模型](../../design/数值模型.md)的尚未给值表（装备那一组：词条取值区间、强化每级幅度与代价、两种孔的分配、宝石两侧的量，并写明「满配」指什么；**接口常量不在表里** —— 家在正典与[`装备系统`](../../design/装备系统.md)）与[内容规模基准](../../canon/gameplay/玩法定位.md)（宝物那一行复看，另加宝石与副词条候选池两行）。归档件同 `GP-46` |
| `DOC-33` | 2026-09-24 | [系统文档与提案索引](../../design/README.md)里装备系统那一行、[ADR 索引](../../decisions/README.md)那两行，加本页各行。归档件同 `GP-46` |
| `DOC-34` | 2026-09-24 | 核对结论落在被核对的那几份文档里（[`装备系统`](../../design/装备系统.md)、[`技能系统`](../../design/技能系统.md)、[`成长系统`](../../design/成长系统.md)），没有单独落点。留下两条判据：**交叉核对拆成按词与按量级数字两轮**（后一轮查出三处数字副本），以及**纯文档批次里跑不了的反证记「不适用（已记账）」**。归档件同 `GP-46` |
| `GP-49` | 2026-09-24 | [ADR-0013](../../decisions/ADR-0013-战斗片段就是一屏.md)：战斗片段就是恰好一屏、这一屏清干净才能往前、战斗片段内相机不平移。连带改动在[`关卡系统`](../../design/关卡系统.md)转场那一节（往前多一道门，往回与撤离不锁）与[战斗与关卡 · 完成标准](../../canon/gameplay/战斗与关卡.md)那条的**理由**。落地反证跟着 `GP-42` 与 `GP-36` 走。归档件 [`GP-49-enemy-ai-and-ally-tiers/`](../../archive/spec/GP-49-enemy-ai-and-ally-tiers/prd.md)（**历史背景·非依据**） |
| `GP-50` | 2026-09-24 | [战斗与关卡](../../canon/gameplay/战斗与关卡.md)那几节：两条轴**各成一个档号、不合成**、地板收进三条（收指令、扶倒地同伴、不挤在同一处）、精准防御的回报按角色一律相同且可被天赋与状态提升、敌人侧四样收口（不走那五档、分级面板是子集、动作集按角色声明、载体类别只决定死亡留下什么），加[玩法定位 · 系统清单](../../canon/gameplay/玩法定位.md)三行。顺带修掉正典一处自相矛盾：「响应指令延迟更短」正是同一节开头排掉的「提高反应速度」。归档件同 `GP-49` |
| `DOC-36` | 2026-09-24 | 落在被改的那几份文档自己身上：[`角色动作状态系统`](../../design/角色动作状态系统.md)（动作集按角色声明、流转表是全集、两条连通性载入校验、死亡终态按载体类别收窄）、[`成长系统`](../../design/成长系统.md)派生表精准防御回报那一行、[`关卡系统`](../../design/关卡系统.md)转场那一道门、正典完成标准那条的理由。归档件同 `GP-49` |
| `DOC-37` | 2026-09-24 | [数值模型](../../design/数值模型.md)的尚未给值表（战斗 AI 与感知一组、精准防御回报一组；**接口常量不在表里**）与[内容规模基准](../../canon/gameplay/玩法定位.md)敌人与队友 AI 档位两行。顺带修掉那张表表头一句承诺 —— 它写着改那些数会连带重跑推演，而推演脚本一处都不读它。归档件同 `GP-49` |
| `DOC-38` | 2026-09-24 | [系统文档与提案索引](../../design/README.md)里战斗 AI 与感知视野两行、[ADR 索引](../../decisions/README.md)那一行，加本页各行。归档件同 `GP-49` |
| `DOC-39` | 2026-09-24 | 核对结论落在被核对的那几份文档里（[`战斗AI系统`](../../design/战斗AI系统.md)、[`感知与视野系统`](../../design/感知与视野系统.md)、[战斗与关卡](../../canon/gameplay/战斗与关卡.md)、[玩法定位](../../canon/gameplay/玩法定位.md)），没有单独落点。留下一条判据：**交叉核对分按词、按量级数字、按不变式三轮，一轮找不全**；不变式那一轮的证据是 `git diff --stat` 为空 |
| `GP-53` | 2026-09-24 | [玩法定位 · 随机事件：事件可以晚做，扩展点现在就留](../../canon/gameplay/玩法定位.md)（钩子只被询问、每日结算那个钩子在固定步序之外**且理由与失衡那条不同源**）、[玩法定位 · 剧情演出：一律引擎内，不做视频](../../canon/gameplay/玩法定位.md)（演出期间不由玩家操作的角色也停着）与[玩法定位 · 跨系统基础设施](../../canon/gameplay/玩法定位.md)持续引导那一行。归档件 [`NR-7-cutscene-dialogue-and-events/`](../../archive/spec/NR-7-cutscene-dialogue-and-events/prd.md)（**历史背景·非依据**） |
| `DOC-40` | 2026-09-24 | 落在被改的那几份文档自己身上：[`人际系统`](../../design/人际系统.md)与[`任务系统`](../../design/任务系统.md)那两句「不做」改成指落点、[`关卡系统`](../../design/关卡系统.md)那条种子承诺盖住关卡事件、[`战斗AI系统`](../../design/战斗AI系统.md)补「演出期间决策循环不跑」、[`角色动作状态系统`](../../design/角色动作状态系统.md)那条载入校验盖住演出脚本、[文案写作规范](../../production/文案写作规范.md)还掉 `NR-5` 那件文案遗留。归档件同 `GP-53` |
| `DOC-41` | 2026-09-24 | [数值模型](../../design/数值模型.md)的尚未给值表（事件与引导一组；**按档位的偏移幅度并进活跃失衡那一行、不另开**）与[内容规模基准](../../canon/gameplay/玩法定位.md)（小事件那一行复看，另补随机事件与剧情演出脚本两行）。**接口常量不在那张表里** —— 家在各自那份文档。归档件同 `GP-53` |
| `DOC-42` | 2026-09-24 | [系统文档与提案索引](../../design/README.md)里剧情演出与事件两行、[ADR 索引](../../decisions/README.md)那一行，加本页各行。留下一条判据：**条目文件还在 `spec/issues/` 就必须被本页链着**，归档之后才改成纯文本。归档件同 `GP-53` |
| `DOC-43` | 2026-09-24 | 核对结论落在被核对的那几份文档里（[`剧情演出系统`](../../design/剧情演出系统.md)、[`事件系统`](../../design/事件系统.md)、[玩法定位](../../canon/gameplay/玩法定位.md)），没有单独落点。留下一条判据：**自己写的命名禁令要自己遵守** —— 「过场」那条禁令实测被同一份文档的正文与正典各违反了一次，一条自己都不遵守的禁令比没有禁令更坏。归档件同 `GP-53` |
| `NR-10` | 2026-09-24 | [角色与成长 · 日记](../../canon/gameplay/角色与成长.md)（每个角色一本、条目按类别分、**记与读是两件事**）与[人物 · 主角设定](../../canon/characters/人物.md)（性格乐观积极开朗、与笑脸面具同源）。[玩法定位](../../canon/gameplay/玩法定位.md)气泡那一节、跨系统基础设施与系统清单都已挂链接。归档件 [`NR-8-bubble-dialogue-and-diary/`](../../archive/spec/NR-8-bubble-dialogue-and-diary/prd.md)（**历史背景·非依据**） |
| `DOC-44` | 2026-09-24 | 落在被改的那几份文档自己身上：[`事件系统`](../../design/事件系统.md)（「还没有落点」改成指落点、回看那一句按日记的新形状改写、小事件前置那一格改读需求标识）、[`剧情演出系统`](../../design/剧情演出系统.md)同样改成指落点、[`人际系统`](../../design/人际系统.md)那句「不做日记」改成指落点且读者表里解锁那一行并进日记、[`活跃失衡系统`](../../design/活跃失衡系统.md)气泡那一格挂上链接。归档件同 `NR-10` |
| `DOC-45` | 2026-09-24 | [数值模型](../../design/数值模型.md)的尚未给值表（气泡一组、日记一组）与[内容规模基准](../../canon/gameplay/玩法定位.md)（气泡语料补一行，纪事类条目按派生量不补）。**接口常量不在那张表里** —— 各写明归属。留下一条判据：**内容规模基准那一行不许复述系统文档里的类别名**，写描述加节名链接才判得到过期。归档件同 `NR-10` |
| `DOC-46` | 2026-09-24 | [系统文档与提案索引](../../design/README.md)里气泡对话与日记两行，加本页各行与 `UI-19` 那条记账行。本批**不新建 ADR**，判据与被排掉的那几个候选写在归档件里。归档件同 `NR-10` |
| `DOC-47` | 2026-09-24 | 核对结论落在被核对的那几份文档里（[`气泡对话系统`](../../design/气泡对话系统.md)、[`日记系统`](../../design/日记系统.md)、[玩法定位](../../canon/gameplay/玩法定位.md)），没有单独落点。留下一条判据：**按词那一轮必须分区** —— 只有活文档里出现过期说法才算失败，`spec/` 下的过程件在描述「从什么改成什么」，出现旧说法是应该的。归档件同 `NR-10` |
| `DOC-48` | 2026-09-24 | 落在被挂上链接的那几处正典自己身上（[玩法定位](../../canon/gameplay/玩法定位.md)、[时间与经营](../../canon/gameplay/时间与经营.md)），没有单独落点。**[结算顺序（固定，不得改动）](../../canon/gameplay/时间与经营.md)那串一个字未改，也没在它上面加链接** —— 动它的形状会让人以为它可以被讨论。归档件 [`GP-54-save-settlement-and-stats-bus/`](../../archive/spec/GP-54-save-settlement-and-stats-bus/prd.md)（**历史背景·非依据**） |
| `DOC-49` | 2026-09-24 | 落在被改的那几份文档自己身上：那几处「不设计存档序列化格式」改成指落点、各份「下游同步」里的存档那一格改成可点、[`日记系统`](../../design/日记系统.md)那句「序列化形状不在本页」对上了答案、[`角色动作状态系统`](../../design/角色动作状态系统.md)那句「经营动作细节归各自实现」按结果改写。归档件同 `DOC-48` |
| `DOC-50` | 2026-09-24 | [数值模型](../../design/数值模型.md)的尚未给值表（本批只加了俯视移动速度一项，**并进 `GP-6` 那一行**）与[内容规模基准](../../canon/gameplay/玩法定位.md)（逐样重判后都不补行，理由各不相同）。**接口常量不在那张表里** —— 存档位与续玩点份数、版本号、结算步数各写明了归属。归档件同 `DOC-48` |
| `DOC-51` | 2026-09-24 | [系统文档与提案索引](../../design/README.md)里那几行与改写过的动作机那一行、[ADR 索引](../../decisions/README.md)那一行，加本页各行与 `UI-20` 那条记账行。`ENG-7` 与经营侧动作机**都不新开编号** —— 同一件事不新开登记行。归档件同 `DOC-48` |
| `DOC-52` | 2026-09-24 | 核对结论落在被核对的那几份文档里（[`存档系统`](../../design/存档系统.md)、[`统计事件总线系统`](../../design/统计事件总线系统.md)、[`每日结算系统`](../../design/每日结算系统.md)），没有单独落点。留下一条判据：**「不加一步」这类结论各写自己的理由、不列名单** —— 名单会静默过期，要查按词搜。归档件同 `DOC-48` |
