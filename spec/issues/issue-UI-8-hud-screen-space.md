---
type: workdoc
status: draft
owner: project
last_verified: 2026-08-30
---

# UI-8：关卡 HUD 的屏幕空间部分

## 目标

侧视关卡里玩家能一眼看清自己的资源、技能位、目标进度与队友状态，且这些东西不压掉屏幕中间那块该看角色的区域。

## 来源

- PRD：[`spec/prd-ui-framework.md`](../prd-ui-framework.md) 的 `US-007`（屏幕空间那一半；世界空间部分在 `UI-9`）
- 取 HUD 当样板界面由作者 2026-08-30 定。理由：它落在全游戏最挤的一块地方 —— 640×360 在侧视 2 倍缩放下有效视野只有 320×180。

## 依赖

`UI-6`（层级与排版单位）、`UI-7`（修饰键组合的当前组数据）、`ART-4`（占位素材）。

## 第一步：验收脚手架（2026-08-31 已落地）

HUD 要加在一个**有会动的世界**的场景上，而本条自己的验收标准就写着「看 12px 文字**在动态下**
读不读得清」—— 没有那个场景，这条签不掉。同一个场景也是 `UI-5`（相机取景与手感）、`UI-7`
（知道自己按的是哪个技能）、`ART-4`（占位件在 640×360 下看得出布局意图）三条实机确认的落点，
所以它先做，作为本条的第一步而不是另立编号（作者 2026-08-31 定）。

已落成 `src/World/CameraHarness.cs`：可拼接地面、5 格／20 格的格线（横向移动没有它看不出来，
是存图看出来的）、画出来的地图边界与可建造区边界与推镜触发带、15 个 32×32 剪影（数量按本条
「同屏 10–15 个敌人时 HUD 不遮挡」那条取）、一个走 `InputRouter` 移动的角色（侧视用侍武士精灵
表、俯视用占位剪影），加六个调试键（`F1` 视角／`F2` 建造／`F3` 震一下／`F4` 震动开关／`F9` 演出／
`F10` 打印数值）。存图入口 `python tools/harness_shot.py` 两种视角各存一张，**排 HUD 时靠它复核**。

## 验收标准

- [x] 有一个能走能打的验收场景，两种视角都跑得起来且引擎无报错 —— 见上，`harness_shot.py` 存图复核
- [ ] 主角 HP／SP／MP 与体力常驻可见
- [ ] 6 个技能位可见并有冷却表现；手柄修饰键按下时显示当前那一组对应哪三个技能
- [ ] 目标进度常驻可见（还差几个素材、还剩几个来源点）；为 0 或已达成时仍显示而不隐藏（[战斗与关卡](../../canon/gameplay/战斗与关卡.md)要求始终可见）
- [ ] 队友状态可见，容纳编队上限 4 名学员；队友为 0（单人采集）时该区收起而不是留空槽
- [ ] 同屏 10–15 个敌人时 HUD 不遮挡角色所在的可读区
- [ ] 显示的数值全部由视图模型传入，代码里没有任何写死的数字（`GP-2` 的参数表不在本条落地）
- [ ] 没有绝对像素坐标，全部锚点与容器
- [ ] 作者实机确认：打开样板场景，看 12px 文字在动态下读不读得清、HUD 有没有压掉该看的东西

## 实现笔记

> 由 `/note-it` 填写。

### 设计决策

### 偏离

### 权衡

### 待确认

## 验证结果

> 由 `/verify-round` 填写。只写实际运行过的内容。

| 命令 | 结果 | 判定 |
| --- | --- | --- |
|  |  |  |
