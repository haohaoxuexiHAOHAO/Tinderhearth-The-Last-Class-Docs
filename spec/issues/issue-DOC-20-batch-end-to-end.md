---
type: workdoc
status: draft
owner: project
last_verified: 2026-09-22
---

# DOC-20：这一批的端到端与交叉核对

## 目标

一条命令看出这一批有没有把某处说法改漏，再人工核一遍机器判不了的那几处。

## 来源

- PRD：[`spec/prd-growth-skill-damage.md`](../prd-growth-skill-damage.md) 的 `US-008`
- 检查器判不了两份散文对同一件事的描述是否分叉（理由见 [WORKFLOW § 3. 反漂移：事实只许有一个家](../../WORKFLOW.md)的两条边界），所以交叉核对只能人工做。

## 依赖

本批全部条目（`DOC-18`、`GP-33`、`GP-34`、`GP-32`、`GP-31`、`GP-30`、`GP-35`、`DOC-19`）。

## 验收标准

- [ ] `python tools/check_docs.py` 0 项必须修复（含体裁归位、八节骨架、正典上游、节名引用、断链、编号、入口可达、行尾）。
- [ ] `python tools/simulate_week.py` 与 `--check-doc` 都绿，判据条数按脚本自报，本条不复述。
- [ ] 代码仓 `python tools/verify.py` 五步全绿（行尾 → 构建 → 测试 → 导出 → 跑产物）。本批动了 `data/text/zh-CN.json`，所以它进验收。
- [ ] **三个入口都要看脚本自己打印的摘要**，不只看退出码 —— 退出码 0 不等于成功，最常见的是跑在旧产物上。
- [ ] 人工交叉核对：**属性派生出什么**、**一次命中怎么算**、**防御与抗性从哪来**、**技能占不占槽位** —— 这几件事在正典、三份系统文档、参数表与台账里各出现一次且互相不矛盾。
- [ ] 人工核一遍改名有没有留尾：全库搜不到「受击硬直抗性」这个旧名；「体力」不再同时指属性与两条不同的资源；`stamina` 在参数表与代码里不再同时指两件事。
- [ ] 全库搜不到「火抗」这类把元素当伤害类型的说法。
- [ ] 作者实机确认：打开 HUD 看资源标签（体力条与精力条）读不读得清、两字标签在那个字宽下排得下。
- [ ] 工作区根的 `temp/` 为空（临时资源用完即删，检查器看不到它，只能在验收里报）。

## 实现笔记

> 由 `/note-it` 在实现和评审之后填写。

### 设计决策

- **模糊点**：
- **选择**：
- **理由**：

### 偏离

- **方案怎么说**：
- **实际怎么做**：
- **为什么**：

### 权衡

### 待确认

## 验证结果

> 由 `/verify-round` 填写。**只写实际运行过的内容。**

| 命令 | 结果 | 判定 |
| --- | --- | --- |
|  |  |  |
