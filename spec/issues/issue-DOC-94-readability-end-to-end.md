---
type: workdoc
status: draft
owner: project
last_verified: 2026-09-26
---

# DOC-94：端到端与交叉核对

## 目标

确认这件事真做完了，而不是每批各自绿过。

## 来源

PRD：[`spec/prd-doc-readability.md`](../prd-doc-readability.md) 的 `US-006`。

## 依赖

`DOC-89` 到 `DOC-93` 全部。

## 验收标准

- [ ] 可核对：`python tools/check_docs.py` 0 FAIL，且脚本自报的覆盖量里节名引用与长期规格份数都不为零 —— **退出码 0 不等于执行了**。
- [ ] 可核对：`design/` 下每份系统文档都有二级标题的术语节，没有一份停在占位说法上。
- [ ] 可核对：按词交叉核对一轮 —— 共用术语表里的每个词条，在各页术语节里都搜不到重复定义。
- [ ] 可核对：那个只列不判的入口跑一遍，报出的生词候选清单为空，或每条都有留下的理由。
- [ ] 可核对：本轮发现但超出范围的问题都已按编号记进[待办台账](./README.md)，没有一条「顺手修完就算了」。
- [ ] 可核对：`temp/` 为空，临时文件已删（撞失败路径用的那些）。
- [ ] 可核对：画图技能里那处过期说法已改掉（它写着本项目没有代码、`ENG-2` 未落地，而那个编号已归档、代码仓在跑）。
- [ ] **作者实读确认**：作者挑三份没参与过讨论的文档各读一遍，判定「不用问人就看懂了」。这是最后一道判定，也是唯一一道机器判不了的。

## 实现笔记

> 由 `/note-it` 在实现和评审之后填写。

### 设计决策

- **模糊点**：
- **选择**：
- **理由**：

### 偏离

- **方案怎么说**：
- **实际怎么做**：
- **为什么**：

### 权衡

### 待确认

## 验证结果

> 由 `/verify-round` 填写。**只写实际运行过的内容。**

| 命令 | 结果 | 判定 |
| --- | --- | --- |
|  |  |  |
