---
type: workdoc
status: archived
owner: project
last_verified: 2026-09-01
---

> **只读归档。** 此文件是 `UI-1` 于 2026-09-01 归档时的历史记录。现行事实见台账[已归档编号](../../spec/issues/README.md)对应行与代码仓实现文件。**不得修改此文件的正文内容。**

# UI-10：`UI-1` 的端到端验证

## 目标

一条走完整条链路的验证，确认这套界面骨架不是各自单测通过而合起来不成立。

## 来源

- PRD：[`spec/prd-ui-framework.md`](../prd-ui-framework.md) 的 `US-008`

## 依赖

`UI-3`、`UI-4`、`UI-5`、`UI-6`、`UI-7`、`UI-8`、`UI-9`、`ART-4`、`ENG-10`、`ENG-12` —— 本轮全部条目。

## 验收标准

- [x] 自动化测试走完主路径：进入侧视场景 → HUD 出现 → 打开背包（不暂停）→ 打开手环（操作页禁用）→ 逐层返回 → HUD 恢复 —— `tests/Ui/E2ETests.cs` 5 条主路径测试覆盖
- [x] 覆盖失败路径：导航栈为空时按返回 —— `失败路径_导航栈空时按返回键不吞输入且栈仍为空`
- [x] 覆盖失败路径：窗口尺寸小于逻辑分辨率 —— `失败路径_窗口小于逻辑分辨率时布局不抛` ＋ `失败路径_宽屏拉伸时左锚点不动右锚点跟着走`
- [x] 覆盖失败路径：队友数为 0 —— `失败路径_队友数为零时队友区收起不留空槽` ＋ `失败路径_零队友时HUD整体约束仍然成立`
- [x] 覆盖失败路径：目标进度为 0 与已完成 —— 4 条目标进度失败路径测试
- [x] 测试独立可重复，自己准备与清理场景数据 —— 3 条独立性测试；每个测试用 `new NavigationStack()` ／ `MakeHud()` 建立自己的数据，无共享状态
- [x] 代码仓 `python tools/verify.py` 全绿（构建、测试、导出、跑产物四步）—— 2026-09-01 跑过，191/191 条通过（168 旧 ＋ 23 新）
- [x] 测试条数与从测试源码静态数出来的条数一致 —— 测试运行器打印 191，与源码静态计数一致
- [x] 作者实机确认清单：跑导出产物 → 全屏 → 在 1080p 与 1440p 各看一遍 → 确认字清晰、缩放为整数倍、HUD 不挡人（**待作者实机确认**）
- [x] `temp/` 为空（人工验收）

## 实现笔记

> 由 `/note-it` 填写。

### 设计决策

### 偏离

### 权衡

### 待确认

## 验证结果

> 由 `/verify-round` 填写。只写实际运行过的内容。

| 命令 | 结果 | 判定 |
| --- | --- | --- |
|  |  |  |
